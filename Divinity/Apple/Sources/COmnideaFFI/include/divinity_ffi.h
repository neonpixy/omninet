// Divinity FFI — C bindings for the Omnidea Rust core.
// AUTO-GENERATED by cbindgen. Do not edit manually.


#ifndef DIVINITY_FFI_H
#define DIVINITY_FFI_H

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
// Opaque types — Equipment structs live in a separate crate that cbindgen
// doesn't parse (parse_deps = false), so we forward-declare them here.
// Crown types (CrownKeyring, CrownSoul) are auto-generated by cbindgen
// since they're defined in this crate.
typedef struct Phone Phone;
typedef struct Email Email;
typedef struct Contacts Contacts;
typedef struct Pager Pager;
typedef struct Omnibus Omnibus;
typedef struct LingoBabel LingoBabel;
typedef struct ExporterRegistry ExporterRegistry;
typedef struct ImporterRegistry ImporterRegistry;
typedef struct BridgeRegistry BridgeRegistry;
typedef struct FederationRegistry FederationRegistry;


/**
 * Thread-safe wrapper around `CognitiveLoop` for FFI.
 */
typedef struct AdvisorLoop AdvisorLoop;

/**
 * Thread-safe wrapper around `ProviderRouter` for FFI.
 */
typedef struct AdvisorRouter AdvisorRouter;

/**
 * Thread-safe wrapper around `SkillRegistry` for FFI.
 */
typedef struct AdvisorSkills AdvisorSkills;

/**
 * Thread-safe wrapper around `CognitiveStore` for FFI.
 */
typedef struct AdvisorStore AdvisorStore;

typedef struct BulwarkConsentValidator BulwarkConsentValidator;

typedef struct BulwarkPermissionChecker BulwarkPermissionChecker;

typedef struct CommerceCart CommerceCart;

typedef struct CommerceOrder CommerceOrder;

typedef struct CommerceProduct CommerceProduct;

typedef struct CrownKeyring CrownKeyring;

typedef struct CrownSoul CrownSoul;

typedef struct DiviAppCatalog DiviAppCatalog;

typedef struct DiviDeviceFleet DiviDeviceFleet;

/**
 * Shared async runtime for all crates that need async operations.
 *
 * Not Globe-specific — any future async crate (Advisor streaming, etc.)
 * can share this runtime.
 */
typedef struct DiviRuntime DiviRuntime;

typedef struct DiviSyncPriority DiviSyncPriority;

typedef struct DiviSyncTracker DiviSyncTracker;

typedef struct DiviVault DiviVault;

/**
 * Thread-safe wrapper around `DependencyGraph` for FFI.
 */
typedef struct FormulaDepsHandle FormulaDepsHandle;

/**
 * Thread-safe wrapper around `FormulaEvaluator` for FFI.
 */
typedef struct FormulaEvaluatorHandle FormulaEvaluatorHandle;

/**
 * Immutable AST node handle for FFI. No Mutex needed — immutable after parse.
 */
typedef struct FormulaNodeHandle FormulaNodeHandle;

/**
 * Thread-safe wrapper around `FunctionRegistry` for FFI.
 */
typedef struct FormulaRegistryHandle FormulaRegistryHandle;

typedef struct FortuneLedger FortuneLedger;

typedef struct FortuneTreasury FortuneTreasury;

typedef struct FortuneUbi FortuneUbi;

/**
 * Opaque wrapper for the mDNS advertiser.
 */
typedef struct GlobeAdvertiser GlobeAdvertiser;

/**
 * Opaque wrapper for the mDNS browser.
 */
typedef struct GlobeBrowser GlobeBrowser;

/**
 * Wraps RelayPool with a runtime reference for async operations.
 */
typedef struct GlobePool GlobePool;

/**
 * FFI wrapper for Globe's RelayServer.
 */
typedef struct GlobeServer GlobeServer;

/**
 * Thread-safe wrapper for IdeaPackage (filesystem I/O, mutable state).
 */
typedef struct IdeasPackage IdeasPackage;

/**
 * Thread-safe wrapper for SchemaRegistry (accumulates schemas, queried repeatedly).
 */
typedef struct IdeasSchemaRegistry IdeasSchemaRegistry;

typedef struct JailTrustGraph JailTrustGraph;

/**
 * Opaque wrapper for Babel (thread-safe).
 */
typedef struct LingoBabel LingoBabel;

typedef struct MagicCanvasState MagicCanvasState;

typedef struct MagicDocumentHistory MagicDocumentHistory;

typedef struct MagicDocumentState MagicDocumentState;

typedef struct MagicRenderCache MagicRenderCache;

typedef struct MagicRendererRegistry MagicRendererRegistry;

typedef struct MagicToolRegistry MagicToolRegistry;

/**
 * Opaque handle wrapping `oracle::WorkflowRegistry` behind a `Mutex`.
 */
typedef struct OracleWorkflowRegistry OracleWorkflowRegistry;

typedef struct PolityBreachRegistry PolityBreachRegistry;

typedef struct PolityConsentRegistry PolityConsentRegistry;

typedef struct PolityDutiesRegistry PolityDutiesRegistry;

typedef struct PolityEnactmentRegistry PolityEnactmentRegistry;

typedef struct PolityProtectionsRegistry PolityProtectionsRegistry;

typedef struct PolityRightsRegistry PolityRightsRegistry;

/**
 * Thread-safe wrapper around `QuestEngine` for FFI.
 */
typedef struct QuestEngineFFI QuestEngineFFI;

typedef struct RegaliaThemeCollection RegaliaThemeCollection;

typedef struct UndercraftHistory UndercraftHistory;

/**
 * Opaque wrapper around `LocalCache` for FFI.
 */
typedef struct ZeitgeistCache ZeitgeistCache;

/**
 * Opaque wrapper around `TowerDirectory` for FFI.
 */
typedef struct ZeitgeistDirectory ZeitgeistDirectory;

/**
 * Opaque wrapper around `QueryRouter` for FFI.
 */
typedef struct ZeitgeistRouter ZeitgeistRouter;

/**
 * Opaque wrapper around `TrendTracker` for FFI.
 */
typedef struct ZeitgeistTrends ZeitgeistTrends;

/**
 * C function pointer for querying provider status.
 *
 * Returns a `*mut c_char` containing JSON `ProviderStatus`.
 * The returned pointer must be allocated by the caller (Swift/C) side.
 */
typedef char *(*DiviAdvisorStatusCallback)(void *context);

/**
 * C function pointer for a shutdown callback.
 */
typedef void (*DiviShutdownCallback)(void *context);

/**
 * C function pointer for an Email subscriber.
 *
 * - Receives event bytes (`data` + `len`).
 * - Returns nothing (fire-and-forget).
 * - `context` is the opaque pointer passed at subscription time.
 */
typedef void (*DiviEmailHandler)(const uint8_t *data, uintptr_t len, void *context);

/**
 * C function pointer for resolving a single cell reference.
 *
 * - `cell_ref_json`: JSON-encoded `FormulaCellRef`.
 * - `context`: opaque pointer passed at call time.
 * - Returns a `*mut c_char` containing JSON-encoded `FormulaValue`.
 *   The returned pointer must be allocated with `malloc` or equivalent
 *   on the caller (Swift/C) side.
 */
typedef char *(*DiviFormulaCellResolver)(const char *cell_ref_json, void *context);

/**
 * C function pointer for resolving a range of cells.
 *
 * - `start_ref_json`: JSON-encoded `FormulaCellRef` for range start.
 * - `end_ref_json`: JSON-encoded `FormulaCellRef` for range end.
 * - `context`: opaque pointer passed at call time.
 * - Returns a `*mut c_char` containing a JSON array of `FormulaValue`.
 *   The returned pointer must be allocated with `malloc` or equivalent
 *   on the caller (Swift/C) side.
 */
typedef char *(*DiviFormulaRangeResolver)(const char *start_ref_json,
                                          const char *end_ref_json,
                                          void *context);

/**
 * Callback for receiving pool events.
 */
typedef void (*GlobeEventCallback)(const char *event_json, const char *source_relay, void *context);

/**
 * Callback for receiving Omnibus events.
 */
typedef void (*OmnibusEventCallback)(const char *event_json, const char *source_relay, void *context);

/**
 * C function pointer for a Phone handler.
 *
 * - Receives request bytes (`request_data` + `request_len`).
 * - Must write response bytes into `*response_data` and `*response_len`.
 *   The response buffer should be allocated with `malloc` (Swift/C side).
 * - Returns 0 on success, -1 on error.
 * - `context` is the opaque pointer passed at registration time.
 */
typedef int32_t (*DiviPhoneHandler)(const uint8_t *request_data,
                                    uintptr_t request_len,
                                    uint8_t **response_data,
                                    uintptr_t *response_len,
                                    void *context);

/**
 * Create a new cognitive loop.
 *
 * `config_json` is a JSON `AdvisorConfig` (or null for default).
 * `session_id` is a UUID string for the home session.
 * Free with `divi_advisor_loop_free`.
 *
 * # Safety
 * `session_id` must be a valid C string. `config_json` may be null.
 */
struct AdvisorLoop *divi_advisor_loop_new(const char *config_json, const char *session_id);

/**
 * Free a cognitive loop.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_advisor_loop_free(struct AdvisorLoop *ptr);

/**
 * Advance the loop by one tick. Returns JSON array of `CognitiveAction`.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
char *divi_advisor_loop_tick(const struct AdvisorLoop *loop_ptr, uint64_t elapsed_ms);

/**
 * Feed an LLM generation result back into the loop.
 *
 * `result_json` is a JSON `GenerationResult`.
 * Returns JSON array of `CognitiveAction`.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `loop_ptr` must be valid. `result_json` must be a valid C string.
 */
char *divi_advisor_loop_receive_generation(const struct AdvisorLoop *loop_ptr,
                                           const char *result_json);

/**
 * Apply a state command to the loop.
 *
 * `cmd_json` is a JSON `StateCommand`.
 * Returns JSON array of `CognitiveAction`.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `loop_ptr` must be valid. `cmd_json` must be a valid C string.
 */
char *divi_advisor_loop_apply_command(const struct AdvisorLoop *loop_ptr, const char *cmd_json);

/**
 * Set the energy level (0.0-1.0) on the cognitive loop.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
void divi_advisor_loop_set_energy(const struct AdvisorLoop *loop_ptr, double energy);

/**
 * Set the novelty level (0.0-1.0) on the cognitive loop.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
void divi_advisor_loop_set_novelty(const struct AdvisorLoop *loop_ptr, double novelty);

/**
 * Notify the loop that a conversation started.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
void divi_advisor_loop_begin_conversation(const struct AdvisorLoop *loop_ptr);

/**
 * Notify the loop that a conversation ended.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
void divi_advisor_loop_end_conversation(const struct AdvisorLoop *loop_ptr);

/**
 * Get a pressure snapshot from the loop.
 *
 * Returns JSON `PressureSnapshot`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
char *divi_advisor_loop_pressure_snapshot(const struct AdvisorLoop *loop_ptr);

/**
 * Get the current cognitive mode.
 *
 * Returns JSON `CognitiveMode`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
char *divi_advisor_loop_mode(const struct AdvisorLoop *loop_ptr);

/**
 * Get the current expression consent.
 *
 * Returns JSON `ExpressionConsent`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `loop_ptr` must be a valid pointer.
 */
char *divi_advisor_loop_consent(const struct AdvisorLoop *loop_ptr);

/**
 * Set expression consent on the loop.
 *
 * `consent_json` is a JSON `ExpressionConsent`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `loop_ptr` must be valid. `consent_json` must be a valid C string.
 */
int32_t divi_advisor_loop_set_consent(const struct AdvisorLoop *loop_ptr, const char *consent_json);

/**
 * Create a new cognitive store.
 *
 * `clipboard_max` is the maximum number of clipboard entries.
 * Free with `divi_advisor_store_free`.
 */
struct AdvisorStore *divi_advisor_store_new(uintptr_t clipboard_max);

/**
 * Free a cognitive store.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_advisor_store_free(struct AdvisorStore *ptr);

/**
 * Save a thought to the store.
 *
 * `thought_json` is a JSON `Thought`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `store` must be a valid pointer. `thought_json` must be a valid C string.
 */
int32_t divi_advisor_store_save_thought(const struct AdvisorStore *store, const char *thought_json);

/**
 * Get a thought by ID.
 *
 * `id` is a UUID string.
 * Returns JSON `Thought`, or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `store` must be valid. `id` must be a valid C string.
 */
char *divi_advisor_store_get_thought(const struct AdvisorStore *store, const char *id);

/**
 * Get all thoughts for a session.
 *
 * `session_id` is a UUID string.
 * Returns JSON array of `Thought`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `store` must be valid. `session_id` must be a valid C string.
 */
char *divi_advisor_store_thoughts_for_session(const struct AdvisorStore *store,
                                              const char *session_id);

/**
 * Delete a thought by ID.
 *
 * `id` is a UUID string.
 * Returns true if the thought existed and was deleted.
 *
 * # Safety
 * `store` must be valid. `id` must be a valid C string.
 */
bool divi_advisor_store_delete_thought(const struct AdvisorStore *store, const char *id);

/**
 * Save a session to the store.
 *
 * `session_json` is a JSON `Session`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `store` must be valid. `session_json` must be a valid C string.
 */
int32_t divi_advisor_store_save_session(const struct AdvisorStore *store, const char *session_json);

/**
 * Get a session by ID.
 *
 * `id` is a UUID string.
 * Returns JSON `Session`, or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `store` must be valid. `id` must be a valid C string.
 */
char *divi_advisor_store_get_session(const struct AdvisorStore *store, const char *id);

/**
 * Get all active (non-archived) sessions.
 *
 * Returns JSON array of `Session`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `store` must be a valid pointer.
 */
char *divi_advisor_store_active_sessions(const struct AdvisorStore *store);

/**
 * Save a memory to the store.
 *
 * `memory_json` is a JSON `Memory`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `store` must be valid. `memory_json` must be a valid C string.
 */
int32_t divi_advisor_store_save_memory(const struct AdvisorStore *store, const char *memory_json);

/**
 * Search memories by keyword.
 *
 * Returns JSON array of `MemoryResult`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `store` must be valid. `query` must be a valid C string.
 */
char *divi_advisor_store_search_memories(const struct AdvisorStore *store,
                                         const char *query,
                                         uintptr_t max_results);

/**
 * Save a synapse to the store.
 *
 * `synapse_json` is a JSON `Synapse`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `store` must be valid. `synapse_json` must be a valid C string.
 */
int32_t divi_advisor_store_save_synapse(const struct AdvisorStore *store, const char *synapse_json);

/**
 * Prune weak synapses below the given minimum strength.
 *
 * Returns the number of synapses pruned.
 *
 * # Safety
 * `store` must be a valid pointer.
 */
uintptr_t divi_advisor_store_prune_weak_synapses(const struct AdvisorStore *store,
                                                 double min_strength);

/**
 * Get the number of thoughts in the store.
 *
 * # Safety
 * `store` must be a valid pointer.
 */
uintptr_t divi_advisor_store_thought_count(const struct AdvisorStore *store);

/**
 * Get the number of sessions in the store.
 *
 * # Safety
 * `store` must be a valid pointer.
 */
uintptr_t divi_advisor_store_session_count(const struct AdvisorStore *store);

/**
 * Get the number of memories in the store.
 *
 * # Safety
 * `store` must be a valid pointer.
 */
uintptr_t divi_advisor_store_memory_count(const struct AdvisorStore *store);

/**
 * Get the number of synapses in the store.
 *
 * # Safety
 * `store` must be a valid pointer.
 */
uintptr_t divi_advisor_store_synapse_count(const struct AdvisorStore *store);

/**
 * Create a new provider router with an empty registry.
 * Free with `divi_advisor_router_free`.
 */
struct AdvisorRouter *divi_advisor_router_new(void);

/**
 * Free a provider router.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_advisor_router_free(struct AdvisorRouter *ptr);

/**
 * Register a provider in the router.
 *
 * `id` and `name` are C strings identifying the provider.
 * `capabilities_bitflags` is the raw u32 value of `ProviderCapabilities`.
 * `is_cloud` indicates whether the provider requires cloud access.
 * `status_fn` is called to get the provider's current status.
 * `context` is an opaque pointer passed to the status callback.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `router`, `id`, `name` must be valid pointers. `status_fn` must be valid.
 * `context` must be valid for the lifetime of the provider registration.
 */
int32_t divi_advisor_router_register_provider(const struct AdvisorRouter *router,
                                              const char *id,
                                              const char *name,
                                              uint32_t capabilities_bitflags,
                                              bool is_cloud,
                                              DiviAdvisorStatusCallback status_fn,
                                              void *context);

/**
 * Unregister a provider from the router.
 *
 * Returns 0 on success, -1 on error (provider not found).
 *
 * # Safety
 * `router` must be valid. `id` must be a valid C string.
 */
int32_t divi_advisor_router_unregister(const struct AdvisorRouter *router, const char *id);

/**
 * Set provider preferences on the router.
 *
 * `preferences_json` is a JSON `ProviderPreferences`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `router` must be valid. `preferences_json` must be a valid C string.
 */
int32_t divi_advisor_router_set_preferences(const struct AdvisorRouter *router,
                                            const char *preferences_json);

/**
 * Set the security tier on the router.
 *
 * `tier_json` is a JSON `SecurityTier`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `router` must be valid. `tier_json` must be a valid C string.
 */
int32_t divi_advisor_router_set_security_tier(const struct AdvisorRouter *router,
                                              const char *tier_json);

/**
 * Select the best provider for a request with the given capability requirements.
 *
 * `required_caps` is the raw u32 value of required `ProviderCapabilities`.
 * Returns JSON `ProviderInfo`, or null if no provider matches.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `router` must be a valid pointer.
 */
char *divi_advisor_router_select(const struct AdvisorRouter *router, uint32_t required_caps);

/**
 * Get info for all registered providers.
 *
 * Returns JSON array of `ProviderInfo`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `router` must be a valid pointer.
 */
char *divi_advisor_router_provider_info(const struct AdvisorRouter *router);

/**
 * Create a new empty skill registry.
 * Free with `divi_advisor_skills_free`.
 */
struct AdvisorSkills *divi_advisor_skills_new(void);

/**
 * Free a skill registry.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_advisor_skills_free(struct AdvisorSkills *ptr);

/**
 * Register a skill.
 *
 * `skill_json` is a JSON `SkillDefinition`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `skills` must be valid. `skill_json` must be a valid C string.
 */
int32_t divi_advisor_skills_register(const struct AdvisorSkills *skills, const char *skill_json);

/**
 * Unregister a skill by ID.
 *
 * Returns 0 on success, -1 on error (skill not found).
 *
 * # Safety
 * `skills` must be valid. `id` must be a valid C string.
 */
int32_t divi_advisor_skills_unregister(const struct AdvisorSkills *skills, const char *id);

/**
 * Get a skill by ID.
 *
 * Returns JSON `SkillDefinition`, or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `skills` must be valid. `id` must be a valid C string.
 */
char *divi_advisor_skills_get(const struct AdvisorSkills *skills, const char *id);

/**
 * Search skills by name/description.
 *
 * Returns JSON array of `SkillDefinition`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `skills` must be valid. `query` must be a valid C string.
 */
char *divi_advisor_skills_search(const struct AdvisorSkills *skills, const char *query);

/**
 * Get all registered skills.
 *
 * Returns JSON array of `SkillDefinition`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `skills` must be a valid pointer.
 */
char *divi_advisor_skills_all(const struct AdvisorSkills *skills);

/**
 * Get the number of registered skills.
 *
 * # Safety
 * `skills` must be a valid pointer.
 */
uintptr_t divi_advisor_skills_count(const struct AdvisorSkills *skills);

/**
 * Get the default `AdvisorConfig` as JSON.
 *
 * Caller must free via `divi_free_string`.
 */
char *divi_advisor_config_default(void);

/**
 * Get the contemplative `AdvisorConfig` preset as JSON.
 *
 * Caller must free via `divi_free_string`.
 */
char *divi_advisor_config_contemplative(void);

/**
 * Get the responsive `AdvisorConfig` preset as JSON.
 *
 * Caller must free via `divi_free_string`.
 */
char *divi_advisor_config_responsive(void);

/**
 * Create a new `Thought` as JSON.
 *
 * `session_id` is a UUID string.
 * `content` is the thought content.
 * `source_json` is a JSON `ThoughtSource`.
 * Returns JSON `Thought`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_advisor_thought_new(const char *session_id,
                               const char *content,
                               const char *source_json);

/**
 * Create a home session as JSON.
 *
 * Returns JSON `Session`. Caller must free via `divi_free_string`.
 */
char *divi_advisor_session_home(void);

/**
 * Create a user session as JSON.
 *
 * `title` is a C string for the session title.
 * Returns JSON `Session`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `title` must be a valid C string.
 */
char *divi_advisor_session_user(const char *title);

/**
 * Create a new sponsorship bond as JSON.
 *
 * `sponsor` is the sponsor's public key (crown_id).
 * `companion` is a UUID string for the companion identity.
 * Returns JSON `SponsorshipBond`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `sponsor` and `companion` must be valid C strings.
 */
char *divi_advisor_sponsorship_bond_new(const char *sponsor, const char *companion);

/**
 * Get the default `ExpressionConsent` as JSON.
 *
 * Returns JSON `ExpressionConsent`. Caller must free via `divi_free_string`.
 */
char *divi_advisor_expression_consent_default(void);

/**
 * Get capabilities for a trust layer.
 *
 * `layer_json` is a JSON TrustLayer (e.g. `"Verified"`).
 * Returns JSON (LayerCapabilities). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `layer_json` must be a valid C string.
 */
char *divi_bulwark_trust_layer_capabilities(const char *layer_json);

/**
 * Create a new visible bond between two parties.
 *
 * `depth_json` is a JSON BondDepth (e.g. `"Friend"`).
 * Returns JSON (VisibleBond). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_bond_new(const char *party_a, const char *party_b, const char *depth_json);

/**
 * Update a bond's depth from one party's perspective.
 *
 * Takes bond JSON, returns modified bond JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_bond_update_depth(const char *bond_json,
                                     const char *pubkey,
                                     const char *new_depth_json);

/**
 * Compute a user health pulse from factors.
 *
 * `factors_json` is a JSON UserHealthFactors.
 * Returns JSON (UserHealthPulse). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_user_health_compute(const char *pubkey, const char *factors_json);

/**
 * Compute a collective health pulse from factors.
 *
 * `collective_id` is a UUID string. `factors_json` is a JSON CollectiveHealthFactors.
 * Returns JSON (CollectiveHealthPulse). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_collective_health_compute(const char *collective_id,
                                             const char *factors_json,
                                             uint32_t contributing_members);

/**
 * Create a new reputation for a pubkey (starts at default scores).
 *
 * Returns JSON (Reputation). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `pubkey` must be a valid C string.
 */
char *divi_bulwark_reputation_new(const char *pubkey);

/**
 * Apply a reputation event to a reputation.
 *
 * Takes reputation JSON + event JSON, returns modified reputation JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_reputation_apply_event(const char *reputation_json, const char *event_json);

/**
 * File a child safety flag.
 *
 * `concern_json` is a JSON ChildSafetyConcern (e.g. `"Grooming"`).
 * Returns JSON (ChildSafetyFlag). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_child_safety_flag_file(const char *reporter_pubkey,
                                          const char *concern_json,
                                          const char *description);

/**
 * Get the default child safety protocol (all protections enabled).
 *
 * Returns JSON (ChildSafetyProtocol). Caller must free via `divi_free_string`.
 */
char *divi_bulwark_child_safety_protocol_default(void);

/**
 * Get US default real-world resources (911, 988, Childhelp).
 *
 * Returns JSON (RealWorldResources). Caller must free via `divi_free_string`.
 */
char *divi_bulwark_real_world_resources(void);

/**
 * Create a new empty permission checker.
 * Free with `divi_bulwark_permission_checker_free`.
 */
struct BulwarkPermissionChecker *divi_bulwark_permission_checker_new(void);

/**
 * Free a permission checker.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_bulwark_permission_checker_free(struct BulwarkPermissionChecker *ptr);

/**
 * Register an app-defined role in the permission checker.
 *
 * `role_json` is a JSON Role (name, description, permissions, etc.).
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `checker` must be a valid pointer. `role_json` must be a valid C string.
 */
int32_t divi_bulwark_permission_checker_register_role(const struct BulwarkPermissionChecker *checker,
                                                      const char *role_json);

/**
 * Check whether an actor can perform an action on a resource (no context).
 *
 * `actor_json` is a JSON ActorContext. `action` and `resource` are plain strings.
 * Returns JSON `{"allowed": true/false}`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `checker` must be a valid pointer. All C strings must be valid.
 */
char *divi_bulwark_permission_checker_can(const struct BulwarkPermissionChecker *checker,
                                          const char *actor_json,
                                          const char *action,
                                          const char *resource);

/**
 * Full permission check with context — returns decision with source/denial details.
 *
 * `actor_json` is a JSON ActorContext. `action` and `resource` are plain strings.
 * `context_json` is a JSON PermissionContext (may be null for empty context).
 * Returns JSON (FfiPermissionDecision). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `checker` must be a valid pointer. C strings must be valid. `context_json` may be null.
 */
char *divi_bulwark_permission_checker_check(const struct BulwarkPermissionChecker *checker,
                                            const char *actor_json,
                                            const char *action,
                                            const char *resource,
                                            const char *context_json);

/**
 * Grant a delegation in the permission checker.
 *
 * `delegation_json` is a JSON Delegation.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `checker` must be a valid pointer. `delegation_json` must be a valid C string.
 */
int32_t divi_bulwark_permission_checker_grant_delegation(const struct BulwarkPermissionChecker *checker,
                                                         const char *delegation_json);

/**
 * Revoke a delegation by UUID string.
 *
 * `delegation_id` is a UUID string.
 * Returns 0 on success (revoked), -1 on error (not found or invalid UUID).
 *
 * # Safety
 * `checker` must be a valid pointer. `delegation_id` must be a valid C string.
 */
int32_t divi_bulwark_permission_checker_revoke_delegation(const struct BulwarkPermissionChecker *checker,
                                                          const char *delegation_id);

/**
 * Get the default Kids Sphere config.
 *
 * Returns JSON (KidsSphereConfig). Caller must free via `divi_free_string`.
 */
char *divi_bulwark_kids_sphere_config_default(void);

/**
 * Get parent oversight settings appropriate for a given age.
 *
 * Returns JSON (ParentOversight). Caller must free via `divi_free_string`.
 */
char *divi_bulwark_parent_oversight_for_age(uint8_t age);

/**
 * Create a new siloed minor (local-only, no network access).
 *
 * `pubkey` is the minor's public key. `age` is their claimed age (0 if unknown).
 * `reason_json` is a JSON MinorDetectionReason (e.g. `"SelfDeclared"`).
 * Returns JSON (SiloedMinor). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_siloed_minor_new(const char *pubkey, uint8_t age, const char *reason_json);

/**
 * Link a parent to a siloed minor. Transitions state to ParentLinked.
 *
 * `minor_json` is a JSON SiloedMinor. `parent_link_json` is a JSON ParentLink.
 * Returns modified JSON (SiloedMinor). Caller must free via `divi_free_string`.
 * Returns null on error (e.g. already linked).
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_siloed_minor_link_parent(const char *minor_json, const char *parent_link_json);

/**
 * Authorize a parent-linked minor for Kids Sphere access.
 *
 * `minor_json` is a JSON SiloedMinor (must be in ParentLinked state).
 * Returns modified JSON (SiloedMinor). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * `minor_json` must be a valid C string.
 */
char *divi_bulwark_siloed_minor_authorize(const char *minor_json);

/**
 * Create a new family bond between two parents. REQUIRES proximity proof.
 *
 * `parent_a` and `parent_b` are pubkeys. `proof_json` is a JSON ProximityProof.
 * Returns JSON (FamilyBond). Caller must free via `divi_free_string`.
 * Returns null if proximity proof is invalid.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_family_bond_new(const char *parent_a,
                                   const char *parent_b,
                                   const char *proof_json);

/**
 * Create a new empty consent validator.
 * Free with `divi_bulwark_consent_validator_free`.
 */
struct BulwarkConsentValidator *divi_bulwark_consent_validator_new(void);

/**
 * Free a consent validator.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_bulwark_consent_validator_free(struct BulwarkConsentValidator *ptr);

/**
 * Grant a consent record.
 *
 * `record_json` is a JSON ConsentRecord.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `validator` must be a valid pointer. `record_json` must be a valid C string.
 */
int32_t divi_bulwark_consent_validator_grant(const struct BulwarkConsentValidator *validator,
                                             const char *record_json);

/**
 * Check if active consent exists from grantor to recipient for a scope.
 *
 * `grantor` and `recipient` are pubkey strings. `scope_json` is a JSON ConsentScope.
 * Returns true if active consent exists.
 *
 * # Safety
 * `validator` must be a valid pointer. All C strings must be valid.
 */
bool divi_bulwark_consent_validator_has_consent(const struct BulwarkConsentValidator *validator,
                                                const char *grantor,
                                                const char *recipient,
                                                const char *scope_json);

/**
 * Revoke all consent from grantor to recipient.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `validator` must be a valid pointer. C strings must be valid.
 */
int32_t divi_bulwark_consent_validator_revoke_all(const struct BulwarkConsentValidator *validator,
                                                  const char *grantor,
                                                  const char *recipient);

/**
 * Get the age tier for a given age.
 *
 * `config_json` may be null (uses default AgeTierConfig).
 * Returns JSON (AgeTier, e.g. `"Kid"`). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `config_json` may be null.
 */
char *divi_bulwark_age_tier_from_age(uint8_t age, const char *config_json);

/**
 * Check eligibility for a trust layer transition.
 *
 * `current_json` and `target_json` are JSON TrustLayer values.
 * `requirements_json` is a JSON LayerTransitionRequirements (null for defaults).
 * `evidence_json` is a JSON LayerTransitionEvidence.
 * Returns JSON with `allowed: bool` and `blockers: [string]`.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. `requirements_json` may be null.
 */
char *divi_bulwark_check_layer_transition(const char *current_json,
                                          const char *target_json,
                                          const char *requirements_json,
                                          const char *evidence_json);

/**
 * Compute a fraud risk score from indicators.
 *
 * `pubkey` is the subject's public key. `indicators_json` is a JSON array
 * of FraudIndicator.
 * Returns JSON (RiskScore). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_bulwark_risk_score_compute(const char *pubkey, const char *indicators_json);

/**
 * Create a new product linked to an .idea digit.
 *
 * Free with `divi_commerce_product_free`.
 *
 * # Safety
 * `idea_id` and `seller_pubkey` must be valid C strings.
 */
struct CommerceProduct *divi_commerce_product_new(const char *idea_id, const char *seller_pubkey);

/**
 * Free a product.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_commerce_product_free(struct CommerceProduct *ptr);

/**
 * Serialize a product to JSON.
 *
 * Returns JSON (Product). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `product` must be a valid pointer.
 */
char *divi_commerce_product_to_json(const struct CommerceProduct *product);

/**
 * Deserialize a product from JSON.
 *
 * Returns a new opaque pointer. Free with `divi_commerce_product_free`.
 *
 * # Safety
 * `json` must be a valid C string containing Product JSON.
 */
struct CommerceProduct *divi_commerce_product_from_json(const char *json);

/**
 * Add a variant to the product.
 *
 * `variant_json` is a JSON ProductVariant.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `product` must be a valid pointer. `variant_json` must be a valid C string.
 */
int32_t divi_commerce_product_add_variant(const struct CommerceProduct *product,
                                          const char *variant_json);

/**
 * Remove a variant by UUID string.
 *
 * Returns true if found and removed, false otherwise.
 *
 * # Safety
 * `product` must be a valid pointer. `variant_id` must be a valid C string UUID.
 */
bool divi_commerce_product_remove_variant(const struct CommerceProduct *product,
                                          const char *variant_id);

/**
 * Reserve stock for a pending order.
 *
 * Returns 0 on success, -1 on error (insufficient inventory).
 *
 * # Safety
 * `product` must be a valid pointer.
 */
int32_t divi_commerce_product_reserve(const struct CommerceProduct *product, uint32_t quantity);

/**
 * Release a stock reservation (e.g., cancelled order).
 *
 * # Safety
 * `product` must be a valid pointer.
 */
void divi_commerce_product_release_reservation(const struct CommerceProduct *product,
                                               uint32_t quantity);

/**
 * Set the product inventory from JSON.
 *
 * `inventory_json` is a JSON Inventory.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `product` must be a valid pointer. `inventory_json` must be a valid C string.
 */
int32_t divi_commerce_product_set_inventory(const struct CommerceProduct *product,
                                            const char *inventory_json);

/**
 * Activate the product listing.
 *
 * # Safety
 * `product` must be a valid pointer.
 */
void divi_commerce_product_activate(const struct CommerceProduct *product);

/**
 * Deactivate the product listing.
 *
 * # Safety
 * `product` must be a valid pointer.
 */
void divi_commerce_product_deactivate(const struct CommerceProduct *product);

/**
 * Whether the product is currently in stock.
 *
 * # Safety
 * `product` must be a valid pointer.
 */
bool divi_commerce_product_is_in_stock(const struct CommerceProduct *product);

/**
 * Available stock. Returns -1 for unlimited (None), actual count otherwise.
 *
 * # Safety
 * `product` must be a valid pointer.
 */
int64_t divi_commerce_product_available_stock(const struct CommerceProduct *product);

/**
 * Create an empty cart.
 *
 * Free with `divi_commerce_cart_free`.
 */
struct CommerceCart *divi_commerce_cart_new(void);

/**
 * Free a cart.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_commerce_cart_free(struct CommerceCart *ptr);

/**
 * Serialize the cart to JSON.
 *
 * Returns JSON (Cart). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `cart` must be a valid pointer.
 */
char *divi_commerce_cart_to_json(const struct CommerceCart *cart);

/**
 * Deserialize a cart from JSON.
 *
 * Returns a new opaque pointer. Free with `divi_commerce_cart_free`.
 *
 * # Safety
 * `json` must be a valid C string containing Cart JSON.
 */
struct CommerceCart *divi_commerce_cart_from_json(const char *json);

/**
 * Apply an action to the cart.
 *
 * `action_json` is a JSON CartAction (Add, Remove, UpdateQuantity, Clear).
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `cart` must be a valid pointer. `action_json` must be a valid C string.
 */
int32_t divi_commerce_cart_apply(const struct CommerceCart *cart, const char *action_json);

/**
 * Total price in Cool cents.
 *
 * # Safety
 * `cart` must be a valid pointer.
 */
int64_t divi_commerce_cart_total_cents(const struct CommerceCart *cart);

/**
 * Total item count (sum of quantities).
 *
 * # Safety
 * `cart` must be a valid pointer.
 */
uintptr_t divi_commerce_cart_item_count(const struct CommerceCart *cart);

/**
 * Whether the cart is empty.
 *
 * # Safety
 * `cart` must be a valid pointer.
 */
bool divi_commerce_cart_is_empty(const struct CommerceCart *cart);

/**
 * Find a cart item by product reference.
 *
 * Returns JSON (CartItem) or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `cart` must be a valid pointer. `product_ref` must be a valid C string.
 */
char *divi_commerce_cart_find_item(const struct CommerceCart *cart, const char *product_ref);

/**
 * Unique seller pubkeys in the cart.
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `cart` must be a valid pointer.
 */
char *divi_commerce_cart_sellers(const struct CommerceCart *cart);

/**
 * Items from a specific seller.
 *
 * Returns JSON array of CartItem. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `cart` must be a valid pointer. `seller` must be a valid C string.
 */
char *divi_commerce_cart_items_for_seller(const struct CommerceCart *cart, const char *seller);

/**
 * Create a new order.
 *
 * `items_json` is a JSON array of OrderItem.
 * Free with `divi_commerce_order_free`.
 *
 * # Safety
 * `buyer`, `seller`, and `items_json` must be valid C strings.
 */
struct CommerceOrder *divi_commerce_order_new(const char *buyer,
                                              const char *seller,
                                              const char *items_json);

/**
 * Free an order.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_commerce_order_free(struct CommerceOrder *ptr);

/**
 * Serialize an order to JSON.
 *
 * Returns JSON (Order). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `order` must be a valid pointer.
 */
char *divi_commerce_order_to_json(const struct CommerceOrder *order);

/**
 * Deserialize an order from JSON.
 *
 * Returns a new opaque pointer. Free with `divi_commerce_order_free`.
 *
 * # Safety
 * `json` must be a valid C string containing Order JSON.
 */
struct CommerceOrder *divi_commerce_order_from_json(const char *json);

/**
 * Advance the order status through its lifecycle.
 *
 * Placed -> Paid -> Preparing -> Shipped -> Delivered -> Confirmed.
 * Returns 0 on success, -1 on error (invalid transition).
 *
 * # Safety
 * `order` must be a valid pointer.
 */
int32_t divi_commerce_order_advance_status(const struct CommerceOrder *order);

/**
 * Dispute the order (only valid from Delivered status).
 *
 * Returns 0 on success, -1 on error (invalid transition).
 *
 * # Safety
 * `order` must be a valid pointer.
 */
int32_t divi_commerce_order_dispute(const struct CommerceOrder *order);

/**
 * Set the payment reference (Ledger transaction UUID).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `order` must be a valid pointer. `uuid_str` must be a valid C string UUID.
 */
int32_t divi_commerce_order_set_payment_ref(const struct CommerceOrder *order,
                                            const char *uuid_str);

/**
 * Set the delivery reference (Caravan delivery UUID).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `order` must be a valid pointer. `uuid_str` must be a valid C string UUID.
 */
int32_t divi_commerce_order_set_delivery_ref(const struct CommerceOrder *order,
                                             const char *uuid_str);

/**
 * Whether the order is in a terminal state (Confirmed or Disputed).
 *
 * # Safety
 * `order` must be a valid pointer.
 */
bool divi_commerce_order_is_complete(const struct CommerceOrder *order);

/**
 * Get the current order status as JSON.
 *
 * Returns JSON (OrderStatus). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `order` must be a valid pointer.
 */
char *divi_commerce_order_status(const struct CommerceOrder *order);

/**
 * Create a new storefront.
 *
 * Returns JSON (Storefront). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_commerce_storefront_new(const char *owner, const char *name, const char *description);

/**
 * Get default storefront policies as JSON.
 *
 * Returns JSON (StorefrontPolicies). Caller must free via `divi_free_string`.
 */
char *divi_commerce_storefront_default_policies(void);

/**
 * Create a receipt from an order.
 *
 * Clones the order data and wraps it in a Receipt. Returns JSON (Receipt).
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `order` must be a valid pointer.
 */
char *divi_commerce_receipt_from_order(const struct CommerceOrder *order);

/**
 * Create a checkout proposal from a cart for a specific seller.
 *
 * Returns JSON (TradeProposal). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * `cart` must be a valid pointer. `seller` and `buyer` must be valid C strings.
 */
char *divi_commerce_checkout_create_proposal(const struct CommerceCart *cart,
                                             const char *seller,
                                             const char *buyer);

/**
 * Execute a checkout: convert a TradeProposal + Cart into an Order.
 *
 * `proposal_json` is a JSON TradeProposal. `cart` is the opaque cart pointer.
 * Returns a new `CommerceOrder` opaque pointer. Free with `divi_commerce_order_free`.
 * Returns null on error.
 *
 * # Safety
 * `proposal_json` must be a valid C string. `cart` must be a valid pointer.
 */
struct CommerceOrder *divi_commerce_checkout_execute(const char *proposal_json,
                                                     const struct CommerceCart *cart);

/**
 * Create a new Contacts registry. Returns an opaque pointer.
 * Free with `divi_contacts_free`.
 */
Contacts *divi_contacts_new(void);

/**
 * Free a Contacts created by `divi_contacts_new`.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_contacts_new`, called exactly once.
 */
void divi_contacts_free(Contacts *ptr);

/**
 * Register a module from a JSON string.
 *
 * The JSON must deserialize to a `ModuleInfo`:
 * ```json
 * {"id": "vault", "name": "Vault", "module_type": "source", "depends_on": ["sentinal"]}
 * ```
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `contacts` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_contacts_register(const Contacts *contacts, const char *json);

/**
 * Register a module with a shutdown callback.
 *
 * Same as `divi_contacts_register`, but the `callback` is invoked when the
 * module is unregistered or during `divi_contacts_shutdown_all`.
 *
 * # Safety
 * `contacts` must be a valid pointer. `json` must be a valid C string.
 * `context` must be valid until the callback fires, or null.
 */
int32_t divi_contacts_register_with_shutdown(const Contacts *contacts,
                                             const char *json,
                                             DiviShutdownCallback callback,
                                             void *context);

/**
 * Unregister a module by ID. Dependents are shut down first.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `contacts` must be a valid pointer. `module_id` must be a valid C string.
 */
int32_t divi_contacts_unregister(const Contacts *contacts, const char *module_id);

/**
 * Shut down all modules in dependency order.
 *
 * # Safety
 * `contacts` must be a valid pointer.
 */
void divi_contacts_shutdown_all(const Contacts *contacts);

/**
 * Look up a module by ID. Returns JSON string or null if not found.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `contacts` must be a valid pointer. `module_id` must be a valid C string.
 */
char *divi_contacts_lookup(const Contacts *contacts, const char *module_id);

/**
 * Get all registered modules as a JSON array string.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `contacts` must be a valid pointer.
 */
char *divi_contacts_all_modules(const Contacts *contacts);

/**
 * Get all registered module IDs as a JSON array string.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `contacts` must be a valid pointer.
 */
char *divi_contacts_registered_module_ids(const Contacts *contacts);

/**
 * Get all modules that depend on the given module, as a JSON array.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `contacts` must be a valid pointer. `module_id` must be a valid C string.
 */
char *divi_contacts_dependents_of(const Contacts *contacts, const char *module_id);

/**
 * Start advertising this device's relay on the local network.
 *
 * `instance_name` is a human-readable name (e.g., "Sam's Mac").
 * `port` is the relay server port.
 * `pubkey_hex` is optional — the device's public key (null if not known yet).
 *
 * Returns an advertiser pointer. Free with `divi_discovery_advertiser_free`.
 *
 * # Safety
 * `instance_name` must be a valid C string. `pubkey_hex` may be null.
 */
struct GlobeAdvertiser *divi_discovery_advertise(const char *instance_name,
                                                 uint16_t port,
                                                 const char *pubkey_hex);

/**
 * Stop advertising and free the advertiser.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_discovery_advertise`, called exactly once.
 */
void divi_discovery_advertiser_free(struct GlobeAdvertiser *ptr);

/**
 * Start browsing for Omnidea relays on the local network.
 *
 * Returns a browser pointer. Free with `divi_discovery_browser_free`.
 */
struct GlobeBrowser *divi_discovery_browse(void);

/**
 * Get all currently discovered peers as a JSON array.
 *
 * Returns JSON like: `[{"name":"Sam's Mac","addresses":["192.168.1.5"],"port":8080,"pubkey_hex":null}]`
 * Free via `divi_free_string`.
 *
 * # Safety
 * `browser` must be a valid pointer from `divi_discovery_browse`.
 */
char *divi_discovery_peers(const struct GlobeBrowser *browser);

/**
 * Get the number of currently discovered peers.
 *
 * # Safety
 * `browser` must be a valid pointer from `divi_discovery_browse`.
 */
uint32_t divi_discovery_peer_count(const struct GlobeBrowser *browser);

/**
 * Stop browsing and free the browser.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_discovery_browse`, called exactly once.
 */
void divi_discovery_browser_free(struct GlobeBrowser *ptr);

/**
 * Create a new empty (locked) keyring.
 * Free with `divi_crown_keyring_free`.
 */
struct CrownKeyring *divi_crown_keyring_new(void);

/**
 * Free a keyring.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_crown_keyring_new`, called exactly once.
 */
void divi_crown_keyring_free(struct CrownKeyring *ptr);

/**
 * Whether the keyring has a primary identity loaded.
 *
 * # Safety
 * `keyring` must be a valid pointer.
 */
bool divi_crown_keyring_is_unlocked(const struct CrownKeyring *keyring);

/**
 * Generate a new random primary keypair.
 *
 * Returns JSON: `{"crown_id": "cpub1...", "cpub_hex": "abcd..."}`.
 * Returns null on error (e.g., primary already exists).
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer.
 */
char *divi_crown_keyring_generate_primary(const struct CrownKeyring *keyring);

/**
 * Import a primary keypair from an crown_secret bech32 string.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `keyring` must be a valid pointer. `crown_secret` must be a valid C string.
 */
int32_t divi_crown_keyring_import_primary(const struct CrownKeyring *keyring,
                                          const char *crown_secret);

/**
 * Get the primary identity's crown_id string.
 *
 * Returns null if locked. The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer.
 */
char *divi_crown_keyring_public_key(const struct CrownKeyring *keyring);

/**
 * Get the primary identity's public key as a 64-char hex string.
 *
 * Returns null if locked. The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer.
 */
char *divi_crown_keyring_public_key_hex(const struct CrownKeyring *keyring);

/**
 * Sign data with the primary identity.
 *
 * Returns JSON Signature: `{"data": "hex...", "signer": "cpub1...", "timestamp": "..."}`.
 * Returns null on error. The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer. `data` must be valid for `data_len` bytes.
 */
char *divi_crown_keyring_sign(const struct CrownKeyring *keyring,
                              const uint8_t *data,
                              uintptr_t data_len);

/**
 * Export the keyring as JSON bytes (hex-encoded private keys).
 *
 * The caller should encrypt these bytes before persisting.
 * Returns 0 on success, -1 on error. Output written to `out_data`/`out_len`.
 * The caller must free `out_data` via `divi_free_bytes`.
 *
 * # Safety
 * `keyring` must be a valid pointer. `out_data` and `out_len` must be valid pointers.
 */
int32_t divi_crown_keyring_export(const struct CrownKeyring *keyring,
                                  uint8_t **out_data,
                                  uintptr_t *out_len);

/**
 * Load keyring state from JSON bytes (previously exported).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `keyring` must be a valid pointer. `data` must be valid for `data_len` bytes.
 */
int32_t divi_crown_keyring_load(const struct CrownKeyring *keyring,
                                const uint8_t *data,
                                uintptr_t data_len);

/**
 * Lock the keyring — clear all keys from memory.
 *
 * # Safety
 * `keyring` must be a valid pointer.
 */
void divi_crown_keyring_lock(const struct CrownKeyring *keyring);

/**
 * List persona names as a JSON array of strings.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer.
 */
char *divi_crown_keyring_list_personas(const struct CrownKeyring *keyring);

/**
 * Create a new persona keypair.
 *
 * Returns the persona's crown_id as a string, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer. `name` must be a valid C string.
 */
char *divi_crown_keyring_create_persona(const struct CrownKeyring *keyring, const char *name);

/**
 * Delete a named persona.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `keyring` must be a valid pointer. `name` must be a valid C string.
 */
int32_t divi_crown_keyring_delete_persona(const struct CrownKeyring *keyring, const char *name);

/**
 * Create a new in-memory soul with defaults.
 * Free with `divi_crown_soul_free`.
 */
struct CrownSoul *divi_crown_soul_new(void);

/**
 * Create a new soul at the given path, writing soul.json with defaults.
 *
 * Returns null on error. Free with `divi_crown_soul_free`.
 *
 * # Safety
 * `path` must be a valid C string.
 */
struct CrownSoul *divi_crown_soul_create(const char *path);

/**
 * Load an existing soul from a directory (reads soul.json).
 *
 * Returns null on error. Free with `divi_crown_soul_free`.
 *
 * # Safety
 * `path` must be a valid C string.
 */
struct CrownSoul *divi_crown_soul_load(const char *path);

/**
 * Create a new soul at the given path with at-rest encryption.
 *
 * `key` must be a 32-byte AES-256-GCM key (from `divi_sentinal_derive_soul_key`
 * or Vault's `Custodian::soul_key()`). The soul.json file is encrypted before
 * writing and decrypted on load.
 *
 * Returns null on error. Free with `divi_crown_soul_free`.
 *
 * # Safety
 * `path` must be a valid C string. `key` must be valid for `key_len` bytes.
 */
struct CrownSoul *divi_crown_soul_create_encrypted(const char *path,
                                                   const uint8_t *key,
                                                   uintptr_t key_len);

/**
 * Load an existing soul from a directory with at-rest decryption.
 *
 * `key` must be a 32-byte AES-256-GCM key. For backward compatibility,
 * if the file contains unencrypted plaintext JSON, loading still succeeds
 * (migration case). Subsequent saves will encrypt the data.
 *
 * Returns null on error. Free with `divi_crown_soul_free`.
 *
 * # Safety
 * `path` must be a valid C string. `key` must be valid for `key_len` bytes.
 */
struct CrownSoul *divi_crown_soul_load_encrypted(const char *path,
                                                 const uint8_t *key,
                                                 uintptr_t key_len);

/**
 * Free a soul.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_crown_soul_*` constructor, called exactly once.
 */
void divi_crown_soul_free(struct CrownSoul *ptr);

/**
 * Save the soul to disk.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `soul` must be a valid pointer.
 */
int32_t divi_crown_soul_save(const struct CrownSoul *soul);

/**
 * Whether the soul has unsaved changes.
 *
 * # Safety
 * `soul` must be a valid pointer.
 */
bool divi_crown_soul_is_dirty(const struct CrownSoul *soul);

/**
 * Get the profile as JSON.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `soul` must be a valid pointer.
 */
char *divi_crown_soul_profile(const struct CrownSoul *soul);

/**
 * Update the profile from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `soul` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_crown_soul_update_profile(const struct CrownSoul *soul, const char *json);

/**
 * Get preferences as JSON.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `soul` must be a valid pointer.
 */
char *divi_crown_soul_preferences(const struct CrownSoul *soul);

/**
 * Update preferences from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `soul` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_crown_soul_update_preferences(const struct CrownSoul *soul, const char *json);

/**
 * Get the social graph as JSON.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `soul` must be a valid pointer.
 */
char *divi_crown_soul_social_graph(const struct CrownSoul *soul);

/**
 * Follow an crown_id.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
void divi_crown_soul_follow(const struct CrownSoul *soul, const char *crown_id);

/**
 * Unfollow an crown_id.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
void divi_crown_soul_unfollow(const struct CrownSoul *soul, const char *crown_id);

/**
 * Block an crown_id (also removes from following).
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
void divi_crown_soul_block(const struct CrownSoul *soul, const char *crown_id);

/**
 * Unblock an crown_id.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
void divi_crown_soul_unblock(const struct CrownSoul *soul, const char *crown_id);

/**
 * Check if an crown_id is followed.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
bool divi_crown_soul_is_following(const struct CrownSoul *soul, const char *crown_id);

/**
 * Check if an crown_id is blocked.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
bool divi_crown_soul_is_blocked(const struct CrownSoul *soul, const char *crown_id);

/**
 * Check if an crown_id is muted.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
bool divi_crown_soul_is_muted(const struct CrownSoul *soul, const char *crown_id);

/**
 * Mute an crown_id.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
void divi_crown_soul_mute(const struct CrownSoul *soul, const char *crown_id);

/**
 * Unmute an crown_id.
 *
 * # Safety
 * `soul` must be a valid pointer. `crown_id` must be a valid C string.
 */
void divi_crown_soul_unmute(const struct CrownSoul *soul, const char *crown_id);

/**
 * Derive a blinded keypair from a keyring's primary identity and a context.
 *
 * `context_json` is a JSON BlindingContext (e.g., `{"context_id":"community:woodworkers","version":0}`).
 * Returns JSON with the blinded public key: `{"crown_id":"cpub1...","cpub_hex":"abcd..."}`.
 * Returns null on error. Caller must free via `divi_free_string`.
 *
 * The blinded key is cached in the keyring for subsequent signing.
 *
 * # Safety
 * `keyring` must be a valid pointer. `context_json` must be a valid C string.
 */
char *divi_crown_derive_blinded_keypair(const struct CrownKeyring *keyring,
                                        const char *context_json);

/**
 * Sign data with a blinded keypair for a specific context.
 *
 * The blinded key must already be in the keyring's cache (via
 * `divi_crown_derive_blinded_keypair`).
 *
 * Returns JSON Signature, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer. `data` must be valid for `data_len` bytes.
 * `context_json` must be a valid C string.
 */
char *divi_crown_sign_blinded(const struct CrownKeyring *keyring,
                              const uint8_t *data,
                              uintptr_t data_len,
                              const char *context_json);

/**
 * Create a blinding proof: the master key signs a binding message proving
 * ownership of the blinded key within a specific context.
 *
 * `blinded_crown_id` is the bech32 crown_id of the blinded key.
 * `context_json` is a JSON BlindingContext.
 * Returns JSON BlindingProof, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `keyring` must be a valid pointer. `blinded_crown_id` and `context_json` must be
 * valid C strings.
 */
char *divi_crown_blinding_proof_create(const struct CrownKeyring *keyring,
                                       const char *blinded_crown_id,
                                       const char *context_json);

/**
 * Verify a blinding proof.
 *
 * `proof_json` is a JSON BlindingProof.
 * Returns 1 if valid, 0 if invalid, -1 on error.
 *
 * # Safety
 * `proof_json` must be a valid C string.
 */
int32_t divi_crown_blinding_proof_verify(const char *proof_json);

/**
 * Clear all cached blinded keys from a keyring.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `keyring` must be a valid pointer.
 */
int32_t divi_crown_clear_blinded_cache(const struct CrownKeyring *keyring);

/**
 * Compute an ECDH shared secret between a keyring's primary key and
 * another party's public key (32 bytes, x-only).
 *
 * Both sides independently arrive at the same 32-byte shared secret.
 * Output written to `out_secret`/`out_secret_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `keyring` must be a valid pointer. `their_pubkey` must be 32 bytes.
 */
int32_t divi_crown_shared_secret(const struct CrownKeyring *keyring,
                                 const uint8_t *their_pubkey,
                                 uint8_t **out_secret,
                                 uintptr_t *out_secret_len);

/**
 * Recover a Keyring from a raw 32-byte private key.
 *
 * Takes the raw identity key bytes (e.g., from `divi_sentinal_derive_identity_key`)
 * and produces a new Keyring with that key imported as the primary identity.
 *
 * Returns a new CrownKeyring pointer on success, null on error.
 * Caller must free via `divi_crown_keyring_free`.
 *
 * # Safety
 * `secret` must be valid for `secret_len` bytes (must be exactly 32 bytes).
 */
struct CrownKeyring *divi_crown_recover_from_secret(const uint8_t *secret, uintptr_t secret_len);

/**
 * Create a new Email hub. Returns an opaque pointer.
 * Free with `divi_email_free`.
 */
Email *divi_email_new(void);

/**
 * Free an Email created by `divi_email_new`.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_email_new`, called exactly once.
 */
void divi_email_free(Email *ptr);

/**
 * Subscribe a raw handler for an email ID.
 *
 * On success, writes the 16-byte subscriber UUID into `out_uuid` and returns 0.
 * On error, returns -1.
 *
 * # Safety
 * `email` must be a valid pointer from `divi_email_new`.
 * `email_id` must be a valid null-terminated C string.
 * `out_uuid` must point to at least 16 writable bytes.
 * `context` must be valid for the lifetime of the subscription, or null.
 */
int32_t divi_email_subscribe_raw(const Email *email,
                                 const char *email_id,
                                 DiviEmailHandler handler,
                                 void *context,
                                 uint8_t *out_uuid);

/**
 * Send raw bytes to all subscribers of an email ID. Fire-and-forget.
 *
 * # Safety
 * `email` must be a valid pointer from `divi_email_new`.
 * `email_id` must be a valid null-terminated C string.
 * `data` must point to `data_len` valid bytes (or be null if `data_len` is 0).
 */
void divi_email_send_raw(const Email *email,
                         const char *email_id,
                         const uint8_t *data,
                         uintptr_t data_len);

/**
 * Unsubscribe a specific subscriber by UUID (16 bytes).
 *
 * # Safety
 * `email` must be a valid pointer. `uuid_bytes` must point to 16 valid bytes.
 */
void divi_email_unsubscribe(const Email *email, const uint8_t *uuid_bytes);

/**
 * Unsubscribe all subscribers for an email ID.
 *
 * # Safety
 * `email` must be a valid pointer. `email_id` must be a valid C string.
 */
void divi_email_unsubscribe_all(const Email *email, const char *email_id);

/**
 * Check if any subscribers exist for an email ID.
 *
 * # Safety
 * `email` must be a valid pointer. `email_id` must be a valid C string.
 */
bool divi_email_has_subscribers(const Email *email, const char *email_id);

/**
 * Get all active email IDs as a JSON array string.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `email` must be a valid pointer.
 */
char *divi_email_active_email_ids(const Email *email);

/**
 * Parse and evaluate a formula string in one call.
 *
 * This is the most common use case: given a formula string like `"=SUM(A1:A10)"`,
 * parse it, evaluate it using the provided cell resolver callbacks, and return
 * the result as a JSON-encoded `FormulaValue`.
 *
 * Returns a `*mut c_char` containing JSON `FormulaValue`, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `evaluator` must be a valid pointer from `divi_formula_evaluator_new`.
 * `formula` must be a valid C string.
 * `resolve_fn` and `resolve_range_fn` must be valid function pointers.
 * `context` must be valid for the duration of the call, or null.
 */
char *divi_formula_evaluate(const struct FormulaEvaluatorHandle *evaluator,
                            const char *formula,
                            DiviFormulaCellResolver resolve_fn,
                            DiviFormulaRangeResolver resolve_range_fn,
                            void *context);

/**
 * Parse a formula string into an opaque AST node.
 *
 * Returns a `*mut FormulaNodeHandle`, or null on parse error.
 * Free with `divi_formula_node_free`.
 *
 * # Safety
 * `formula` must be a valid C string.
 */
struct FormulaNodeHandle *divi_formula_parse(const char *formula);

/**
 * Free a parsed formula node.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_formula_parse`, called exactly once.
 */
void divi_formula_node_free(struct FormulaNodeHandle *ptr);

/**
 * Create a new formula evaluator with default built-in functions.
 * Free with `divi_formula_evaluator_free`.
 */
struct FormulaEvaluatorHandle *divi_formula_evaluator_new(void);

/**
 * Create a new formula evaluator with a custom function registry.
 *
 * The registry is consumed — its functions are cloned into the evaluator.
 * The registry handle remains valid and can be reused.
 *
 * Returns a `*mut FormulaEvaluatorHandle`. Free with `divi_formula_evaluator_free`.
 *
 * # Safety
 * `registry` must be a valid pointer from `divi_formula_registry_new` or
 * `divi_formula_registry_with_defaults`.
 */
struct FormulaEvaluatorHandle *divi_formula_evaluator_with_registry(const struct FormulaRegistryHandle *registry);

/**
 * Free a formula evaluator.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_formula_evaluator_*` constructor,
 * called exactly once.
 */
void divi_formula_evaluator_free(struct FormulaEvaluatorHandle *ptr);

/**
 * Evaluate a previously parsed formula node.
 *
 * Returns a `*mut c_char` containing JSON `FormulaValue`, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `evaluator` must be a valid pointer from `divi_formula_evaluator_new`.
 * `node` must be a valid pointer from `divi_formula_parse`.
 * `resolve_fn` and `resolve_range_fn` must be valid function pointers.
 * `context` must be valid for the duration of the call, or null.
 */
char *divi_formula_evaluator_evaluate(const struct FormulaEvaluatorHandle *evaluator,
                                      const struct FormulaNodeHandle *node,
                                      DiviFormulaCellResolver resolve_fn,
                                      DiviFormulaRangeResolver resolve_range_fn,
                                      void *context);

/**
 * Create a new empty function registry.
 * Free with `divi_formula_registry_free`.
 */
struct FormulaRegistryHandle *divi_formula_registry_new(void);

/**
 * Create a new function registry pre-populated with all 23 built-in functions.
 * Free with `divi_formula_registry_free`.
 */
struct FormulaRegistryHandle *divi_formula_registry_with_defaults(void);

/**
 * Free a function registry.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_formula_registry_*` constructor,
 * called exactly once.
 */
void divi_formula_registry_free(struct FormulaRegistryHandle *ptr);

/**
 * Check if a function is registered by name.
 *
 * # Safety
 * `registry` must be a valid pointer. `name` must be a valid C string.
 */
bool divi_formula_registry_has(const struct FormulaRegistryHandle *registry, const char *name);

/**
 * Create a new empty dependency graph.
 * Free with `divi_formula_deps_free`.
 */
struct FormulaDepsHandle *divi_formula_deps_new(void);

/**
 * Free a dependency graph.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_formula_deps_new`, called exactly once.
 */
void divi_formula_deps_free(struct FormulaDepsHandle *ptr);

/**
 * Add a dependency: `cell` depends on `depends_on`.
 *
 * For example, if A1 = B1 + C1, call this twice:
 * `divi_formula_deps_add(graph, "A1", "B1")` and
 * `divi_formula_deps_add(graph, "A1", "C1")`.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `graph` must be a valid pointer. `cell` and `depends_on` must be valid C strings.
 */
int32_t divi_formula_deps_add(const struct FormulaDepsHandle *graph,
                              const char *cell,
                              const char *depends_on);

/**
 * Remove all dependencies for a cell (e.g., when its formula changes).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `graph` must be a valid pointer. `cell` must be a valid C string.
 */
int32_t divi_formula_deps_remove_cell(const struct FormulaDepsHandle *graph, const char *cell);

/**
 * Check if evaluating `cell` would create a circular reference.
 *
 * # Safety
 * `graph` must be a valid pointer. `cell` must be a valid C string.
 */
bool divi_formula_deps_has_circular(const struct FormulaDepsHandle *graph, const char *cell);

/**
 * Get all cells that directly or indirectly depend on the given cell.
 *
 * Returns a `*mut c_char` containing a JSON array of cell name strings.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `graph` must be a valid pointer. `cell` must be a valid C string.
 */
char *divi_formula_deps_dependents(const struct FormulaDepsHandle *graph, const char *cell);

/**
 * Compute a topological evaluation order (cells with no dependencies first).
 *
 * Returns a `*mut c_char` containing a JSON array of cell name strings
 * in evaluation order. Returns null if a circular reference is detected
 * (check `divi_last_error()` for details).
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `graph` must be a valid pointer.
 */
char *divi_formula_deps_evaluation_order(const struct FormulaDepsHandle *graph);

/**
 * Convert a canonical (English) formula to localized display form.
 *
 * Translates function names and adjusts separators for the target locale.
 * For example, `"=SUM(1.5, 2.5)"` with locale `"fr"` becomes
 * `"=SOMME(1,5; 2,5)"`.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `locale` and `formula` must be valid C strings.
 */
char *divi_formula_locale_to_display(const char *locale, const char *formula);

/**
 * Convert a localized formula display string to canonical (English) form
 * for storage.
 *
 * For example, `"=SOMME(1,5; 2,5)"` with locale `"fr"` becomes
 * `"=SUM(1.5, 2.5)"`.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `locale` and `display` must be valid C strings.
 */
char *divi_formula_locale_to_canonical(const char *locale, const char *display);

/**
 * Get the localized name for a canonical function name.
 *
 * For example, `"SUM"` with locale `"fr"` returns `"SOMME"`.
 * Returns the canonical name unchanged if no translation exists.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `locale` and `name` must be valid C strings.
 */
char *divi_formula_locale_localize_name(const char *locale, const char *name);

/**
 * Create a new empty ledger.
 * Free with `divi_fortune_ledger_free`.
 */
struct FortuneLedger *divi_fortune_ledger_new(void);

/**
 * Free a ledger.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_fortune_ledger_free(struct FortuneLedger *ptr);

/**
 * Get balance for a pubkey.
 *
 * Returns JSON (Balance). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ledger` must be a valid pointer. `pubkey` must be a valid C string.
 */
char *divi_fortune_ledger_balance(const struct FortuneLedger *ledger, const char *pubkey);

/**
 * Credit a pubkey with Cool.
 *
 * `reason_json` is a JSON TransactionReason. `reference` may be null.
 * Returns 0 on success.
 *
 * # Safety
 * `ledger` must be a valid pointer. C strings must be valid.
 */
int32_t divi_fortune_ledger_credit(const struct FortuneLedger *ledger,
                                   const char *pubkey,
                                   int64_t amount,
                                   const char *reason_json,
                                   const char *reference);

/**
 * Debit Cool from a pubkey.
 *
 * Returns 0 on success, -1 on error (insufficient balance).
 *
 * # Safety
 * `ledger` must be a valid pointer. C strings must be valid.
 */
int32_t divi_fortune_ledger_debit(const struct FortuneLedger *ledger,
                                  const char *pubkey,
                                  int64_t amount,
                                  const char *reason_json,
                                  const char *reference);

/**
 * Transfer Cool between two pubkeys.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `ledger` must be a valid pointer. C strings must be valid.
 */
int32_t divi_fortune_ledger_transfer(const struct FortuneLedger *ledger,
                                     const char *sender,
                                     const char *recipient,
                                     int64_t amount,
                                     const char *reference);

/**
 * Get transaction history for a pubkey.
 *
 * Returns JSON array of Transactions. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ledger` must be a valid pointer. `pubkey` must be a valid C string.
 */
char *divi_fortune_ledger_transactions(const struct FortuneLedger *ledger, const char *pubkey);

/**
 * Get a transaction summary for a pubkey.
 *
 * Returns JSON (TransactionSummary). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ledger` must be a valid pointer. `pubkey` must be a valid C string.
 */
char *divi_fortune_ledger_summary(const struct FortuneLedger *ledger, const char *pubkey);

/**
 * Create a new treasury with a policy.
 *
 * `policy_json` is a JSON FortunePolicy (or null for default).
 * Free with `divi_fortune_treasury_free`.
 *
 * # Safety
 * `policy_json` may be null (uses default).
 */
struct FortuneTreasury *divi_fortune_treasury_new(const char *policy_json);

/**
 * Free a treasury.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_fortune_treasury_free(struct FortuneTreasury *ptr);

/**
 * Get treasury status.
 *
 * Returns JSON (TreasuryStatus). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `treasury` must be a valid pointer.
 */
char *divi_fortune_treasury_status(const struct FortuneTreasury *treasury);

/**
 * Mint Cool into the treasury for a recipient.
 *
 * `reason_json` is a JSON MintReason. Returns the actual amount minted (may be capped),
 * or -1 on error.
 *
 * # Safety
 * `treasury` must be a valid pointer. C strings must be valid.
 */
int64_t divi_fortune_treasury_mint(const struct FortuneTreasury *treasury,
                                   int64_t amount,
                                   const char *recipient,
                                   const char *reason_json);

/**
 * Update network metrics in the treasury.
 *
 * `metrics_json` is a JSON NetworkMetrics.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `treasury` must be a valid pointer. `metrics_json` must be a valid C string.
 */
int32_t divi_fortune_treasury_update_metrics(const struct FortuneTreasury *treasury,
                                             const char *metrics_json);

/**
 * Create a new UBI distributor.
 * Free with `divi_fortune_ubi_free`.
 */
struct FortuneUbi *divi_fortune_ubi_new(void);

/**
 * Free a UBI distributor.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_fortune_ubi_free(struct FortuneUbi *ptr);

/**
 * Mark a pubkey as verified for UBI eligibility.
 *
 * # Safety
 * `ubi` must be a valid pointer. `pubkey` must be a valid C string.
 */
void divi_fortune_ubi_verify_identity(const struct FortuneUbi *ubi, const char *pubkey);

/**
 * Check UBI eligibility for a pubkey.
 *
 * Returns JSON (UbiEligibility). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All pointers must be valid. `policy_json` may be null (uses default).
 */
char *divi_fortune_ubi_check_eligibility(const struct FortuneUbi *ubi,
                                         const char *pubkey,
                                         const struct FortuneLedger *ledger,
                                         const struct FortuneTreasury *treasury,
                                         const char *policy_json);

/**
 * Claim UBI for a pubkey.
 *
 * Returns JSON (ClaimRecord) on success. Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * All pointers must be valid. `policy_json` may be null.
 */
char *divi_fortune_ubi_claim(const struct FortuneUbi *ubi,
                             const char *pubkey,
                             const struct FortuneLedger *ledger,
                             const struct FortuneTreasury *treasury,
                             const char *policy_json);

/**
 * Preview demurrage for a pubkey.
 *
 * Returns JSON (DemurragePreview). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ledger` must be a valid pointer. C strings must be valid.
 */
char *divi_fortune_demurrage_preview(const char *pubkey,
                                     const struct FortuneLedger *ledger,
                                     const char *policy_json);

/**
 * Get the default FortunePolicy as JSON.
 *
 * Returns JSON (FortunePolicy). Caller must free via `divi_free_string`.
 */
char *divi_fortune_policy_default(void);

/**
 * Create a trade proposal.
 *
 * Returns JSON (TradeProposal). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_fortune_trade_new(const char *proposer,
                             const char *recipient,
                             int64_t offering_cool,
                             int64_t requesting_cool);

/**
 * Accept a trade proposal.
 *
 * Takes trade JSON, returns modified trade JSON.
 *
 * # Safety
 * `trade_json` must be a valid C string.
 */
char *divi_fortune_trade_accept(const char *trade_json);

/**
 * Execute a trade proposal.
 *
 * Takes trade JSON, returns modified trade JSON.
 *
 * # Safety
 * `trade_json` must be a valid C string.
 */
char *divi_fortune_trade_execute(const char *trade_json);

/**
 * Generate a random cash serial number.
 *
 * Returns a C string. Caller must free via `divi_free_string`.
 */
char *divi_fortune_cash_generate_serial(void);

/**
 * Validate a cash serial number format.
 *
 * Returns true if valid.
 *
 * # Safety
 * `serial` must be a valid C string.
 */
bool divi_fortune_cash_validate_serial(const char *serial);

/**
 * Build and sign a text note event (kind 1).
 *
 * Returns JSON OmniEvent, or null on error. Free via `divi_free_string`.
 *
 * # Safety
 * `content` must be a valid C string. `keyring` must be a valid pointer.
 */
char *divi_globe_event_text_note(const char *content, const struct CrownKeyring *keyring);

/**
 * Sign an unsigned event (JSON) with a keyring.
 *
 * Input JSON: `{"kind": 1, "tags": [...], "content": "..."}`
 * Returns JSON OmniEvent, or null on error. Free via `divi_free_string`.
 *
 * # Safety
 * `unsigned_json` must be a valid C string. `keyring` must be a valid pointer.
 */
char *divi_globe_event_sign(const char *unsigned_json, const struct CrownKeyring *keyring);

/**
 * Verify an event's ID and signature.
 *
 * Returns true if valid, false otherwise.
 *
 * # Safety
 * `event_json` must be a valid C string.
 */
bool divi_globe_event_verify(const char *event_json);

/**
 * Build an OmniFilter for a user's profile (kind 0).
 *
 * `pubkey_hex` is the 64-char hex public key.
 * Returns JSON filter. Free via `divi_free_string`.
 *
 * # Safety
 * `pubkey_hex` must be a valid C string.
 */
char *divi_globe_filter_for_profile(const char *pubkey_hex);

/**
 * Build an OmniFilter for a user's contact list (kind 3).
 *
 * Returns JSON filter. Free via `divi_free_string`.
 *
 * # Safety
 * `pubkey_hex` must be a valid C string.
 */
char *divi_globe_filter_for_contact_list(const char *pubkey_hex);

/**
 * Build an OmniFilter from JSON.
 *
 * Returns JSON filter (round-trip for validation). Free via `divi_free_string`.
 *
 * # Safety
 * `json` must be a valid C string.
 */
char *divi_globe_filter_from_json(const char *json);

/**
 * Create a new relay pool.
 *
 * `runtime` must be a valid DiviRuntime pointer.
 * `config_json` is optional — pass null for defaults.
 * Returns a pool pointer. Free with `divi_globe_pool_free`.
 *
 * # Safety
 * `runtime` must be a valid pointer from `divi_runtime_new`.
 */
struct GlobePool *divi_globe_pool_new(const struct DiviRuntime *runtime, const char *config_json);

/**
 * Free a relay pool.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_globe_pool_new`, called exactly once.
 */
void divi_globe_pool_free(struct GlobePool *ptr);

/**
 * Add a relay to the pool and connect.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pool` must be a valid pointer. `url` must be a valid C string (e.g., "ws://localhost:8080").
 */
int32_t divi_globe_pool_add_relay(const struct GlobePool *pool, const char *url);

/**
 * Publish an event to all connected relays.
 *
 * `event_json` is a JSON OmniEvent.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pool` must be a valid pointer. `event_json` must be a valid C string.
 */
int32_t divi_globe_pool_publish(const struct GlobePool *pool, const char *event_json);

/**
 * Subscribe to events matching the given filters.
 *
 * `filters_json` is a JSON array of OmniFilter objects.
 * Returns the subscription ID as a string, or null on error.
 * Free the returned string via `divi_free_string`.
 *
 * # Safety
 * `pool` must be a valid pointer. `filters_json` must be a valid C string.
 */
char *divi_globe_pool_subscribe(const struct GlobePool *pool, const char *filters_json);

/**
 * Register a callback for incoming pool events.
 *
 * The callback fires on a background thread for each deduplicated event.
 * Only one callback can be active — calling again replaces the previous one.
 *
 * Events arrive as JSON strings. The callback receives the event JSON,
 * the source relay URL, and the context pointer.
 *
 * # Safety
 * `pool` must be a valid pointer. `context` must be valid for the lifetime
 * of the callback, or null.
 */
void divi_globe_pool_on_event(const struct GlobePool *pool,
                              GlobeEventCallback callback,
                              void *context);

/**
 * Create a BucketPaddingConfig from a mode string.
 *
 * `mode` is one of: `"disabled"`, `"minimal"`, `"standard"`, `"aggressive"`.
 * Returns JSON BucketPaddingConfig. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `mode` must be a valid C string.
 */
char *divi_globe_padding_config_new(const char *mode);

/**
 * Pad data to a bucket size boundary.
 *
 * Returns padded bytes. Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * `data` must be valid for `data_len` bytes. `out_len` must be a valid pointer.
 */
uint8_t *divi_globe_pad_to_bucket(const uint8_t *data,
                                  uintptr_t data_len,
                                  uintptr_t bucket_size,
                                  uintptr_t *out_len);

/**
 * Remove bucket padding and recover original data.
 *
 * Returns unpadded bytes. Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * `data` must be valid for `data_len` bytes. `out_len` must be a valid pointer.
 */
uint8_t *divi_globe_unpad(const uint8_t *data, uintptr_t data_len, uintptr_t *out_len);

/**
 * Create a ShapingConfig with the given parameters.
 *
 * Returns JSON ShapingConfig. Caller must free via `divi_free_string`.
 */
char *divi_globe_shaping_config_new(uint32_t bucket_secs,
                                    uint64_t jitter_min_ms,
                                    uint64_t jitter_max_ms);

/**
 * Build a forwarding envelope for multi-hop relay delivery.
 *
 * `payload` is the (typically onion-encrypted) message body.
 * `path_json` is a JSON array of relay URL strings.
 * Returns JSON ForwardEnvelope. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `payload` must be valid for `payload_len` bytes. `path_json` must be a valid C string.
 */
char *divi_globe_forward_envelope_create(const uint8_t *payload,
                                         uintptr_t payload_len,
                                         const char *path_json);

/**
 * Process a forwarding envelope at the current relay.
 *
 * `envelope_json` is a JSON ForwardEnvelope.
 * Returns JSON with either `{"action":"forward","next":"url","envelope":{...}}`
 * or `{"action":"deliver","payload":[...]}`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `envelope_json` must be a valid C string.
 */
char *divi_globe_forward_process(const char *envelope_json);

/**
 * Create an ephemeral session for anonymous subscriptions.
 *
 * Returns JSON EphemeralSession. Caller must free via `divi_free_string`.
 */
char *divi_globe_ephemeral_session_create(void);

/**
 * Create an anonymous auth response for a relay challenge.
 *
 * `challenge` is the relay's challenge string.
 * `relay_url` is the relay URL.
 * `session_json` is a JSON EphemeralSession.
 * Returns JSON AnonymousAuthResponse. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All parameters must be valid C strings.
 */
char *divi_globe_anonymous_auth(const char *challenge,
                                const char *relay_url,
                                const char *session_json);

/**
 * Strip the author field from an event JSON string.
 *
 * `event_json` is a JSON OmniEvent.
 * Returns JSON with the author field replaced by an empty string.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `event_json` must be a valid C string.
 */
char *divi_globe_strip_author(const char *event_json);

/**
 * Check whether a path is a .idea package (directory with Header.json).
 *
 * Returns `true` if it is, `false` otherwise (including on null path).
 *
 * # Safety
 * `path` must be a valid null-terminated C string, or null.
 */
bool divi_hall_is_idea_package(const char *path);

/**
 * Read just the header from an .idea package (no key needed).
 *
 * Returns a JSON-encoded `Header` string, or null on error.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `path` must be a valid null-terminated C string, or null.
 */
char *divi_hall_read_header(const char *path);

/**
 * Read a full .idea package with decryption and graceful degradation.
 *
 * Returns a JSON-encoded `IdeaPackage` string (with `path` field omitted),
 * or null on error. Warnings (non-fatal issues) are written to
 * `out_warnings_json` as a JSON array of `HallWarning` objects.
 * Both the returned string and the warnings string must be freed
 * via `divi_free_string`.
 *
 * # Safety
 * - `path` must be a valid null-terminated C string.
 * - `content_key` must point to `key_len` valid bytes.
 * - `out_warnings_json` must be a valid pointer to a `*mut c_char`.
 */
char *divi_hall_read(const char *path,
                     const uint8_t *content_key,
                     uintptr_t key_len,
                     char **out_warnings_json);

/**
 * Write an IdeaPackage to disk with encryption.
 *
 * `package_json` is a JSON-encoded `IdeaPackage`. The `path` parameter
 * overrides the package's path field (which is skipped during serde).
 *
 * Returns the number of bytes written on success, or -1 on error.
 *
 * # Safety
 * - `package_json` and `path` must be valid null-terminated C strings.
 * - `content_key` must point to `key_len` valid bytes.
 */
int64_t divi_hall_write(const char *package_json,
                        const char *path,
                        const uint8_t *content_key,
                        uintptr_t key_len);

/**
 * Import raw bytes as an encrypted asset. Returns the SHA-256 hex hash.
 *
 * Caller must free the returned string via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * - `data` must point to `data_len` valid bytes.
 * - `idea_path` must be a valid null-terminated C string.
 * - `content_key` must point to `key_len` valid bytes.
 * - `vocab_seed` must point to `seed_len` valid bytes.
 */
char *divi_hall_asset_import(const uint8_t *data,
                             uintptr_t data_len,
                             const char *idea_path,
                             const uint8_t *content_key,
                             uintptr_t key_len,
                             const uint8_t *vocab_seed,
                             uintptr_t seed_len);

/**
 * Import a file as an encrypted asset. Returns the SHA-256 hex hash.
 *
 * Caller must free the returned string via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * - `source_path` and `idea_path` must be valid null-terminated C strings.
 * - `content_key` must point to `key_len` valid bytes.
 * - `vocab_seed` must point to `seed_len` valid bytes.
 */
char *divi_hall_asset_import_file(const char *source_path,
                                  const char *idea_path,
                                  const uint8_t *content_key,
                                  uintptr_t key_len,
                                  const uint8_t *vocab_seed,
                                  uintptr_t seed_len);

/**
 * Read an asset by its hash. Decrypts, deobfuscates, and verifies integrity.
 *
 * Output bytes are written to `out_data`/`out_len`. Caller must free
 * via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * - `hash` and `idea_path` must be valid null-terminated C strings.
 * - `content_key` must point to `key_len` valid bytes.
 * - `vocab_seed` must point to `seed_len` valid bytes.
 * - `out_data` and `out_len` must be valid pointers.
 */
int32_t divi_hall_asset_read(const char *hash,
                             const char *idea_path,
                             const uint8_t *content_key,
                             uintptr_t key_len,
                             const uint8_t *vocab_seed,
                             uintptr_t seed_len,
                             uint8_t **out_data,
                             uintptr_t *out_len);

/**
 * Export an asset to a destination file (decrypted).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * - `hash`, `idea_path`, and `dest_path` must be valid null-terminated C strings.
 * - `content_key` must point to `key_len` valid bytes.
 * - `vocab_seed` must point to `seed_len` valid bytes.
 */
int32_t divi_hall_asset_export(const char *hash,
                               const char *idea_path,
                               const char *dest_path,
                               const uint8_t *content_key,
                               uintptr_t key_len,
                               const uint8_t *vocab_seed,
                               uintptr_t seed_len);

/**
 * List all asset hashes in the Assets/ directory.
 *
 * Returns a JSON array of hex hash strings. Caller must free via
 * `divi_free_string`. Returns null on error.
 *
 * # Safety
 * `idea_path` must be a valid null-terminated C string, or null.
 */
char *divi_hall_asset_list(const char *idea_path);

/**
 * Check if an asset exists by its hash.
 *
 * Returns `true` if the asset file exists, `false` otherwise.
 *
 * # Safety
 * `hash` and `idea_path` must be valid null-terminated C strings, or null.
 */
bool divi_hall_asset_exists(const char *hash, const char *idea_path);

/**
 * Delete an asset by its hash.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `hash` and `idea_path` must be valid null-terminated C strings.
 */
int32_t divi_hall_asset_delete(const char *hash, const char *idea_path);

/**
 * Extract image metadata from raw bytes.
 *
 * Detects image format, reads dimensions, and generates a blurhash
 * placeholder. Returns a JSON-encoded `ImageMetadata` string with
 * `width`, `height`, `mime`, `size`, and `blurhash` fields.
 *
 * Caller must free the returned string via `divi_free_string`.
 * Returns null on error (e.g. unrecognized image format).
 *
 * # Safety
 * - `data` must point to `data_len` valid bytes, or be null.
 */
char *divi_hall_extract_image_metadata(const uint8_t *data, uintptr_t data_len);

/**
 * Get the last error message, or null if no error.
 *
 * The returned pointer is valid until the next FFI call on this thread.
 * Do NOT free it — it is owned by the thread-local storage.
 */
const char *divi_last_error(void);

/**
 * Free a Rust-allocated C string.
 *
 * # Safety
 * `ptr` must have been returned by a `divi_*` function that allocates strings,
 * or be null (no-op).
 */
void divi_free_string(char *ptr);

/**
 * Free a Rust-allocated byte buffer.
 *
 * # Safety
 * `ptr` must have been returned by a `divi_*` function that allocates bytes,
 * with the matching `len`. Or null (no-op).
 */
void divi_free_bytes(uint8_t *ptr, uintptr_t len);

/**
 * Create a new IdeaPackage in memory.
 *
 * `path` is the filesystem path for the .idea directory.
 * `header_json` is a JSON Header. `root_digit_json` is a JSON Digit.
 * Returns an opaque pointer, or null on error. Free with `divi_ideas_package_free`.
 *
 * # Safety
 * All C strings must be valid.
 */
struct IdeasPackage *divi_ideas_package_new(const char *path,
                                            const char *header_json,
                                            const char *root_digit_json);

/**
 * Load an IdeaPackage from a .idea directory on disk.
 *
 * Returns an opaque pointer, or null on error. Free with `divi_ideas_package_free`.
 *
 * # Safety
 * `path` must be a valid C string.
 */
struct IdeasPackage *divi_ideas_package_load(const char *path);

/**
 * Free an IdeaPackage.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_ideas_package_*` constructor, called exactly once.
 */
void divi_ideas_package_free(struct IdeasPackage *ptr);

/**
 * Save the package to disk.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer.
 */
int32_t divi_ideas_package_save(const struct IdeasPackage *pkg);

/**
 * Get the package header as JSON.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `pkg` must be a valid pointer.
 */
char *divi_ideas_package_header(const struct IdeasPackage *pkg);

/**
 * Get the root digit as JSON, or null if not found.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `pkg` must be a valid pointer.
 */
char *divi_ideas_package_root_digit(const struct IdeasPackage *pkg);

/**
 * Get a digit by ID (UUID string) as JSON, or null if not found.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `pkg` and `id` must be valid pointers.
 */
char *divi_ideas_package_digit(const struct IdeasPackage *pkg, const char *id);

/**
 * Get the number of digits in the package.
 *
 * # Safety
 * `pkg` must be a valid pointer.
 */
uint32_t divi_ideas_package_digit_count(const struct IdeasPackage *pkg);

/**
 * Get all digits as a JSON array.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `pkg` must be a valid pointer.
 */
char *divi_ideas_package_all_digits(const struct IdeasPackage *pkg);

/**
 * Add a digit to the package from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer. `digit_json` must be a valid C string.
 */
int32_t divi_ideas_package_add_digit(const struct IdeasPackage *pkg, const char *digit_json);

/**
 * Set the package's book (authority) from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_ideas_package_set_book(const struct IdeasPackage *pkg, const char *json);

/**
 * Set the package's tree (provenance) from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_ideas_package_set_tree(const struct IdeasPackage *pkg, const char *json);

/**
 * Set the package's cool (currency value) from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_ideas_package_set_cool(const struct IdeasPackage *pkg, const char *json);

/**
 * Set the package's redemption from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_ideas_package_set_redemption(const struct IdeasPackage *pkg, const char *json);

/**
 * Set the package's bonds from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_ideas_package_set_bonds(const struct IdeasPackage *pkg, const char *json);

/**
 * Set the package's position from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `pkg` must be a valid pointer. `json` must be a valid C string.
 */
int32_t divi_ideas_package_set_position(const struct IdeasPackage *pkg, const char *json);

/**
 * Read only the header from a .idea directory (static, no package needed).
 *
 * Returns JSON Header, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `path` must be a valid C string.
 */
char *divi_ideas_package_read_header(const char *path);

/**
 * Create a new digit.
 *
 * `digit_type` is the type string (e.g. "text", "media.image").
 * `content_json` is a JSON Value for the content.
 * `author` is the creator's crown_id.
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_new(const char *digit_type, const char *content_json, const char *author);

/**
 * Return a new digit with updated content (copy-on-write).
 *
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_with_content(const char *digit_json,
                                    const char *content_json,
                                    const char *by);

/**
 * Return a new digit with a property set (copy-on-write).
 *
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_with_property(const char *digit_json,
                                     const char *key,
                                     const char *value_json,
                                     const char *by);

/**
 * Return a new digit with a child added (copy-on-write).
 *
 * `child_id` is a UUID string.
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_with_child(const char *digit_json, const char *child_id, const char *by);

/**
 * Return a tombstoned copy of this digit.
 *
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_deleted(const char *digit_json, const char *by);

/**
 * Return a restored (un-tombstoned) copy of this digit.
 *
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_restored(const char *digit_json, const char *by);

/**
 * Get the digit's UUID as a string.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`, or null on error.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
char *divi_ideas_digit_id(const char *digit_json);

/**
 * Get the digit's type string.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`, or null on error.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
char *divi_ideas_digit_type(const char *digit_json);

/**
 * Get the digit's author string.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`, or null on error.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
char *divi_ideas_digit_author(const char *digit_json);

/**
 * Check if the digit is tombstoned (deleted).
 *
 * Returns false on parse error.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
bool divi_ideas_digit_is_deleted(const char *digit_json);

/**
 * Check if the digit has children.
 *
 * Returns false on parse error.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
bool divi_ideas_digit_has_children(const char *digit_json);

/**
 * Extract all text content from a digit for search indexing.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`, or null on error.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
char *divi_ideas_digit_extract_text(const char *digit_json);

/**
 * Validate a digit (checks type and property keys).
 *
 * Returns 0 on success (valid), -1 on validation error.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
int32_t divi_ideas_digit_validate(const char *digit_json);

/**
 * Return a new digit with TextSpan data added/replaced on the "spans" property.
 *
 * `spans_json` is a JSON array of `TextSpan` objects (see `textspan.rs`).
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_with_spans(const char *digit_json,
                                  const char *spans_json,
                                  const char *author);

/**
 * Extract TextSpan data from a digit's "spans" property.
 *
 * Returns JSON array of `TextSpan` objects, or null if no spans are present.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
char *divi_ideas_digit_spans(const char *digit_json);

/**
 * Return a new digit with a child removed by UUID string.
 *
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_without_child(const char *digit_json, const char *child_id_str);

/**
 * Return a new digit with a child inserted at a specific index.
 *
 * `index` is the 0-based insertion position. If >= current child count, appends.
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_with_child_at(const char *digit_json,
                                     uintptr_t index,
                                     const char *child_id_str,
                                     const char *author);

/**
 * Return a new digit with children reordered.
 *
 * `children_json` is a JSON array of UUID strings representing the new order.
 * Returns JSON Digit, or null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_reorder_children(const char *digit_json,
                                        const char *children_json,
                                        const char *author);

/**
 * Create a new header.
 *
 * `pubkey` and `signature` are creator identity strings.
 * `root_id` is a UUID string. `key_slot_json` is a JSON KeySlot.
 * Returns JSON Header. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_header_create(const char *pubkey,
                               const char *signature,
                               const char *root_id,
                               const char *key_slot_json);

/**
 * Validate a header.
 *
 * Returns 0 on success, -1 on validation error.
 *
 * # Safety
 * `header_json` must be a valid C string.
 */
int32_t divi_ideas_header_validate(const char *header_json);

/**
 * Return a header with updated modified timestamp.
 *
 * Returns JSON Header. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `header_json` must be a valid C string.
 */
char *divi_ideas_header_touched(const char *header_json);

/**
 * Check if Babel obfuscation is enabled.
 *
 * Returns false on parse error.
 *
 * # Safety
 * `header_json` must be a valid C string.
 */
bool divi_ideas_header_is_babel_enabled(const char *header_json);

/**
 * Check if the header has a password key slot.
 *
 * Returns false on parse error.
 *
 * # Safety
 * `header_json` must be a valid C string.
 */
bool divi_ideas_header_has_password_slot(const char *header_json);

/**
 * Get the list of public key recipients who can unlock this idea.
 *
 * Returns a JSON array of strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `header_json` must be a valid C string.
 */
char *divi_ideas_header_shared_with(const char *header_json);

/**
 * Get the file extension for this idea.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`, or null on error.
 *
 * # Safety
 * `header_json` must be a valid C string.
 */
char *divi_ideas_header_file_extension(const char *header_json);

/**
 * Validate a digit type string.
 *
 * Returns 0 if valid, -1 if invalid.
 *
 * # Safety
 * `type_str` must be a valid C string.
 */
int32_t divi_ideas_validate_digit_type(const char *type_str);

/**
 * Validate a property key string.
 *
 * Returns 0 if valid, -1 if invalid.
 *
 * # Safety
 * `key` must be a valid C string.
 */
int32_t divi_ideas_validate_property_key(const char *key);

/**
 * Validate a local bond path.
 *
 * Returns 0 if valid, -1 if invalid.
 *
 * # Safety
 * `path` must be a valid C string.
 */
int32_t divi_ideas_validate_local_bond_path(const char *path);

/**
 * Create a new empty SchemaRegistry.
 *
 * Free with `divi_ideas_schema_registry_free`.
 */
struct IdeasSchemaRegistry *divi_ideas_schema_registry_new(void);

/**
 * Free a SchemaRegistry.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_ideas_schema_registry_new`, called exactly once.
 */
void divi_ideas_schema_registry_free(struct IdeasSchemaRegistry *ptr);

/**
 * Register a schema from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `schema_json` must be a valid C string.
 */
int32_t divi_ideas_schema_registry_register(const struct IdeasSchemaRegistry *registry,
                                            const char *schema_json);

/**
 * Look up a schema by versioned type string.
 *
 * Returns JSON DigitSchema, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `versioned_type` must be a valid C string.
 */
char *divi_ideas_schema_registry_get(const struct IdeasSchemaRegistry *registry,
                                     const char *versioned_type);

/**
 * Look up the latest version of a schema for a given digit type.
 *
 * Returns JSON DigitSchema, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `digit_type` must be a valid C string.
 */
char *divi_ideas_schema_registry_latest(const struct IdeasSchemaRegistry *registry,
                                        const char *digit_type);

/**
 * Get all registered schemas as a JSON array.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_ideas_schema_registry_all(const struct IdeasSchemaRegistry *registry);

/**
 * Get all versions of a given digit type as a JSON array.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `digit_type` must be a valid C string.
 */
char *divi_ideas_schema_registry_versions_of(const struct IdeasSchemaRegistry *registry,
                                             const char *digit_type);

/**
 * Get the number of schemas in the registry.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
uint32_t divi_ideas_schema_registry_len(const struct IdeasSchemaRegistry *registry);

/**
 * Whether the registry is empty.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
bool divi_ideas_schema_registry_is_empty(const struct IdeasSchemaRegistry *registry);

/**
 * Resolve a schema by flattening its extends chain using the registry.
 *
 * Returns JSON DigitSchema (resolved). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `schema_json` must be a valid C string.
 */
char *divi_ideas_schema_registry_resolve(const struct IdeasSchemaRegistry *registry,
                                         const char *schema_json);

/**
 * Create a new schema at version 1 for the given digit type.
 *
 * Returns JSON DigitSchema. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `digit_type` must be a valid C string.
 */
char *divi_ideas_schema_new(const char *digit_type);

/**
 * Validate the schema itself (check defaults match types, property key rules).
 *
 * Returns a JSON array of validation errors, or "[]" if valid.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `schema_json` must be a valid C string.
 */
char *divi_ideas_schema_validate_self(const char *schema_json);

/**
 * Validate a digit against a schema.
 *
 * Returns null if valid, or a JSON array of validation errors if invalid.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_schema_validate_digit(const char *digit_json, const char *schema_json);

/**
 * Validate a digit against a schema with extends chain resolution.
 *
 * Returns null if valid, or a JSON array of validation errors if invalid.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All pointers must be valid. All C strings must be valid.
 */
char *divi_ideas_schema_validate_composed(const char *digit_json,
                                          const char *schema_json,
                                          const struct IdeasSchemaRegistry *registry);

/**
 * Attach accessibility metadata to a digit.
 *
 * Returns JSON Digit with a11y_ properties set. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_with_accessibility(const char *digit_json,
                                          const char *meta_json,
                                          const char *author);

/**
 * Extract accessibility metadata from a digit.
 *
 * Returns JSON AccessibilityMetadata, or null if no a11y metadata present.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
char *divi_ideas_digit_accessibility(const char *digit_json);

/**
 * Create empty bonds as JSON.
 *
 * Returns JSON Bonds. Caller must free via `divi_free_string`.
 */
char *divi_ideas_bonds_new(void);

/**
 * Count total references across all bond types.
 *
 * Returns 0 on parse error.
 *
 * # Safety
 * `json` must be a valid C string.
 */
uint32_t divi_ideas_bonds_count(const char *json);

/**
 * Check if bonds are empty.
 *
 * Returns true on parse error.
 *
 * # Safety
 * `json` must be a valid C string.
 */
bool divi_ideas_bonds_is_empty(const char *json);

/**
 * Validate bonds (check local bond paths).
 *
 * Returns 0 on success, -1 on validation error.
 *
 * # Safety
 * `json` must be a valid C string.
 */
int32_t divi_ideas_bonds_validate(const char *json);

/**
 * Attach a data binding to a digit.
 *
 * Returns JSON Digit with binding_ properties set. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_digit_with_binding(const char *digit_json,
                                    const char *binding_json,
                                    const char *author);

/**
 * Extract data binding from a digit.
 *
 * Returns JSON DataBinding, or null if no binding present.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `digit_json` must be a valid C string.
 */
char *divi_ideas_digit_parse_binding(const char *digit_json);

/**
 * Create an Insert CRDT operation.
 *
 * `digit_json` is a raw JSON value for the digit content.
 * `parent_id` is a UUID string, or null.
 * `vector_json` is a JSON VectorClock.
 * Returns JSON DigitOperation. Caller must free via `divi_free_string`.
 *
 * # Safety
 * C strings must be valid. `parent_id` may be null.
 */
char *divi_ideas_operation_insert(const char *digit_json,
                                  const char *parent_id,
                                  const char *author,
                                  const char *vector_json);

/**
 * Create an Update CRDT operation.
 *
 * `digit_id` is the target digit UUID.
 * `field` is the property name being updated.
 * `old_json` and `new_json` are JSON Value representations.
 * Returns JSON DigitOperation. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_operation_update(const char *digit_id,
                                  const char *field,
                                  const char *old_json,
                                  const char *new_json,
                                  const char *author,
                                  const char *vector_json);

/**
 * Create a Delete CRDT operation.
 *
 * `digit_id` is the target digit UUID. `tombstone` indicates soft delete.
 * Returns JSON DigitOperation. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_ideas_operation_delete(const char *digit_id,
                                  bool tombstone,
                                  const char *author,
                                  const char *vector_json);

/**
 * Create a new empty trust graph.
 * Free with `divi_jail_trust_graph_free`.
 */
struct JailTrustGraph *divi_jail_trust_graph_new(void);

/**
 * Free a trust graph.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_jail_trust_graph_new`, called exactly once.
 */
void divi_jail_trust_graph_free(struct JailTrustGraph *ptr);

/**
 * Add a verification edge to the trust graph.
 *
 * `edge_json` is a JSON VerificationEdge.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `graph` must be a valid pointer. `edge_json` must be a valid C string.
 */
int32_t divi_jail_trust_graph_add_edge(const struct JailTrustGraph *graph, const char *edge_json);

/**
 * Remove a verification edge by ID (UUID string).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `graph` must be a valid pointer. `edge_id` must be a valid C string.
 */
int32_t divi_jail_trust_graph_remove_edge(const struct JailTrustGraph *graph, const char *edge_id);

/**
 * Query network intelligence about a target person.
 *
 * `flags_json` is a JSON array of AccountabilityFlags.
 * `config_json` is a JSON JailConfig (or null for defaults).
 * Returns JSON (NetworkIntelligence). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `graph` must be a valid pointer. All C strings must be valid.
 */
char *divi_jail_trust_graph_query_intelligence(const struct JailTrustGraph *graph,
                                               const char *querier,
                                               const char *target,
                                               const char *flags_json,
                                               const char *config_json);

/**
 * Get the node count in the trust graph.
 *
 * # Safety
 * `graph` must be a valid pointer.
 */
uint32_t divi_jail_trust_graph_node_count(const struct JailTrustGraph *graph);

/**
 * Get the edge count in the trust graph.
 *
 * # Safety
 * `graph` must be a valid pointer.
 */
uint32_t divi_jail_trust_graph_edge_count(const struct JailTrustGraph *graph);

/**
 * Raise an accountability flag.
 *
 * `category_json` is a JSON FlagCategory. `severity_json` is a JSON FlagSeverity.
 * Returns JSON (AccountabilityFlag). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_jail_flag_raise(const char *flagger,
                           const char *flagged,
                           const char *category_json,
                           const char *severity_json,
                           const char *description);

/**
 * Check admission for a prospect to a community.
 *
 * `members_json` is a JSON array of pubkey strings.
 * `flags_json` is a JSON array of AccountabilityFlags.
 * Returns JSON (AdmissionRecommendation). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `graph` must be a valid pointer. All C strings must be valid.
 */
char *divi_jail_check_admission(const struct JailTrustGraph *graph,
                                const char *prospect,
                                const char *community_id,
                                const char *members_json,
                                const char *flags_json,
                                const char *config_json);

/**
 * Begin a graduated response against a target.
 *
 * Returns JSON (GraduatedResponse). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_jail_response_begin(const char *target_pubkey,
                               const char *reason,
                               const char *initiated_by);

/**
 * Escalate a graduated response.
 *
 * Takes response JSON, returns modified response JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_jail_response_escalate(const char *response_json,
                                  const char *reason,
                                  const char *initiated_by);

/**
 * De-escalate a graduated response.
 *
 * Takes response JSON, returns modified response JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_jail_response_de_escalate(const char *response_json,
                                     const char *reason,
                                     const char *initiated_by);

/**
 * Get the default JailConfig as JSON.
 *
 * Returns JSON (JailConfig). Caller must free via `divi_free_string`.
 */
char *divi_jail_config_default(void);

/**
 * Create a new community.
 *
 * `basis_json` is a JSON string for CommunityBasis (e.g. `"Interest"`).
 * Returns JSON (Community). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `name` and `basis_json` must be valid C strings.
 */
char *divi_kingdom_community_new(const char *name, const char *basis_json);

/**
 * Add a member to a community.
 *
 * Takes community JSON, returns modified community JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. `sponsor` may be null.
 */
char *divi_kingdom_community_add_member(const char *community_json,
                                        const char *pubkey,
                                        const char *sponsor);

/**
 * Remove a member from a community.
 *
 * Takes community JSON, returns modified community JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_community_remove_member(const char *community_json, const char *pubkey);

/**
 * Update a member's role in a community.
 *
 * `role_json` is a JSON CommunityRole (e.g. `"Steward"`).
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_community_update_role(const char *community_json,
                                         const char *pubkey,
                                         const char *role_json);

/**
 * Activate a forming community.
 *
 * # Safety
 * `community_json` must be a valid C string.
 */
char *divi_kingdom_community_activate(const char *community_json);

/**
 * Create a new proposal.
 *
 * `deciding_body_json` is a JSON DecidingBody (e.g. `{"Community":"uuid..."}`).
 * Returns JSON (Proposal). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_proposal_new(const char *author,
                                const char *deciding_body_json,
                                const char *title,
                                const char *body);

/**
 * Add a vote to a proposal.
 *
 * `vote_json` is a JSON Vote. Returns modified proposal JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_proposal_add_vote(const char *proposal_json, const char *vote_json);

/**
 * Open voting on a proposal. `closes_at` is a Unix timestamp (seconds).
 *
 * # Safety
 * `proposal_json` must be a valid C string.
 */
char *divi_kingdom_proposal_open_voting(const char *proposal_json, int64_t closes_at);

/**
 * Compute the vote tally for a proposal.
 *
 * Returns JSON (VoteTally). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `proposal_json` must be a valid C string.
 */
char *divi_kingdom_proposal_tally(const char *proposal_json, uint32_t eligible_voters);

/**
 * Create a vote.
 *
 * `position_json` is a JSON VotePosition (e.g. `"Support"`).
 * Returns JSON (Vote). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. `proposal_id` must be a UUID string.
 */
char *divi_kingdom_vote_new(const char *voter, const char *proposal_id, const char *position_json);

/**
 * Create a membership application.
 *
 * Returns JSON (MembershipApplication). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. `community_id` must be a UUID string.
 */
char *divi_kingdom_application_new(const char *community_id,
                                   const char *applicant_pubkey,
                                   const char *statement);

/**
 * Approve a membership application.
 *
 * Takes application JSON, returns modified application JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_application_approve(const char *app_json, const char *reviewer);

/**
 * Reject a membership application.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_application_reject(const char *app_json,
                                      const char *reviewer,
                                      const char *reason);

/**
 * Add a founding member to a community.
 *
 * Takes community JSON and a pubkey. Returns modified community JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_community_add_founder(const char *community_json, const char *pubkey);

/**
 * Transition an active community to dormant.
 *
 * Takes community JSON. Returns modified community JSON.
 *
 * # Safety
 * `community_json` must be a valid C string.
 */
char *divi_kingdom_community_go_dormant(const char *community_json);

/**
 * Dissolution state machine for a community.
 *
 * `stage`: 0 = begin dissolution, 1 = complete dissolution, 2 = reactivate from dormant.
 * Returns modified community JSON.
 *
 * # Safety
 * `community_json` must be a valid C string.
 */
char *divi_kingdom_community_dissolve(const char *community_json, uint8_t stage);

/**
 * Create a new charter.
 *
 * Returns JSON (Charter). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. `community_id` must be a UUID string.
 */
char *divi_kingdom_charter_new(const char *community_id, const char *name, const char *purpose);

/**
 * Sign a charter with a pubkey and signature.
 *
 * Takes charter JSON, returns modified charter JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_charter_sign(const char *charter_json,
                                const char *pubkey,
                                const char *signature);

/**
 * Create an amended version of a charter.
 *
 * Takes charter JSON and a community_id UUID. Returns new charter JSON (version incremented).
 *
 * # Safety
 * All C strings must be valid. `community_id` must be a UUID string.
 */
char *divi_kingdom_charter_amend(const char *charter_json, const char *community_id);

/**
 * Open discussion on a proposal (Draft -> Discussion).
 *
 * Takes proposal JSON. Returns modified proposal JSON.
 *
 * # Safety
 * `proposal_json` must be a valid C string.
 */
char *divi_kingdom_proposal_open_discussion(const char *proposal_json);

/**
 * Resolve a proposal with an outcome.
 *
 * Takes proposal JSON and outcome JSON. Returns modified proposal JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_proposal_resolve(const char *proposal_json, const char *outcome_json);

/**
 * Withdraw a proposal.
 *
 * Takes proposal JSON. Returns modified proposal JSON.
 *
 * # Safety
 * `proposal_json` must be a valid C string.
 */
char *divi_kingdom_proposal_withdraw(const char *proposal_json);

/**
 * Create a new discussion post.
 *
 * `reply_to` may be null (top-level post) or a UUID string (reply).
 * Returns JSON (DiscussionPost). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `author` and `content` must be valid C strings. `reply_to` may be null.
 */
char *divi_kingdom_discussion_post_new(const char *author,
                                       const char *content,
                                       const char *reply_to);

/**
 * Create a new mandate.
 *
 * `preset`: 0 = empty (custom), 1 = standard, 2 = limited.
 * `delegate_pubkey`, `community_id`, `consortium_id` are required strings.
 * Returns JSON (Mandate). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. UUID strings must be valid UUIDs.
 */
char *divi_kingdom_mandate_new(const char *delegate_pubkey,
                               const char *community_id,
                               const char *consortium_id,
                               uint8_t preset);

/**
 * Check whether a mandate authorizes a given decision type.
 *
 * `proposal_type_json` is a JSON ProposalType (e.g. `"Standard"`).
 * Returns: 0 = Authorized, 1 = RequiresConsultation, 2 = Prohibited, -1 = error.
 *
 * # Safety
 * All C strings must be valid.
 */
int32_t divi_kingdom_mandate_can_decide(const char *mandate_json, const char *proposal_type_json);

/**
 * Create a new delegate.
 *
 * `appointment_json` is a JSON AppointmentSource.
 * `mandate_json` is a JSON Mandate.
 * Returns JSON (Delegate). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. UUID strings must be valid UUIDs.
 */
char *divi_kingdom_delegate_new(const char *pubkey,
                                const char *community_id,
                                const char *consortium_id,
                                const char *mandate_json,
                                const char *appointment_json);

/**
 * Create a recall motion against a delegate.
 *
 * `delegate_id` and `community_id` are UUID strings.
 * `signatures_required` is the number of signatures needed to trigger the recall.
 * Returns JSON (DelegateRecall). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. UUID strings must be valid UUIDs.
 */
char *divi_kingdom_recall_new(const char *delegate_id,
                              const char *delegate_pubkey,
                              const char *community_id,
                              const char *reason,
                              uint32_t signatures_required);

/**
 * Add a signature to a recall motion.
 *
 * Takes recall JSON, returns modified recall JSON.
 * If the signature threshold is met, the recall auto-transitions to Triggered.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_recall_add_signature(const char *recall_json,
                                        const char *pubkey,
                                        const char *signature);

/**
 * Create a new consortium.
 *
 * `charter_json` is a JSON ConsortiumCharter.
 * Returns JSON (Consortium). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_consortium_new(const char *name, const char *purpose, const char *charter_json);

/**
 * Add a member community to a consortium.
 *
 * `member_json` is a JSON ConsortiumMember.
 * Returns modified consortium JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_consortium_add_member(const char *consortium_json, const char *member_json);

/**
 * Remove a member community from a consortium by community_id.
 *
 * Returns modified consortium JSON.
 *
 * # Safety
 * All C strings must be valid. `community_id` must be a UUID string.
 */
char *divi_kingdom_consortium_remove_member(const char *consortium_json, const char *community_id);

/**
 * Create a new assembly (convocation).
 *
 * `type_json` is a JSON AssemblyType (e.g. `"CommunityAssembly"`).
 * `trigger_json` is a JSON ConvocationTrigger (e.g. `"Scheduled"`).
 * Returns JSON (Assembly). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_assembly_new(const char *name,
                                const char *type_json,
                                const char *convened_by,
                                const char *purpose,
                                const char *trigger_json);

/**
 * Transition an assembly's lifecycle.
 *
 * `action`: 0 = begin (Convened -> InProgress), 1 = pause, 2 = resume, 3 = conclude.
 * Returns modified assembly JSON.
 *
 * # Safety
 * `assembly_json` must be a valid C string.
 */
char *divi_kingdom_assembly_transition(const char *assembly_json, uint8_t action);

/**
 * Add a record to an assembly.
 *
 * `record_json` is a JSON AssemblyRecord.
 * Returns modified assembly JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_assembly_add_record(const char *assembly_json, const char *record_json);

/**
 * Create a new challenge.
 *
 * `type_json` is a JSON ChallengeType (e.g. `"CoreViolation"`).
 * `target_json` is a JSON ChallengeTarget.
 * Returns JSON (Challenge). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_challenge_new(const char *challenger,
                                 const char *type_json,
                                 const char *target_json,
                                 const char *grounds);

/**
 * Respond to a challenge.
 *
 * `response_json` is a JSON ChallengeResponse.
 * Returns modified challenge JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_challenge_respond(const char *challenge_json, const char *response_json);

/**
 * Resolve a challenge by upholding or dismissing it.
 *
 * `upheld`: 0 = dismiss, 1 = uphold.
 * Returns modified challenge JSON.
 *
 * # Safety
 * `challenge_json` must be a valid C string.
 */
char *divi_kingdom_challenge_resolve(const char *challenge_json, uint8_t upheld);

/**
 * Create a new dispute.
 *
 * `type_json` is a JSON DisputeType (e.g. `"ContractBreach"`).
 * `context_json` is a JSON DisputeContext (e.g. `"Interpersonal"`).
 * Returns JSON (Dispute). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_dispute_new(const char *complainant,
                               const char *respondent,
                               const char *type_json,
                               const char *context_json,
                               const char *description);

/**
 * Transition a dispute through its lifecycle.
 *
 * `action`: 0 = require_response, 1 = advance_to_hearing, 2 = advance_to_resolution,
 *           3 = resolve, 4 = dismiss, 5 = withdraw.
 * Returns modified dispute JSON.
 *
 * # Safety
 * `dispute_json` must be a valid C string.
 */
char *divi_kingdom_dispute_transition(const char *dispute_json, uint8_t action);

/**
 * Create a new resolution for a dispute.
 *
 * `decided_by_json` is a JSON array of adjudicator UUIDs.
 * `decision_json` is a JSON DecisionOutcome (e.g. `"ForComplainant"`).
 * Returns JSON (Resolution). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. `dispute_id` must be a UUID string.
 */
char *divi_kingdom_resolution_new(const char *dispute_id,
                                  const char *decided_by_json,
                                  const char *decision_json,
                                  const char *reasoning);

/**
 * Create a new appeal of a resolution.
 *
 * `grounds_json` is a JSON array of AppealGround (e.g. `["ProceduralError","Bias"]`).
 * Returns JSON (Appeal). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. UUID strings must be valid UUIDs.
 */
char *divi_kingdom_appeal_new(const char *resolution_id,
                              const char *dispute_id,
                              const char *appellant,
                              const char *grounds_json,
                              const char *argument);

/**
 * Create a new union.
 *
 * `type_json` is a JSON UnionType (e.g. `"Marriage"`, `"WorkerCooperative"`).
 * Returns JSON (Union). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_union_new(const char *name, const char *type_json);

/**
 * Dissolve a union.
 *
 * `record_json` is a JSON DissolutionRecord.
 * Returns modified union JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_union_dissolve(const char *union_json, const char *record_json);

/**
 * Propose a new federation agreement between two communities.
 *
 * `scopes_json` is a JSON array of `FederationScope` values.
 * Returns JSON (FederationAgreement). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_federation_propose(const char *community_a,
                                      const char *community_b,
                                      const char *proposed_by,
                                      const char *scopes_json);

/**
 * Accept a proposed federation agreement (Proposed -> Active).
 *
 * Returns modified agreement JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_federation_accept(const char *agreement_json, const char *accepted_by);

/**
 * Suspend an active federation agreement (Active -> Suspended).
 *
 * Returns modified agreement JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_federation_suspend(const char *agreement_json, const char *reason);

/**
 * Reactivate a suspended federation agreement (Suspended -> Active).
 *
 * Returns modified agreement JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `agreement_json` must be a valid C string.
 */
char *divi_kingdom_federation_reactivate(const char *agreement_json);

/**
 * Withdraw from a federation agreement (Active|Suspended -> Withdrawn).
 *
 * Returns modified agreement JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_federation_withdraw(const char *agreement_json,
                                       const char *withdrawn_by,
                                       const char *reason);

/**
 * Get the status of a federation agreement.
 *
 * Returns JSON (FederationStatus). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `agreement_json` must be a valid C string.
 */
char *divi_kingdom_federation_status(const char *agreement_json);

/**
 * Check whether a federation agreement involves a specific community.
 *
 * Returns `true` if the community is party to the agreement.
 *
 * # Safety
 * All C strings must be valid.
 */
bool divi_kingdom_federation_involves(const char *agreement_json, const char *community_id);

/**
 * Get the partner community ID from a federation agreement.
 *
 * Returns the partner's community ID string, or null if `community_id`
 * is not a party. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_kingdom_federation_partner_of(const char *agreement_json, const char *community_id);

/**
 * Create a new empty federation registry.
 */
FederationRegistry *divi_kingdom_federation_registry_new(void);

/**
 * Free a federation registry.
 *
 * # Safety
 * `ptr` must have been returned by `divi_kingdom_federation_registry_new` or be null.
 */
void divi_kingdom_federation_registry_free(FederationRegistry *ptr);

/**
 * Register a federation agreement in the registry.
 *
 * Returns JSON string of the agreement UUID on success.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ptr` must be a valid `FederationRegistry`. `agreement_json` must be a valid C string.
 */
char *divi_kingdom_federation_registry_register(FederationRegistry *ptr,
                                                const char *agreement_json);

/**
 * Check whether two communities are actively federated.
 *
 * Returns 1 for true, 0 for false, -1 on error.
 *
 * # Safety
 * `ptr` must be a valid `FederationRegistry`. C strings must be valid.
 */
int32_t divi_kingdom_federation_registry_is_federated(const FederationRegistry *ptr,
                                                      const char *community_a,
                                                      const char *community_b);

/**
 * List all communities actively federated with a given community.
 *
 * Returns JSON array of community ID strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ptr` must be a valid `FederationRegistry`. `community_id` must be a valid C string.
 */
char *divi_kingdom_federation_registry_federated_with(const FederationRegistry *ptr,
                                                      const char *community_id);

/**
 * Find a federation path between two communities via active links (BFS).
 *
 * Returns JSON array of community ID strings (the path), or JSON `null`
 * if no path exists. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ptr` must be a valid `FederationRegistry`. C strings must be valid.
 */
char *divi_kingdom_federation_registry_path_between(const FederationRegistry *ptr,
                                                    const char *from,
                                                    const char *to);

/**
 * Get all active federation agreements from the registry.
 *
 * Returns JSON array of FederationAgreement objects.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ptr` must be a valid `FederationRegistry`.
 */
char *divi_kingdom_federation_registry_all_active(const FederationRegistry *ptr);

/**
 * Get the total number of agreements in the registry (any status).
 *
 * Returns the count, or -1 on null pointer.
 *
 * # Safety
 * `ptr` must be a valid `FederationRegistry` or null.
 */
int32_t divi_kingdom_federation_registry_count(const FederationRegistry *ptr);

/**
 * Create a new Babel instance from a vocabulary seed.
 *
 * `seed` is the raw seed bytes (typically 32 bytes from ECDH shared secret
 * or Sentinal key derivation).
 *
 * Returns a Babel pointer. Free with `divi_lingo_babel_free`.
 *
 * # Safety
 * `seed` must be valid for `seed_len` bytes.
 */
struct LingoBabel *divi_lingo_babel_new(const uint8_t *seed, uintptr_t seed_len);

/**
 * Free a Babel instance.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_lingo_babel_new`, called exactly once.
 */
void divi_lingo_babel_free(struct LingoBabel *ptr);

/**
 * Encode text into Babel symbols (hardened, non-deterministic).
 *
 * Same input produces different output each time (nonce + homophones).
 * Returns a C string of Unicode symbols. Free via `divi_free_string`.
 *
 * # Safety
 * `babel` must be a valid pointer. `text` must be a valid C string.
 */
char *divi_lingo_babel_encode(const struct LingoBabel *babel, const char *text);

/**
 * Decode Babel symbols back into text.
 *
 * Returns a C string. Free via `divi_free_string`.
 *
 * # Safety
 * `babel` must be a valid pointer. `encoded` must be a valid C string.
 */
char *divi_lingo_babel_decode(const struct LingoBabel *babel, const char *encoded);

/**
 * Decode Babel symbols back into text using language-aware token rejoining.
 *
 * CJK/Kana/Hangul/Thai tokens are joined without spaces.
 * Latin/Arabic/Cyrillic/Devanagari tokens are joined with spaces.
 * `source_language` is a BCP 47 code (e.g., "ja", "zh-Hans", "en").
 *
 * Returns a C string. Free via `divi_free_string`.
 *
 * # Safety
 * `babel` must be a valid pointer. `encoded` and `source_language` must be valid C strings.
 */
char *divi_lingo_babel_decode_for_language(const struct LingoBabel *babel,
                                           const char *encoded,
                                           const char *source_language);

/**
 * Create a new empty document state with the given author identifier.
 *
 * Free with `divi_magic_document_free`.
 *
 * # Safety
 * `author` must be a valid C string.
 */
struct MagicDocumentState *divi_magic_document_new(const char *author);

/**
 * Free a document state.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_magic_document_new`, called exactly once.
 */
void divi_magic_document_free(struct MagicDocumentState *ptr);

/**
 * Returns the number of digits in the document.
 *
 * # Safety
 * `doc` must be a valid pointer.
 */
uintptr_t divi_magic_document_digit_count(const struct MagicDocumentState *doc);

/**
 * Returns the root digit ID as a UUID string, or null if none.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer.
 */
char *divi_magic_document_root_id(const struct MagicDocumentState *doc);

/**
 * Returns a single digit as JSON, or null if not found.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer. `id` must be a valid C string (UUID).
 */
char *divi_magic_document_digit(const struct MagicDocumentState *doc, const char *id);

/**
 * Returns all digits as a JSON array.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer.
 */
char *divi_magic_document_all_digits(const struct MagicDocumentState *doc);

/**
 * Returns children of a digit as a JSON array.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer. `parent_id` must be a valid C string (UUID).
 */
char *divi_magic_document_children_of(const struct MagicDocumentState *doc, const char *parent_id);

/**
 * Returns the document layout as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer.
 */
char *divi_magic_document_layout(const struct MagicDocumentState *doc);

/**
 * Returns the selection state as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer.
 */
char *divi_magic_document_selection(const struct MagicDocumentState *doc);

/**
 * Returns the current author identifier as a string.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer.
 */
char *divi_magic_document_author(const struct MagicDocumentState *doc);

/**
 * Insert a digit into the document.
 *
 * Returns the generated operation as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer. `digit_json` and (optionally) `parent_id`
 * must be valid C strings.
 */
char *divi_magic_document_insert(const struct MagicDocumentState *doc,
                                 const char *digit_json,
                                 const char *parent_id);

/**
 * Update a digit field.
 *
 * Returns the generated operation as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer. `id`, `field`, `old_json`, `new_json`
 * must be valid C strings.
 */
char *divi_magic_document_update(const struct MagicDocumentState *doc,
                                 const char *id,
                                 const char *field,
                                 const char *old_json,
                                 const char *new_json);

/**
 * Delete a digit (tombstone soft-delete).
 *
 * Returns the generated operation as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `doc` must be a valid pointer. `id` must be a valid C string (UUID).
 */
char *divi_magic_document_delete(const struct MagicDocumentState *doc, const char *id);

/**
 * Apply a DigitOperation (from JSON) to the document.
 *
 * Returns 0 on success (applied), 1 if duplicate (already applied), -1 on error.
 *
 * # Safety
 * `doc` must be a valid pointer. `operation_json` must be a valid C string.
 */
int32_t divi_magic_document_apply(const struct MagicDocumentState *doc, const char *operation_json);

/**
 * Load a set of digits wholesale (replaces existing state).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `doc` must be a valid pointer. `digits_json` must be a valid C string.
 * `root_id` may be null (no root).
 */
int32_t divi_magic_document_load_digits(const struct MagicDocumentState *doc,
                                        const char *digits_json,
                                        const char *root_id);

/**
 * Set the document layout from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `doc` must be a valid pointer. `layout_json` must be a valid C string.
 */
int32_t divi_magic_document_set_layout(const struct MagicDocumentState *doc,
                                       const char *layout_json);

/**
 * Select a digit in the document's selection state.
 *
 * # Safety
 * `doc` must be a valid pointer. `id` must be a valid C string (UUID).
 */
void divi_magic_document_select(const struct MagicDocumentState *doc, const char *id);

/**
 * Deselect a digit from the document's selection state.
 *
 * # Safety
 * `doc` must be a valid pointer. `id` must be a valid C string (UUID).
 */
void divi_magic_document_deselect_digit(const struct MagicDocumentState *doc, const char *id);

/**
 * Clear all selection in the document.
 *
 * # Safety
 * `doc` must be a valid pointer.
 */
void divi_magic_document_clear_selection(const struct MagicDocumentState *doc);

/**
 * Create a new canvas state with default viewport (1024x768).
 *
 * Free with `divi_magic_canvas_free`.
 */
struct MagicCanvasState *divi_magic_canvas_new(void);

/**
 * Create a new canvas state with a specific viewport size.
 *
 * Free with `divi_magic_canvas_free`.
 */
struct MagicCanvasState *divi_magic_canvas_with_viewport(double width, double height);

/**
 * Free a canvas state.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_magic_canvas_*` constructor, called exactly once.
 */
void divi_magic_canvas_free(struct MagicCanvasState *ptr);

/**
 * Convert a screen-space point to canvas-space.
 *
 * Writes the result to `out_cx` and `out_cy`.
 *
 * # Safety
 * `canvas` must be a valid pointer. `out_cx` and `out_cy` must be valid pointers.
 */
void divi_magic_canvas_screen_to_canvas(const struct MagicCanvasState *canvas,
                                        double sx,
                                        double sy,
                                        double *out_cx,
                                        double *out_cy);

/**
 * Convert a canvas-space point to screen-space.
 *
 * Writes the result to `out_sx` and `out_sy`.
 *
 * # Safety
 * `canvas` must be a valid pointer. `out_sx` and `out_sy` must be valid pointers.
 */
void divi_magic_canvas_canvas_to_screen(const struct MagicCanvasState *canvas,
                                        double cx,
                                        double cy,
                                        double *out_sx,
                                        double *out_sy);

/**
 * Returns the current zoom level.
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
double divi_magic_canvas_zoom_level(const struct MagicCanvasState *canvas);

/**
 * Set the zoom level (clamped to valid range).
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_set_zoom(const struct MagicCanvasState *canvas, double level);

/**
 * Zoom by a multiplicative factor around a center point (canvas coords).
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_zoom_by(const struct MagicCanvasState *canvas,
                               double factor,
                               double cx,
                               double cy);

/**
 * Zoom to fit a rectangle in the viewport.
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_zoom_to_fit(const struct MagicCanvasState *canvas,
                                   double x,
                                   double y,
                                   double w,
                                   double h);

/**
 * Set zoom to 100% (actual size).
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_zoom_actual_size(const struct MagicCanvasState *canvas);

/**
 * Set zoom to a specific percentage (e.g. 200.0 for 200%).
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_zoom_percent(const struct MagicCanvasState *canvas, double percent);

/**
 * Returns the current canvas selection as a JSON array of UUID strings.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
char *divi_magic_canvas_selection(const struct MagicCanvasState *canvas);

/**
 * Select a single digit on the canvas (replaces current selection).
 *
 * # Safety
 * `canvas` must be a valid pointer. `id` must be a valid C string (UUID).
 */
void divi_magic_canvas_select(const struct MagicCanvasState *canvas, const char *id);

/**
 * Select multiple digits on the canvas (adds to current selection).
 *
 * # Safety
 * `canvas` must be a valid pointer. `ids_json` must be a valid C string (JSON array of UUIDs).
 */
void divi_magic_canvas_select_multiple(const struct MagicCanvasState *canvas, const char *ids_json);

/**
 * Deselect a digit on the canvas.
 *
 * # Safety
 * `canvas` must be a valid pointer. `id` must be a valid C string (UUID).
 */
void divi_magic_canvas_deselect(const struct MagicCanvasState *canvas, const char *id);

/**
 * Clear the canvas selection entirely.
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_clear_selection(const struct MagicCanvasState *canvas);

/**
 * Check whether a digit is selected on the canvas.
 *
 * # Safety
 * `canvas` must be a valid pointer. `id` must be a valid C string (UUID).
 */
bool divi_magic_canvas_is_selected(const struct MagicCanvasState *canvas, const char *id);

/**
 * Returns the number of selected items on the canvas.
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
uintptr_t divi_magic_canvas_selection_count(const struct MagicCanvasState *canvas);

/**
 * Snap a point to the grid (if enabled). Writes result to `out_x`, `out_y`.
 *
 * # Safety
 * `canvas` must be a valid pointer. `out_x` and `out_y` must be valid pointers.
 */
void divi_magic_canvas_snap_point(const struct MagicCanvasState *canvas,
                                  double x,
                                  double y,
                                  double *out_x,
                                  double *out_y);

/**
 * Set grid size and snap-to-grid. Pass 0 for `size` to disable grid.
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_set_grid(const struct MagicCanvasState *canvas, double size, bool snap);

/**
 * Set whether alignment guides are visible.
 *
 * # Safety
 * `canvas` must be a valid pointer.
 */
void divi_magic_canvas_set_guides(const struct MagicCanvasState *canvas, bool visible);

/**
 * Compute drag handles for a selection bounding rect at the given zoom.
 *
 * Returns a JSON array of DragHandle objects, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * All parameters must be valid.
 */
char *divi_magic_canvas_compute_handles(double x, double y, double w, double h, double zoom);

/**
 * Hit-test a point against a set of handles (from JSON).
 *
 * Returns the matching DragHandle as JSON, or null if no hit.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `handles_json` must be a valid C string.
 */
char *divi_magic_canvas_hit_test_handles(double px, double py, const char *handles_json);

/**
 * Create a new empty renderer registry (with fallback renderer).
 *
 * Free with `divi_magic_renderer_registry_free`.
 */
struct MagicRendererRegistry *divi_magic_renderer_registry_new(void);

/**
 * Create a renderer registry pre-loaded with all built-in renderers.
 *
 * Free with `divi_magic_renderer_registry_free`.
 */
struct MagicRendererRegistry *divi_magic_renderer_registry_new_with_all(void);

/**
 * Free a renderer registry.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_magic_renderer_registry_*` constructor.
 */
void divi_magic_renderer_registry_free(struct MagicRendererRegistry *ptr);

/**
 * Returns the number of registered renderers.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
uintptr_t divi_magic_renderer_registry_count(const struct MagicRendererRegistry *registry);

/**
 * Check whether a renderer is registered for the given digit type.
 *
 * # Safety
 * `registry` must be a valid pointer. `digit_type` must be a valid C string.
 */
bool divi_magic_renderer_registry_has(const struct MagicRendererRegistry *registry,
                                      const char *digit_type);

/**
 * Returns the registered digit types as a JSON array of strings.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_magic_renderer_registry_types(const struct MagicRendererRegistry *registry);

/**
 * Render a digit using the appropriate renderer.
 *
 * Returns the RenderSpec as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `digit_json`, `mode_json`, `context_json`
 * must be valid C strings.
 */
char *divi_magic_renderer_render(const struct MagicRendererRegistry *registry,
                                 const char *digit_json,
                                 const char *mode_json,
                                 const char *context_json);

/**
 * Get the estimated size for a digit without full rendering.
 *
 * Writes width and height to `out_w` and `out_h`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `digit_json`, `context_json`
 * must be valid C strings. `out_w`, `out_h` must be valid pointers.
 */
int32_t divi_magic_renderer_estimated_size(const struct MagicRendererRegistry *registry,
                                           const char *digit_json,
                                           const char *context_json,
                                           double *out_w,
                                           double *out_h);

/**
 * Create a new render cache with default max size (200).
 *
 * Free with `divi_magic_cache_free`.
 */
struct MagicRenderCache *divi_magic_cache_new(void);

/**
 * Create a new render cache with a specific max size.
 *
 * Free with `divi_magic_cache_free`.
 */
struct MagicRenderCache *divi_magic_cache_with_max_size(uintptr_t max_size);

/**
 * Free a render cache.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_magic_cache_*` constructor.
 */
void divi_magic_cache_free(struct MagicRenderCache *ptr);

/**
 * Get a cached RenderSpec as JSON. Returns null if not cached (miss).
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `cache` must be a valid pointer. `digit_id` must be a valid C string (UUID).
 * `mode_json` must be a valid C string.
 */
char *divi_magic_cache_get(const struct MagicRenderCache *cache,
                           const char *digit_id,
                           const char *mode_json);

/**
 * Insert a RenderSpec (from JSON) into the cache.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `cache` must be a valid pointer. `spec_json` must be a valid C string.
 */
int32_t divi_magic_cache_insert(const struct MagicRenderCache *cache, const char *spec_json);

/**
 * Invalidate all cached specs for a digit.
 *
 * # Safety
 * `cache` must be a valid pointer. `digit_id` must be a valid C string (UUID).
 */
void divi_magic_cache_invalidate(const struct MagicRenderCache *cache, const char *digit_id);

/**
 * Clear the entire render cache.
 *
 * # Safety
 * `cache` must be a valid pointer.
 */
void divi_magic_cache_invalidate_all(const struct MagicRenderCache *cache);

/**
 * Returns the number of cached entries.
 *
 * # Safety
 * `cache` must be a valid pointer.
 */
uintptr_t divi_magic_cache_size(const struct MagicRenderCache *cache);

/**
 * Returns the cache hit rate (0.0 to 1.0).
 *
 * # Safety
 * `cache` must be a valid pointer.
 */
double divi_magic_cache_hit_rate(const struct MagicRenderCache *cache);

/**
 * Create a new empty tool registry.
 *
 * Free with `divi_magic_tool_registry_free`.
 */
struct MagicToolRegistry *divi_magic_tool_registry_new(void);

/**
 * Create a tool registry pre-loaded with all default tools.
 *
 * Free with `divi_magic_tool_registry_free`.
 */
struct MagicToolRegistry *divi_magic_tool_registry_new_default(void);

/**
 * Free a tool registry.
 *
 * # Safety
 * `ptr` must be a valid pointer from a `divi_magic_tool_registry_*` constructor.
 */
void divi_magic_tool_registry_free(struct MagicToolRegistry *ptr);

/**
 * Returns the number of registered tools.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
uintptr_t divi_magic_tool_registry_count(const struct MagicToolRegistry *registry);

/**
 * Returns the IDs of all registered tools as a JSON array of strings.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_magic_tool_registry_list(const struct MagicToolRegistry *registry);

/**
 * Select (activate) a tool by ID. Returns `true` if found.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
bool divi_magic_tool_registry_select(const struct MagicToolRegistry *registry, const char *id);

/**
 * Returns the active tool's ID as a string, or null if none active.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_magic_tool_registry_active_id(const struct MagicToolRegistry *registry);

/**
 * Returns the active tool's cursor style as JSON, or null if none active.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_magic_tool_registry_active_cursor(const struct MagicToolRegistry *registry);

/**
 * Handle a press event on the active tool.
 *
 * Lock order: tool registry first, then document (prevents deadlock).
 *
 * Returns the resulting ToolAction as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` and `doc` must be valid pointers.
 */
char *divi_magic_tool_on_press(const struct MagicToolRegistry *registry,
                               double x,
                               double y,
                               bool shift,
                               bool alt,
                               bool command,
                               const struct MagicDocumentState *doc);

/**
 * Handle a drag event on the active tool.
 *
 * Lock order: tool registry first, then document (prevents deadlock).
 *
 * Returns the resulting ToolAction as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` and `doc` must be valid pointers.
 */
char *divi_magic_tool_on_drag(const struct MagicToolRegistry *registry,
                              double x,
                              double y,
                              bool shift,
                              bool alt,
                              bool command,
                              const struct MagicDocumentState *doc);

/**
 * Handle a release event on the active tool.
 *
 * Lock order: tool registry first, then document (prevents deadlock).
 *
 * Returns the resulting ToolAction as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` and `doc` must be valid pointers.
 */
char *divi_magic_tool_on_release(const struct MagicToolRegistry *registry,
                                 double x,
                                 double y,
                                 bool shift,
                                 bool alt,
                                 bool command,
                                 const struct MagicDocumentState *doc);

/**
 * Handle a hover event on the active tool.
 *
 * Returns the cursor style as JSON, or null if no override / no active tool.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry` and `doc` must be valid pointers.
 */
char *divi_magic_tool_on_hover(const struct MagicToolRegistry *registry,
                               double x,
                               double y,
                               const struct MagicDocumentState *doc);

/**
 * Create a new empty document history.
 *
 * Free with `divi_magic_history_free`.
 */
struct MagicDocumentHistory *divi_magic_history_new(void);

/**
 * Free a document history.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_magic_history_new`.
 */
void divi_magic_history_free(struct MagicDocumentHistory *ptr);

/**
 * Whether undo is available.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
bool divi_magic_history_can_undo(const struct MagicDocumentHistory *history);

/**
 * Whether redo is available.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
bool divi_magic_history_can_redo(const struct MagicDocumentHistory *history);

/**
 * Returns the number of undo entries.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
uintptr_t divi_magic_history_undo_count(const struct MagicDocumentHistory *history);

/**
 * Returns the number of redo entries.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
uintptr_t divi_magic_history_redo_count(const struct MagicDocumentHistory *history);

/**
 * Clear all undo/redo history.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
void divi_magic_history_clear(const struct MagicDocumentHistory *history);

/**
 * Execute an action: apply it to the document and record undo history.
 *
 * Lock order: history first, then document (prevents deadlock).
 *
 * Returns the resulting operation as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `history` and `doc` must be valid pointers. `action_json` must be a valid C string.
 */
char *divi_magic_history_execute(const struct MagicDocumentHistory *history,
                                 const struct MagicDocumentState *doc,
                                 const char *action_json);

/**
 * Undo the last action.
 *
 * Lock order: history first, then document (prevents deadlock).
 *
 * Returns the undo operation as JSON, or null if nothing to undo / error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `history` and `doc` must be valid pointers.
 */
char *divi_magic_history_undo(const struct MagicDocumentHistory *history,
                              const struct MagicDocumentState *doc);

/**
 * Redo the last undone action.
 *
 * Lock order: history first, then document (prevents deadlock).
 *
 * Returns the redo operation as JSON, or null if nothing to redo / error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `history` and `doc` must be valid pointers.
 */
char *divi_magic_history_redo(const struct MagicDocumentHistory *history,
                              const struct MagicDocumentState *doc);

/**
 * Build a ProjectionContext from digits and produce it as JSON.
 *
 * Returns the ProjectionContext as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `digits_json` and `reign_json` must be valid C strings. `root_id` may be null.
 */
char *divi_magic_projection_build_context(const char *digits_json,
                                          const char *root_id,
                                          const char *reign_json);

/**
 * Project SwiftUI code from a ProjectionContext (JSON).
 *
 * Returns a JSON array of GeneratedFile, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `context_json` must be a valid C string.
 */
char *divi_magic_projection_swiftui(const char *context_json);

/**
 * Project React (TSX) code from a ProjectionContext (JSON).
 *
 * Returns a JSON array of GeneratedFile, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `context_json` must be a valid C string.
 */
char *divi_magic_projection_react(const char *context_json);

/**
 * Project HTML/CSS code from a ProjectionContext (JSON).
 *
 * Returns a JSON array of GeneratedFile, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `context_json` must be a valid C string.
 */
char *divi_magic_projection_html(const char *context_json);

/**
 * Build an AccessibilitySpec from a digit (JSON), role (JSON), and label.
 *
 * Returns the AccessibilitySpec as JSON, or null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `digit_json`, `role_json`, `label` must be valid C strings.
 */
char *divi_magic_accessibility_from_digit(const char *digit_json,
                                          const char *role_json,
                                          const char *label);

/**
 * Create a new default SlideSequenceState as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 */
char *divi_magic_slide_sequence_new(void);

/**
 * Advance to the next slide. Returns the updated state as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `state_json` must be a valid C string.
 */
char *divi_magic_slide_sequence_next(const char *state_json);

/**
 * Go to the previous slide. Returns the updated state as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `state_json` must be a valid C string.
 */
char *divi_magic_slide_sequence_previous(const char *state_json);

/**
 * Jump to a specific slide index. Returns the updated state as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `state_json` must be a valid C string.
 */
char *divi_magic_slide_sequence_go_to(const char *state_json, uintptr_t index);

/**
 * Insert a slide at the given index. Returns the updated state as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `state_json` and `slide_id` must be valid C strings.
 */
char *divi_magic_slide_sequence_add_slide(const char *state_json, const char *slide_id);

/**
 * Returns the core type registry (9 built-in types) as JSON.
 *
 * The returned pointer must be freed via `divi_free_string`.
 */
char *divi_magic_type_registry_core(void);

/**
 * Get a specific type definition from a registry (JSON).
 *
 * Returns the DigitTypeDefinition as JSON, or null if not found.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `registry_json` and `digit_type` must be valid C strings.
 */
char *divi_magic_type_registry_get(const char *registry_json, const char *digit_type);

/**
 * Start an Omnibus instance.
 *
 * `device_name` — human-readable name (e.g., "Sam's Mac").
 * `port` — relay server port. 0 = OS-assigned.
 * `bind_all` — if true, listens on all interfaces (LAN reachable).
 *
 * Returns an Omnibus pointer. Free with `divi_omnibus_free`.
 *
 * # Safety
 * `device_name` must be a valid C string.
 */
Omnibus *divi_omnibus_start(const char *device_name, uint16_t port, bool bind_all);

/**
 * Free an Omnibus instance.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_omnibus_start`, called exactly once.
 */
void divi_omnibus_free(Omnibus *ptr);

/**
 * Create a new identity with a display name.
 *
 * Returns the crown_id (bech32 public key) as a C string, or null on error.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer. `display_name` must be a valid C string.
 */
char *divi_omnibus_create_identity(const Omnibus *omni, const char *display_name);

/**
 * Get the public key (crown_id bech32), or null if no identity.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_pubkey(const Omnibus *omni);

/**
 * Export the keyring as JSON bytes.
 * Output written to `out_data`/`out_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
int32_t divi_omnibus_export_keyring(const Omnibus *omni, uint8_t **out_data, uintptr_t *out_len);

/**
 * Get the public key as hex, or null if no identity.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_pubkey_hex(const Omnibus *omni);

/**
 * Get the current profile as JSON, or null if no identity.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_profile_json(const Omnibus *omni);

/**
 * Update the display name and re-publish profile.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer. `name` must be a valid C string.
 */
int32_t divi_omnibus_update_display_name(const Omnibus *omni, const char *name);

/**
 * Publish a text note. Signs with the loaded identity.
 *
 * Returns the signed event as JSON, or null on error.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer. `content` must be a valid C string.
 */
char *divi_omnibus_post(const Omnibus *omni, const char *content);

/**
 * Publish a pre-signed event to all connected relays.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer. `event_json` must be a valid C string.
 */
int32_t divi_omnibus_publish(const Omnibus *omni, const char *event_json);

/**
 * Inject an event directly into the local relay store (bypasses network).
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer. `event_json` must be a valid C string.
 */
int32_t divi_omnibus_seed_event(const Omnibus *omni, const char *event_json);

/**
 * Connect to a specific relay.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer. `url` must be a valid C string.
 */
int32_t divi_omnibus_connect_relay(const Omnibus *omni, const char *url);

/**
 * Set a home node for persistent sync.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer. `url` must be a valid C string.
 */
int32_t divi_omnibus_set_home_node(const Omnibus *omni, const char *url);

/**
 * Query events from the local relay store.
 *
 * `filter_json` is a JSON OmniFilter object.
 * Returns a JSON array of matching events. Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer. `filter_json` must be a valid C string.
 */
char *divi_omnibus_query(const Omnibus *omni, const char *filter_json);

/**
 * Subscribe to events matching filters.
 *
 * `filters_json` is a JSON array of OmniFilter objects.
 * Returns the subscription ID as a C string. Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer. `filters_json` must be a valid C string.
 */
char *divi_omnibus_subscribe(const Omnibus *omni, const char *filters_json);

/**
 * Register a callback for ALL incoming events.
 *
 * The callback fires on a background thread for each event.
 * Only one callback can be active — calling again replaces the previous one.
 *
 * # Safety
 * `omni` must be a valid pointer. `context` must be valid for the
 * lifetime of the callback, or null.
 */
void divi_omnibus_on_event(const Omnibus *omni, OmnibusEventCallback callback, void *context);

/**
 * Get all currently discovered peers as a JSON array.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_peers(const Omnibus *omni);

/**
 * Get the number of currently discovered peers.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
uint32_t divi_omnibus_peer_count(const Omnibus *omni);

/**
 * Connect to all currently discovered peers.
 * Returns the number of peers connected.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
uint32_t divi_omnibus_connect_discovered_peers(const Omnibus *omni);

/**
 * Get the full status as JSON.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_status(const Omnibus *omni);

/**
 * Get the local relay server port.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
uint16_t divi_omnibus_port(const Omnibus *omni);

/**
 * Get the local relay server WebSocket URL.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_relay_url(const Omnibus *omni);

/**
 * Get the public URL of this node (if UPnP mapping succeeded).
 * Returns null if UPnP is unavailable or mapping failed.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_public_url(const Omnibus *omni);

/**
 * Load an existing identity from a directory path.
 *
 * The path should contain a `soul/` subdirectory and optionally `keyring.dat`.
 * Returns the crown_id (bech32 public key) as a C string, or null on error.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer. `path` must be a valid C string.
 */
char *divi_omnibus_load_identity(const Omnibus *omni, const char *path);

/**
 * Import a keyring from exported bytes (for syncing from another device).
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer. `data` must point to `len` valid bytes.
 */
int32_t divi_omnibus_import_keyring(const Omnibus *omni, const uint8_t *data, uintptr_t len);

/**
 * Get health snapshots for all relays in the pool as a JSON array.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_relay_health(const Omnibus *omni);

/**
 * Get health snapshot for a specific relay by URL as JSON.
 * Returns null if the relay is not found. Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer. `url` must be a valid C string.
 */
char *divi_omnibus_relay_health_for(const Omnibus *omni, const char *url);

/**
 * Get statistics about the local event store as JSON.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_store_stats(const Omnibus *omni);

/**
 * Get the most recent log entries as a JSON array.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_recent_logs(const Omnibus *omni, uint32_t count);

/**
 * Push a log entry into the capture buffer.
 *
 * `level` — log level string (e.g., "INFO", "WARN", "ERROR").
 * `module_or_null` — optional module name, or null.
 * `message` — the log message.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `omni` must be a valid pointer. `level` and `message` must be valid C strings.
 * `module_or_null` may be null.
 */
int32_t divi_omnibus_push_log(const Omnibus *omni,
                              const char *level,
                              const char *module_or_null,
                              const char *message);

/**
 * Get the gospel registry snapshot as JSON, or null if no registry.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_gospel_json(const Omnibus *omni);

/**
 * Save the gospel registry to the encrypted database.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
void divi_omnibus_save_gospel(const Omnibus *omni);

/**
 * Register a gospel entry (an OmniEvent) into the gospel registry.
 *
 * `entry_json` is a JSON-serialized OmniEvent.
 * Returns 0 on success (inserted or duplicate), -1 on error (rejected, no registry, or parse error).
 *
 * # Safety
 * `omni` must be a valid pointer. `entry_json` must be a valid C string.
 */
int32_t divi_omnibus_register_gospel(const Omnibus *omni,
                                     const char *entry_json);

/**
 * Start an Omnibus instance with a full JSON configuration.
 *
 * `config_json` is a JSON object with fields:
 * - `data_dir` (string | null) — persistent storage directory
 * - `device_name` (string) — human-readable device name
 * - `port` (u16) — relay server port, 0 = OS-assigned
 * - `bind_all` (bool) — listen on all interfaces
 * - `home_node` (string | null) — home node WebSocket URL
 * - `log_capture_capacity` (usize) — max log entries in ring buffer
 *
 * Returns an Omnibus pointer. Free with `divi_omnibus_free`.
 *
 * # Safety
 * `config_json` must be a valid C string.
 */
Omnibus *divi_omnibus_start_with_config(const char *config_json);

/**
 * Get the current Omnibus configuration as JSON.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_config_json(const Omnibus *omni);

/**
 * Returns the HTTP server port, or 0 if not running.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
uint16_t divi_omnibus_http_port(const Omnibus *omni);

/**
 * Returns the HTTP server base URL, or null if not running.
 * Free via `divi_free_string`.
 *
 * # Safety
 * `omni` must be a valid pointer.
 */
char *divi_omnibus_http_url(const Omnibus *omni);

/**
 * Create a new `DisclosureTracker` at Citizen tier.
 *
 * Returns JSON (DisclosureTracker). Caller must free via `divi_free_string`.
 */
char *divi_oracle_disclosure_tracker_new(void);

/**
 * Create a `DisclosureTracker` with custom config.
 *
 * `config_json` is a JSON `DisclosureConfig` (e.g. `{"enthusiast_threshold":5,"operator_threshold":3}`).
 * Returns JSON (DisclosureTracker). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `config_json` must be a valid C string.
 */
char *divi_oracle_disclosure_tracker_with_config(const char *config_json);

/**
 * Record a disclosure signal on a tracker.
 *
 * Takes tracker JSON and signal JSON, returns modified tracker JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_oracle_disclosure_tracker_record(const char *tracker_json, const char *signal_json);

/**
 * Get the current sovereignty tier from a tracker.
 *
 * Returns JSON (SovereigntyTier). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tracker_json` must be a valid C string.
 */
char *divi_oracle_disclosure_tracker_level(const char *tracker_json);

/**
 * Manually set the sovereignty tier on a tracker.
 *
 * `level_json` is a JSON `SovereigntyTier` (e.g. `"Architect"`).
 * Returns modified tracker JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_oracle_disclosure_tracker_set_level(const char *tracker_json, const char *level_json);

/**
 * Clear the manual override on a tracker (return to behavior-driven level).
 *
 * Returns modified tracker JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tracker_json` must be a valid C string.
 */
char *divi_oracle_disclosure_tracker_clear_override(const char *tracker_json);

/**
 * Get signal counts from a tracker.
 *
 * Returns JSON `{"steward":<n>,"architect":<n>}`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tracker_json` must be a valid C string.
 */
char *divi_oracle_disclosure_tracker_signal_counts(const char *tracker_json);

/**
 * Get the default settings for a sovereignty tier.
 *
 * `tier_json` is a JSON `SovereigntyTier` (e.g. `"Citizen"`).
 * Returns JSON (TierDefaults). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tier_json` must be a valid C string.
 */
char *divi_oracle_tier_defaults(const char *tier_json);

/**
 * Get all tier defaults (one per sovereignty tier).
 *
 * Returns JSON array of TierDefaults. Caller must free via `divi_free_string`.
 */
char *divi_oracle_tier_defaults_all(void);

/**
 * Get the feature visibility for a sovereignty tier.
 *
 * `tier_json` is a JSON `SovereigntyTier` (e.g. `"Steward"`).
 * Returns JSON (FeatureVisibility). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tier_json` must be a valid C string.
 */
char *divi_oracle_feature_visibility(const char *tier_json);

/**
 * Create and validate a workflow from JSON.
 *
 * `workflow_json` is a JSON `Workflow`. This function deserializes and
 * re-serializes to normalize the data (e.g., default fields).
 * Returns JSON (Workflow). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `workflow_json` must be a valid C string.
 */
char *divi_oracle_workflow_new(const char *workflow_json);

/**
 * Check if a trigger matches an event.
 *
 * Returns 1 if the trigger matches the event, 0 if not, -1 on error.
 *
 * # Safety
 * All C strings must be valid.
 */
int32_t divi_oracle_trigger_matches(const char *trigger_json, const char *event_json);

/**
 * Evaluate a condition against a set of fields.
 *
 * `condition_json` is a JSON `Condition`.
 * `fields_json` is a JSON `HashMap<String, String>`.
 * Returns 1 if the condition evaluates to true, 0 if false, -1 on error.
 *
 * # Safety
 * All C strings must be valid.
 */
int32_t divi_oracle_condition_evaluate(const char *condition_json, const char *fields_json);

/**
 * Check if a workflow should fire for the given event.
 *
 * Returns 1 if the workflow should fire, 0 if not, -1 on error.
 *
 * # Safety
 * All C strings must be valid.
 */
int32_t divi_oracle_workflow_should_fire(const char *workflow_json, const char *event_json);

/**
 * Create and validate a `StaticHint` from JSON.
 *
 * Deserializes and re-serializes to normalize. Returns JSON (StaticHint).
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `hint_json` must be a valid C string.
 */
char *divi_oracle_static_hint_new(const char *hint_json);

/**
 * Check if a static hint should be shown given a context.
 *
 * Returns 1 if the hint should be shown, 0 if not, -1 on error.
 *
 * # Safety
 * All C strings must be valid.
 */
int32_t divi_oracle_static_hint_should_show(const char *hint_json, const char *context_json);

/**
 * Create an empty `HintContext`.
 *
 * Returns JSON (HintContext). Caller must free via `divi_free_string`.
 */
char *divi_oracle_hint_context_new(void);

/**
 * Create a new empty `WorkflowRegistry`.
 *
 * Free with `divi_oracle_registry_free`.
 */
struct OracleWorkflowRegistry *divi_oracle_registry_new(void);

/**
 * Free a `WorkflowRegistry`.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_oracle_registry_free(struct OracleWorkflowRegistry *ptr);

/**
 * Register a workflow in the registry.
 *
 * `workflow_json` is a JSON `Workflow`. Replaces any existing workflow with the same ID.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `workflow_json` must be a valid C string.
 */
int32_t divi_oracle_registry_register(const struct OracleWorkflowRegistry *registry,
                                      const char *workflow_json);

/**
 * Unregister a workflow by ID.
 *
 * Returns JSON (Workflow) of the removed workflow, or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
char *divi_oracle_registry_unregister(const struct OracleWorkflowRegistry *registry,
                                      const char *id);

/**
 * Get a workflow by ID.
 *
 * Returns JSON (Workflow) or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
char *divi_oracle_registry_get(const struct OracleWorkflowRegistry *registry, const char *id);

/**
 * Enable a workflow by ID.
 *
 * Returns 0 on success, -1 on error (workflow not found).
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
int32_t divi_oracle_registry_enable(const struct OracleWorkflowRegistry *registry, const char *id);

/**
 * Disable a workflow by ID.
 *
 * Returns 0 on success, -1 on error (workflow not found).
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
int32_t divi_oracle_registry_disable(const struct OracleWorkflowRegistry *registry, const char *id);

/**
 * Grant consent for a workflow by ID.
 *
 * Returns 0 on success, -1 on error (workflow not found).
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
int32_t divi_oracle_registry_grant_consent(const struct OracleWorkflowRegistry *registry,
                                           const char *id);

/**
 * Revoke consent for a workflow by ID.
 *
 * Returns 0 on success, -1 on error (workflow not found).
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
int32_t divi_oracle_registry_revoke_consent(const struct OracleWorkflowRegistry *registry,
                                            const char *id);

/**
 * List all workflows in the registry.
 *
 * Returns JSON array of Workflows. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_oracle_registry_list(const struct OracleWorkflowRegistry *registry);

/**
 * List workflows for a specific actor.
 *
 * Returns JSON array of Workflows. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `actor` must be a valid C string.
 */
char *divi_oracle_registry_list_for_actor(const struct OracleWorkflowRegistry *registry,
                                          const char *actor);

/**
 * Evaluate an event against all workflows in the registry.
 *
 * `event_json` is a JSON `WorkflowEvent`. `timestamp` is a Unix timestamp (seconds).
 * Returns JSON array of `WorkflowMatch`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `event_json` must be a valid C string.
 */
char *divi_oracle_registry_evaluate(const struct OracleWorkflowRegistry *registry,
                                    const char *event_json,
                                    uint64_t timestamp);

/**
 * Get the count of registered workflows.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
uintptr_t divi_oracle_registry_count(const struct OracleWorkflowRegistry *registry);

/**
 * Export all workflows as JSON for persistence.
 *
 * Returns JSON array of Workflows. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_oracle_registry_export(const struct OracleWorkflowRegistry *registry);

/**
 * Import workflows from JSON (e.g., from persistence).
 *
 * `workflows_json` is a JSON array of Workflows. Replaces existing workflows
 * with the same IDs.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `workflows_json` must be a valid C string.
 */
int32_t divi_oracle_registry_import(const struct OracleWorkflowRegistry *registry,
                                    const char *workflows_json);

/**
 * Get the full audit log.
 *
 * Returns JSON array of `AuditEntry`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_oracle_registry_audit_log(const struct OracleWorkflowRegistry *registry);

/**
 * Get audit entries for a specific workflow.
 *
 * Returns JSON array of `AuditEntry`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid C string.
 */
char *divi_oracle_registry_audit_for_workflow(const struct OracleWorkflowRegistry *registry,
                                              const char *id);

/**
 * Clear the audit log.
 *
 * Returns 0 on success.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
int32_t divi_oracle_registry_clear_audit(const struct OracleWorkflowRegistry *registry);

/**
 * Create a new Pager. Returns an opaque pointer.
 * Free with `divi_pager_free`.
 */
Pager *divi_pager_new(void);

/**
 * Free a Pager created by `divi_pager_new`.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_pager_new`, called exactly once.
 */
void divi_pager_free(Pager *ptr);

/**
 * Push a notification from a JSON string.
 *
 * The JSON must deserialize to a `Notification`. Returns the UUID of the
 * notification as a string (`*mut c_char`), or null on error.
 *
 * The returned string must be freed via `divi_free_string`.
 *
 * # Safety
 * `pager` must be a valid pointer. `json` must be a valid C string.
 */
char *divi_pager_notify(const Pager *pager, const char *json);

/**
 * Get pending notifications as a JSON array.
 *
 * If `priority_json` is non-null, it should be a JSON string like `"high"` or `"urgent"`
 * to filter by priority. Pass null for all priorities.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `pager` must be a valid pointer. `priority_json` can be null.
 */
char *divi_pager_get_pending(const Pager *pager, const char *priority_json);

/**
 * Get unread notifications as a JSON array.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `pager` must be a valid pointer.
 */
char *divi_pager_get_unread(const Pager *pager);

/**
 * Mark a notification as read. Returns true if found, false otherwise.
 *
 * # Safety
 * `pager` must be a valid pointer. `uuid_str` must be a valid C string.
 */
bool divi_pager_mark_read(const Pager *pager, const char *uuid_str);

/**
 * Dismiss a notification. Returns true if found, false otherwise.
 *
 * # Safety
 * `pager` must be a valid pointer. `uuid_str` must be a valid C string.
 */
bool divi_pager_dismiss(const Pager *pager, const char *uuid_str);

/**
 * Get the count of unread notifications.
 *
 * # Safety
 * `pager` must be a valid pointer.
 */
uintptr_t divi_pager_badge_count(const Pager *pager);

/**
 * Prune expired notifications.
 *
 * # Safety
 * `pager` must be a valid pointer.
 */
void divi_pager_prune_expired(const Pager *pager);

/**
 * Export pager state as a JSON array of notifications.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 *
 * # Safety
 * `pager` must be a valid pointer.
 */
char *divi_pager_export_state(const Pager *pager);

/**
 * Restore pager state from a JSON array of notifications.
 *
 * # Safety
 * `pager` must be a valid pointer. `json` must be a valid C string.
 */
void divi_pager_restore_state(const Pager *pager, const char *json);

/**
 * Create a new Place with Private visibility.
 *
 * `type_json` is a JSON `PlaceType` (e.g. `"Cafe"`, `{"Custom":"Treehouse"}`).
 * Returns JSON (Place). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `name`, `type_json`, and `owner` must be valid C strings.
 */
char *divi_physical_place_new(const char *name,
                              const char *type_json,
                              double lat,
                              double lon,
                              const char *owner);

/**
 * Update a Place's location. Only the owner may do this.
 *
 * Takes Place JSON, returns modified Place JSON.
 *
 * # Safety
 * `place_json` and `updater` must be valid C strings.
 */
char *divi_physical_place_update_location(const char *place_json,
                                          double lat,
                                          double lon,
                                          const char *updater);

/**
 * Update a Place's name. Only the owner may do this.
 *
 * Takes Place JSON, returns modified Place JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_place_update_name(const char *place_json,
                                      const char *name,
                                      const char *updater);

/**
 * Check whether a viewer can see a Place.
 *
 * `memberships_json` is a JSON `Vec<String>` of community IDs the viewer belongs to.
 * Returns 1=visible, 0=not visible, -1=error.
 *
 * # Safety
 * All C strings must be valid.
 */
int32_t divi_physical_place_is_visible_to(const char *place_json,
                                          const char *viewer,
                                          const char *memberships_json);

/**
 * Set a Place's visibility.
 *
 * `visibility_json` is a JSON `PlaceVisibility` (e.g. `"Public"`, `"Private"`,
 * `{"Shared":["cpub1..."]}`).
 * Returns modified Place JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_place_set_visibility(const char *place_json, const char *visibility_json);

/**
 * Create a new Region.
 *
 * `type_json` is a JSON `RegionType` (e.g. `"City"`, `{"Custom":"Watershed"}`).
 * `boundary_json` is a JSON `RegionBoundary`.
 * Returns JSON (Region). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_region_new(const char *name,
                               const char *type_json,
                               const char *boundary_json,
                               const char *creator);

/**
 * Check whether a point falls inside a Region's boundary.
 *
 * Returns 1=inside, 0=outside, -1=error.
 *
 * # Safety
 * `region_json` must be a valid C string.
 */
int32_t divi_physical_region_contains_point(const char *region_json, double lat, double lon);

/**
 * Create a new RegionDeclaration.
 *
 * `region_ids_json` is a JSON `Vec<Uuid>` (array of UUID strings).
 * Returns JSON (RegionDeclaration). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_declaration_new(const char *person, const char *region_ids_json);

/**
 * Add a region to a RegionDeclaration (idempotent).
 *
 * `id` is a UUID string. Returns modified declaration JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_declaration_add_region(const char *decl_json, const char *id);

/**
 * Remove a region from a RegionDeclaration.
 *
 * `id` is a UUID string. Returns modified declaration JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_declaration_remove_region(const char *decl_json, const char *id);

/**
 * Create a new Rendezvous in Proposed status.
 *
 * `scheduled_at` is a Unix timestamp in seconds. `purpose_json` is a JSON
 * `RendezvousPurpose` (e.g. `"Social"`, `{"Custom":"Hackathon"}`).
 * Returns JSON (Rendezvous). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_rendezvous_new(const char *title,
                                   const char *organizer,
                                   int64_t scheduled_at,
                                   const char *purpose_json);

/**
 * Invite a participant to a Rendezvous (idempotent).
 *
 * Returns modified Rendezvous JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_rendezvous_invite(const char *rv_json, const char *crown_id);

/**
 * Record or update an RSVP for a Rendezvous.
 *
 * `response_json` is a JSON `RsvpResponse` (e.g. `"Attending"`, `"Maybe"`, `"Declined"`).
 * `message` may be null. Returns modified Rendezvous JSON.
 *
 * # Safety
 * All C strings must be valid. `message` may be null.
 */
char *divi_physical_rendezvous_rsvp(const char *rv_json,
                                    const char *person,
                                    const char *response_json,
                                    const char *message);

/**
 * Confirm a Rendezvous (Proposed -> Confirmed). Only the organizer.
 *
 * Returns modified Rendezvous JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_rendezvous_confirm(const char *rv_json, const char *updater);

/**
 * Cancel a Rendezvous. Only the organizer.
 *
 * `reason` may be null. Returns modified Rendezvous JSON.
 *
 * # Safety
 * All C strings must be valid. `reason` may be null.
 */
char *divi_physical_rendezvous_cancel(const char *rv_json,
                                      const char *reason,
                                      const char *canceller);

/**
 * Complete a Rendezvous. Only the organizer.
 *
 * Returns modified Rendezvous JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_rendezvous_complete(const char *rv_json, const char *updater);

/**
 * Reschedule a Rendezvous. Only the organizer.
 *
 * `new_time` is a Unix timestamp in seconds. Returns modified Rendezvous JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_rendezvous_reschedule(const char *rv_json,
                                          int64_t new_time,
                                          const char *updater);

/**
 * Light a new Lantern beacon.
 *
 * `audience_json` is a JSON `LanternAudience`. `purpose_json` is a JSON
 * `LanternPurpose`. `config_json` may be null (uses default LanternConfig).
 * Returns JSON (LanternShare). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid. `config_json` may be null.
 */
char *divi_physical_lantern_new(const char *person,
                                double lat,
                                double lon,
                                const char *audience_json,
                                const char *purpose_json,
                                const char *config_json);

/**
 * Update a Lantern's position.
 *
 * Returns modified LanternShare JSON.
 *
 * # Safety
 * `lantern_json` must be a valid C string.
 */
char *divi_physical_lantern_update_location(const char *lantern_json, double lat, double lon);

/**
 * Extinguish a Lantern (set expiry to now).
 *
 * Returns modified LanternShare JSON.
 *
 * # Safety
 * `lantern_json` must be a valid C string.
 */
char *divi_physical_lantern_extinguish(const char *lantern_json);

/**
 * Extend a Lantern's lifetime by `seconds`.
 *
 * `config_json` may be null (uses default LanternConfig for max TTL check).
 * Returns modified LanternShare JSON, or null on error (e.g. TTL exceeded).
 *
 * # Safety
 * `lantern_json` must be a valid C string. `config_json` may be null.
 */
char *divi_physical_lantern_extend(const char *lantern_json,
                                   uint64_t seconds,
                                   const char *config_json);

/**
 * Activate an SOS emergency beacon.
 *
 * `contacts_json` is a JSON `Vec<String>` of emergency contact crown IDs.
 * Returns JSON (LanternSos). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_lantern_sos_activate(const char *person,
                                         double lat,
                                         double lon,
                                         const char *contacts_json);

/**
 * Update an SOS beacon's position.
 *
 * Returns modified LanternSos JSON.
 *
 * # Safety
 * `sos_json` must be a valid C string.
 */
char *divi_physical_lantern_sos_update_location(const char *sos_json, double lat, double lon);

/**
 * Resolve (deactivate) an SOS beacon.
 *
 * Returns modified LanternSos JSON.
 *
 * # Safety
 * `sos_json` must be a valid C string.
 */
char *divi_physical_lantern_sos_resolve(const char *sos_json);

/**
 * Create a new Handoff between two parties.
 *
 * `purpose_json` is a JSON `HandoffPurpose` (e.g. `"CashExchange"`).
 * Fails if initiator == counterparty.
 * Returns JSON (Handoff). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_handoff_new(const char *initiator,
                                const char *counterparty,
                                const char *purpose_json);

/**
 * Record proximity verification for a Handoff.
 *
 * `proof_ref_json` is a JSON `ProximityProofRef`.
 * Returns modified Handoff JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_handoff_verify_proximity(const char *handoff_json, const char *proof_ref_json);

/**
 * Record the initiator's signature on a Handoff.
 *
 * Returns modified Handoff JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_handoff_sign_initiator(const char *handoff_json, const char *signature);

/**
 * Record the counterparty's signature on a Handoff.
 *
 * Returns modified Handoff JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_handoff_sign_counterparty(const char *handoff_json, const char *signature);

/**
 * Complete a fully-signed Handoff.
 *
 * Returns modified Handoff JSON.
 *
 * # Safety
 * `handoff_json` must be a valid C string.
 */
char *divi_physical_handoff_complete(const char *handoff_json);

/**
 * Cancel a Handoff. Available from any non-Completed state.
 *
 * Returns modified Handoff JSON.
 *
 * # Safety
 * `handoff_json` must be a valid C string.
 */
char *divi_physical_handoff_cancel(const char *handoff_json);

/**
 * Raise a dispute on a completed Handoff.
 *
 * Returns modified Handoff JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_handoff_dispute(const char *handoff_json, const char *reason);

/**
 * Create a new OmniTag identity (active by default).
 *
 * Returns JSON (OmniTagIdentity). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_tag_new(const char *owner, const char *tag_pubkey);

/**
 * Deactivate an OmniTag.
 *
 * Returns modified OmniTagIdentity JSON.
 *
 * # Safety
 * `tag_json` must be a valid C string.
 */
char *divi_physical_tag_deactivate(const char *tag_json);

/**
 * Activate a previously deactivated OmniTag.
 *
 * Returns modified OmniTagIdentity JSON.
 *
 * # Safety
 * `tag_json` must be a valid C string.
 */
char *divi_physical_tag_activate(const char *tag_json);

/**
 * Create a new Delivery in Created status.
 *
 * Returns JSON (Delivery). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_delivery_new(const char *sender,
                                 const char *recipient,
                                 const char *description);

/**
 * Assign a courier to a Delivery. Only valid from Created status.
 *
 * Returns modified Delivery JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_delivery_assign_courier(const char *delivery_json, const char *courier);

/**
 * Mark a Delivery as picked up. Only valid from CourierAssigned.
 *
 * `handoff_id` is a UUID string for the pickup Handoff.
 * Returns modified Delivery JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_delivery_mark_picked_up(const char *delivery_json, const char *handoff_id);

/**
 * Mark a Delivery as in transit. Only valid from PickedUp.
 *
 * Returns modified Delivery JSON.
 *
 * # Safety
 * `delivery_json` must be a valid C string.
 */
char *divi_physical_delivery_mark_in_transit(const char *delivery_json);

/**
 * Mark a Delivery as delivered. Valid from InTransit or NearDestination.
 *
 * `handoff_id` is a UUID string for the delivery Handoff.
 * Returns modified Delivery JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_delivery_mark_delivered(const char *delivery_json, const char *handoff_id);

/**
 * Confirm a Delivery. Only the recipient, only from Delivered status.
 *
 * Returns modified Delivery JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_delivery_confirm(const char *delivery_json, const char *confirmer);

/**
 * Dispute a Delivery. Valid from Delivered or Confirmed.
 *
 * Returns modified Delivery JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_delivery_dispute(const char *delivery_json, const char *reason);

/**
 * Cancel a Delivery. Valid from any state except Confirmed.
 *
 * Returns modified Delivery JSON.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_physical_delivery_cancel(const char *delivery_json, const char *canceller);

/**
 * Get the immutable rights as JSON.
 *
 * Returns JSON array of `RightCategory` strings. Caller must free via `divi_free_string`.
 */
char *divi_polity_immutable_rights(void);

/**
 * Get the absolute prohibitions as JSON.
 *
 * Returns JSON array of `ProhibitionType` strings. Caller must free via `divi_free_string`.
 */
char *divi_polity_immutable_prohibitions(void);

/**
 * Get the three axioms as JSON.
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 */
char *divi_polity_axioms(void);

/**
 * Check whether a description would violate the immutable foundation.
 *
 * Returns 1 if it would violate, 0 if not, -1 on error.
 *
 * # Safety
 * `description` must be a valid C string.
 */
int32_t divi_polity_would_violate(const char *description);

/**
 * Check whether a right category is immutable.
 *
 * `category_json` is a JSON `RightCategory` string (e.g. `"Dignity"`).
 * Returns 1 if immutable, 0 if not, -1 on error.
 *
 * # Safety
 * `category_json` must be a valid C string.
 */
int32_t divi_polity_is_right_immutable(const char *category_json);

/**
 * Check whether a prohibition type is absolute.
 *
 * `prohibition_json` is a JSON `ProhibitionType` string (e.g. `"Surveillance"`).
 * Returns 1 if absolute, 0 if not, -1 on error.
 *
 * # Safety
 * `prohibition_json` must be a valid C string.
 */
int32_t divi_polity_is_prohibition_absolute(const char *prohibition_json);

/**
 * Create an empty rights registry.
 * Free with `divi_polity_rights_free`.
 */
struct PolityRightsRegistry *divi_polity_rights_new(void);

/**
 * Create a rights registry pre-populated with Covenant rights.
 * Free with `divi_polity_rights_free`.
 */
struct PolityRightsRegistry *divi_polity_rights_new_with_covenant(void);

/**
 * Free a rights registry.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_polity_rights_free(struct PolityRightsRegistry *ptr);

/**
 * Register a new right.
 *
 * `right_json` is a JSON `Right`. Returns UUID string on success,
 * null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `right_json` must be a valid C string.
 */
char *divi_polity_rights_register(const struct PolityRightsRegistry *registry,
                                  const char *right_json);

/**
 * Get a right by ID.
 *
 * Returns JSON `Right`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
char *divi_polity_rights_get(const struct PolityRightsRegistry *registry, const char *id);

/**
 * Get rights by category.
 *
 * `category_json` is a JSON `RightCategory` (e.g. `"Dignity"`).
 * Returns JSON array of `Right`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `category_json` must be a valid C string.
 */
char *divi_polity_rights_by_category(const struct PolityRightsRegistry *registry,
                                     const char *category_json);

/**
 * Find a right by name (case-insensitive).
 *
 * Returns JSON `Right`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `name` must be a valid C string.
 */
char *divi_polity_rights_find_by_name(const struct PolityRightsRegistry *registry,
                                      const char *name);

/**
 * Get all rights in the registry.
 *
 * Returns JSON array of `Right`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_rights_all(const struct PolityRightsRegistry *registry);

/**
 * Get all immutable rights in the registry.
 *
 * Returns JSON array of `Right`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_rights_immutable(const struct PolityRightsRegistry *registry);

/**
 * Remove a right (only if not immutable).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
int32_t divi_polity_rights_remove(const struct PolityRightsRegistry *registry, const char *id);

/**
 * Get the number of rights in the registry.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
int32_t divi_polity_rights_count(const struct PolityRightsRegistry *registry);

/**
 * Create an empty duties registry.
 * Free with `divi_polity_duties_free`.
 */
struct PolityDutiesRegistry *divi_polity_duties_new(void);

/**
 * Create a duties registry pre-populated with Covenant duties.
 * Free with `divi_polity_duties_free`.
 */
struct PolityDutiesRegistry *divi_polity_duties_new_with_covenant(void);

/**
 * Free a duties registry.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_polity_duties_free(struct PolityDutiesRegistry *ptr);

/**
 * Register a new duty.
 *
 * `duty_json` is a JSON `Duty`. Returns UUID string on success,
 * null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `duty_json` must be a valid C string.
 */
char *divi_polity_duties_register(const struct PolityDutiesRegistry *registry,
                                  const char *duty_json);

/**
 * Get a duty by ID.
 *
 * Returns JSON `Duty`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
char *divi_polity_duties_get(const struct PolityDutiesRegistry *registry, const char *id);

/**
 * Get duties by category.
 *
 * `category_json` is a JSON `DutyCategory` (e.g. `"Steward"`).
 * Returns JSON array of `Duty`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `category_json` must be a valid C string.
 */
char *divi_polity_duties_by_category(const struct PolityDutiesRegistry *registry,
                                     const char *category_json);

/**
 * Find a duty by name (case-insensitive).
 *
 * Returns JSON `Duty`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `name` must be a valid C string.
 */
char *divi_polity_duties_find_by_name(const struct PolityDutiesRegistry *registry,
                                      const char *name);

/**
 * Get all duties in the registry.
 *
 * Returns JSON array of `Duty`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_duties_all(const struct PolityDutiesRegistry *registry);

/**
 * Get all absolute duties.
 *
 * Returns JSON array of `Duty`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_duties_absolute(const struct PolityDutiesRegistry *registry);

/**
 * Remove a duty (only if not immutable).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
int32_t divi_polity_duties_remove(const struct PolityDutiesRegistry *registry, const char *id);

/**
 * Get the number of duties in the registry.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
int32_t divi_polity_duties_count(const struct PolityDutiesRegistry *registry);

/**
 * Create an empty protections registry.
 * Free with `divi_polity_protections_free`.
 */
struct PolityProtectionsRegistry *divi_polity_protections_new(void);

/**
 * Create a protections registry pre-populated with Covenant protections.
 * Free with `divi_polity_protections_free`.
 */
struct PolityProtectionsRegistry *divi_polity_protections_new_with_covenant(void);

/**
 * Free a protections registry.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_polity_protections_free(struct PolityProtectionsRegistry *ptr);

/**
 * Register a new protection.
 *
 * `protection_json` is a JSON `Protection`. Returns UUID string on success,
 * null on error. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `protection_json` must be a valid C string.
 */
char *divi_polity_protections_register(const struct PolityProtectionsRegistry *registry,
                                       const char *protection_json);

/**
 * Get a protection by ID.
 *
 * Returns JSON `Protection`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
char *divi_polity_protections_get(const struct PolityProtectionsRegistry *registry, const char *id);

/**
 * Get protections by prohibition type.
 *
 * `type_json` is a JSON `ProhibitionType` (e.g. `"Surveillance"`).
 * Returns JSON array of `Protection`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `type_json` must be a valid C string.
 */
char *divi_polity_protections_by_type(const struct PolityProtectionsRegistry *registry,
                                      const char *type_json);

/**
 * Find a protection by name (case-insensitive).
 *
 * Returns JSON `Protection`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `name` must be a valid C string.
 */
char *divi_polity_protections_find_by_name(const struct PolityProtectionsRegistry *registry,
                                           const char *name);

/**
 * Get all protections in the registry.
 *
 * Returns JSON array of `Protection`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_protections_all(const struct PolityProtectionsRegistry *registry);

/**
 * Get all absolute protections.
 *
 * Returns JSON array of `Protection`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_protections_absolute(const struct PolityProtectionsRegistry *registry);

/**
 * Check whether an action violates any protection.
 *
 * `action_json` is a JSON `ActionDescription`.
 * Returns JSON array of `Protection` that are violated. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `action_json` must be a valid C string.
 */
char *divi_polity_protections_check_violation(const struct PolityProtectionsRegistry *registry,
                                              const char *action_json);

/**
 * Remove a protection (only if not immutable).
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
int32_t divi_polity_protections_remove(const struct PolityProtectionsRegistry *registry,
                                       const char *id);

/**
 * Get the number of protections in the registry.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
int32_t divi_polity_protections_count(const struct PolityProtectionsRegistry *registry);

/**
 * Review an action against the Covenant.
 *
 * Takes pointers to both registries and an `ActionDescription` JSON.
 * Returns JSON `ConstitutionalReview`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `rights` and `protections` must be valid pointers. `action_json` must be a valid C string.
 */
char *divi_polity_review(const struct PolityRightsRegistry *rights,
                         const struct PolityProtectionsRegistry *protections,
                         const char *action_json);

/**
 * Quick check: does an action violate any absolute prohibition?
 *
 * Returns 1 if absolutely prohibited, 0 if not, -1 on error.
 *
 * # Safety
 * `rights` and `protections` must be valid pointers. `action_json` must be a valid C string.
 */
int32_t divi_polity_is_absolutely_prohibited(const struct PolityRightsRegistry *rights,
                                             const struct PolityProtectionsRegistry *protections,
                                             const char *action_json);

/**
 * Convert a review breach to a formal Breach record.
 *
 * `review_json` is a JSON `ConstitutionalReview`. Returns JSON `Breach` if the review
 * contained a breach, or null if the review was clean. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `rights` and `protections` must be valid pointers. `review_json` must be a valid C string.
 */
char *divi_polity_review_to_breach(const struct PolityRightsRegistry *rights,
                                   const struct PolityProtectionsRegistry *protections,
                                   const char *review_json);

/**
 * Create a new breach registry.
 * Free with `divi_polity_breach_free`.
 */
struct PolityBreachRegistry *divi_polity_breach_new(void);

/**
 * Free a breach registry.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_polity_breach_free(struct PolityBreachRegistry *ptr);

/**
 * Record a breach.
 *
 * `breach_json` is a JSON `Breach`. Returns UUID string.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `breach_json` must be a valid C string.
 */
char *divi_polity_breach_record(const struct PolityBreachRegistry *registry,
                                const char *breach_json);

/**
 * Get a breach by ID.
 *
 * Returns JSON `Breach`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
char *divi_polity_breach_get(const struct PolityBreachRegistry *registry, const char *id);

/**
 * Update a breach's status.
 *
 * `status_json` is a JSON `BreachStatus` (e.g. `"Investigating"`).
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. C strings must be valid.
 */
int32_t divi_polity_breach_update_status(const struct PolityBreachRegistry *registry,
                                         const char *id,
                                         const char *status_json);

/**
 * Get breaches by actor.
 *
 * Returns JSON array of `Breach`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `actor` must be a valid C string.
 */
char *divi_polity_breach_by_actor(const struct PolityBreachRegistry *registry, const char *actor);

/**
 * Get breaches by severity.
 *
 * `severity_json` is a JSON `BreachSeverity` (e.g. `"Grave"`).
 * Returns JSON array of `Breach`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `severity_json` must be a valid C string.
 */
char *divi_polity_breach_by_severity(const struct PolityBreachRegistry *registry,
                                     const char *severity_json);

/**
 * Get all active breaches (not resolved or dismissed).
 *
 * Returns JSON array of `Breach`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_breach_active(const struct PolityBreachRegistry *registry);

/**
 * Get all foundational breaches (involving immutable foundations).
 *
 * Returns JSON array of `Breach`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_breach_foundational(const struct PolityBreachRegistry *registry);

/**
 * Get the number of breaches in the registry.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
int32_t divi_polity_breach_count(const struct PolityBreachRegistry *registry);

/**
 * Create a new amendment.
 *
 * `trigger_json` is a JSON `AmendmentTrigger` (e.g. `"Contradiction"`).
 * Returns JSON `Amendment` on success, null if the description would violate
 * the immutable foundation. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_polity_amendment_new(const char *trigger_json,
                                const char *title,
                                const char *description,
                                const char *proposer);

/**
 * Begin deliberation on an amendment.
 *
 * Takes amendment JSON, returns modified amendment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `amendment_json` must be a valid C string.
 */
char *divi_polity_amendment_begin_deliberation(const char *amendment_json);

/**
 * Begin ratification on an amendment.
 *
 * Takes amendment JSON, returns modified amendment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `amendment_json` must be a valid C string.
 */
char *divi_polity_amendment_begin_ratification(const char *amendment_json);

/**
 * Add support from a participant.
 *
 * Takes amendment JSON + supporter, returns modified amendment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_polity_amendment_add_support(const char *amendment_json, const char *supporter);

/**
 * Add objection from a participant.
 *
 * Takes amendment JSON + objector, returns modified amendment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_polity_amendment_add_objection(const char *amendment_json, const char *objector);

/**
 * Update the support ratio.
 *
 * Takes amendment JSON + ratio (0.0 to 1.0), returns modified amendment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `amendment_json` must be a valid C string.
 */
char *divi_polity_amendment_update_support(const char *amendment_json, double ratio);

/**
 * Attempt to enact an amendment.
 *
 * Takes amendment JSON, returns modified amendment JSON.
 * Returns null if threshold not met or invalid transition.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `amendment_json` must be a valid C string.
 */
char *divi_polity_amendment_enact(const char *amendment_json);

/**
 * Reject an amendment.
 *
 * Takes amendment JSON, returns modified amendment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `amendment_json` must be a valid C string.
 */
char *divi_polity_amendment_reject(const char *amendment_json);

/**
 * Nullify an amendment (contradicts immutable foundations).
 *
 * Takes amendment JSON, returns modified amendment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `amendment_json` must be a valid C string.
 */
char *divi_polity_amendment_nullify(const char *amendment_json);

/**
 * Create a new enactment.
 *
 * `enactor_type_json` is a JSON `EnactorType` (e.g. `"Person"`).
 * Returns JSON `Enactment`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_polity_enactment_new(const char *enactor,
                                const char *enactor_type_json,
                                const char *affirmation);

/**
 * Suspend an enactment.
 *
 * Takes enactment JSON, returns modified enactment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `enactment_json` must be a valid C string.
 */
char *divi_polity_enactment_suspend(const char *enactment_json);

/**
 * Reactivate a suspended enactment.
 *
 * Takes enactment JSON, returns modified enactment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `enactment_json` must be a valid C string.
 */
char *divi_polity_enactment_reactivate(const char *enactment_json);

/**
 * Withdraw from the Covenant.
 *
 * Takes enactment JSON, returns modified enactment JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `enactment_json` must be a valid C string.
 */
char *divi_polity_enactment_withdraw(const char *enactment_json);

/**
 * Create a new enactment registry.
 * Free with `divi_polity_enactment_registry_free`.
 */
struct PolityEnactmentRegistry *divi_polity_enactment_registry_new(void);

/**
 * Free an enactment registry.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_polity_enactment_registry_free(struct PolityEnactmentRegistry *ptr);

/**
 * Record an enactment in the registry.
 *
 * `enactment_json` is a JSON `Enactment`. Returns UUID string on success,
 * null on error (e.g. duplicate active enactment). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `enactment_json` must be a valid C string.
 */
char *divi_polity_enactment_registry_record(const struct PolityEnactmentRegistry *registry,
                                            const char *enactment_json);

/**
 * Get an enactment by ID.
 *
 * Returns JSON `Enactment`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
char *divi_polity_enactment_registry_get(const struct PolityEnactmentRegistry *registry,
                                         const char *id);

/**
 * Check whether an enactor has an active enactment.
 *
 * Returns 1 if enacted, 0 if not, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. `enactor` must be a valid C string.
 */
int32_t divi_polity_enactment_registry_is_enacted(const struct PolityEnactmentRegistry *registry,
                                                  const char *enactor);

/**
 * Get all active enactments.
 *
 * Returns JSON array of `Enactment`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_enactment_registry_active(const struct PolityEnactmentRegistry *registry);

/**
 * Get the number of enactments in the registry.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
int32_t divi_polity_enactment_registry_count(const struct PolityEnactmentRegistry *registry);

/**
 * Get the default oath of enactment.
 *
 * Returns a C string. Caller must free via `divi_free_string`.
 */
char *divi_polity_default_oath(void);

/**
 * Create a new consent record.
 *
 * `scope_json` is a JSON `ConsentScope`.
 * Returns JSON `ConsentRecord`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_polity_consent_new(const char *grantor, const char *recipient, const char *scope_json);

/**
 * Revoke a consent record.
 *
 * Takes consent JSON + reason, returns modified consent JSON.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_polity_consent_revoke(const char *consent_json, const char *reason);

/**
 * Create a new consent registry.
 * Free with `divi_polity_consent_registry_free`.
 */
struct PolityConsentRegistry *divi_polity_consent_registry_new(void);

/**
 * Free a consent registry.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_polity_consent_registry_free(struct PolityConsentRegistry *ptr);

/**
 * Record a consent in the registry.
 *
 * `consent_json` is a JSON `ConsentRecord`. Returns UUID string.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `consent_json` must be a valid C string.
 */
char *divi_polity_consent_registry_record(const struct PolityConsentRegistry *registry,
                                          const char *consent_json);

/**
 * Get a consent record by ID.
 *
 * Returns JSON `ConsentRecord`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `id` must be a valid UUID C string.
 */
char *divi_polity_consent_registry_get(const struct PolityConsentRegistry *registry,
                                       const char *id);

/**
 * Get all consent records by grantor.
 *
 * Returns JSON array of `ConsentRecord`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. `grantor` must be a valid C string.
 */
char *divi_polity_consent_registry_by_grantor(const struct PolityConsentRegistry *registry,
                                              const char *grantor);

/**
 * Revoke a consent record in the registry.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `registry` must be a valid pointer. C strings must be valid.
 */
int32_t divi_polity_consent_registry_revoke(const struct PolityConsentRegistry *registry,
                                            const char *id,
                                            const char *reason);

/**
 * Get all active consent records.
 *
 * Returns JSON array of `ConsentRecord`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
char *divi_polity_consent_registry_active(const struct PolityConsentRegistry *registry);

/**
 * Validate consent for a given scope.
 *
 * `scope_json` is a JSON `ConsentScope`.
 * Returns JSON `ConsentValidation`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `registry` must be a valid pointer. All C strings must be valid.
 */
char *divi_polity_consent_registry_validate(const struct PolityConsentRegistry *registry,
                                            const char *grantor,
                                            const char *recipient,
                                            const char *scope_json);

/**
 * Get the number of consent records in the registry.
 *
 * # Safety
 * `registry` must be a valid pointer.
 */
int32_t divi_polity_consent_registry_count(const struct PolityConsentRegistry *registry);

/**
 * Create a new Phone. Returns an opaque pointer.
 * Free with `divi_phone_free`.
 */
Phone *divi_phone_new(void);

/**
 * Free a Phone created by `divi_phone_new`.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_phone_new`, called exactly once.
 */
void divi_phone_free(Phone *ptr);

/**
 * Register a raw handler for a call ID.
 *
 * The `handler` function will be called when `divi_phone_call_raw` is invoked
 * with the matching `call_id`. The handler receives request bytes and must
 * write response bytes into the out-parameters.
 *
 * The `context` pointer is passed through to every handler invocation.
 * Pass null if no context is needed.
 *
 * # Safety
 * `phone` must be a valid pointer from `divi_phone_new`.
 * `call_id` must be a valid null-terminated C string.
 * `context` must be valid for the lifetime of the registration, or null.
 */
void divi_phone_register_raw(const Phone *phone,
                             const char *call_id,
                             DiviPhoneHandler handler,
                             void *context);

/**
 * Make a raw call. Returns 0 on success, -1 on error.
 *
 * On success, `*out_data` and `*out_len` are set to the response bytes.
 * The caller must free `*out_data` via `divi_free_bytes(*out_data, *out_len)`.
 *
 * On error, call `divi_last_error()` for details.
 *
 * # Safety
 * `phone` must be a valid pointer from `divi_phone_new`.
 * `call_id` must be a valid null-terminated C string.
 * `data` must point to `data_len` valid bytes (or be null if `data_len` is 0).
 * `out_data` and `out_len` must be valid writable pointers.
 */
int32_t divi_phone_call_raw(const Phone *phone,
                            const char *call_id,
                            const uint8_t *data,
                            uintptr_t data_len,
                            uint8_t **out_data,
                            uintptr_t *out_len);

/**
 * Make a raw call, returning 1 if no handler is registered (not an error).
 *
 * Returns: 0 = success, 1 = no handler (not an error), -1 = error.
 *
 * # Safety
 * Same as `divi_phone_call_raw`.
 */
int32_t divi_phone_call_raw_if_available(const Phone *phone,
                                         const char *call_id,
                                         const uint8_t *data,
                                         uintptr_t data_len,
                                         uint8_t **out_data,
                                         uintptr_t *out_len);

/**
 * Check if a handler is registered for a call ID.
 *
 * # Safety
 * `phone` must be a valid pointer. `call_id` must be a valid C string.
 */
bool divi_phone_has_handler(const Phone *phone, const char *call_id);

/**
 * Get all registered call IDs as a JSON array string.
 *
 * Returns a `*mut c_char` that must be freed via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * `phone` must be a valid pointer.
 */
char *divi_phone_registered_call_ids(const Phone *phone);

/**
 * Unregister a handler by call ID.
 *
 * # Safety
 * `phone` must be a valid pointer. `call_id` must be a valid C string.
 */
void divi_phone_unregister(const Phone *phone, const char *call_id);

/**
 * Run the full Pulse demo. Returns a JSON report (caller must free via `divi_free_string`).
 */
char *divi_pulse_run_demo(void);

/**
 * Create a new Quest engine.
 *
 * `config_json` is a JSON `QuestConfig` (or null for default).
 * Free with `divi_quest_engine_free`.
 *
 * # Safety
 * `config_json` may be null (uses default config).
 */
struct QuestEngineFFI *divi_quest_engine_new(const char *config_json);

/**
 * Free a Quest engine.
 *
 * # Safety
 * `ptr` must be valid and called exactly once, or null (no-op).
 */
void divi_quest_engine_free(struct QuestEngineFFI *ptr);

/**
 * Award XP to an actor. Returns the new level, or 0 on error.
 *
 * # Safety
 * `engine` must be a valid pointer. `actor` and `source` must be valid C strings.
 */
uint32_t divi_quest_award_xp(const struct QuestEngineFFI *engine,
                             const char *actor,
                             uint64_t amount,
                             const char *source);

/**
 * Get an actor's progression as JSON. Returns null if the actor has no progression.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `engine` and `actor` must be valid pointers.
 */
char *divi_quest_progression(const struct QuestEngineFFI *engine, const char *actor);

/**
 * Get an actor's quest status as JSON.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `engine` and `actor` must be valid pointers.
 */
char *divi_quest_status(const struct QuestEngineFFI *engine, const char *actor);

/**
 * Register a counter-based criteria for achievement evaluation.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointer args must be valid C strings or null-checked.
 */
int32_t divi_quest_register_criteria_counter(const struct QuestEngineFFI *engine,
                                             const char *id,
                                             const char *counter_key,
                                             uint64_t target);

/**
 * Register a flag-based criteria for achievement evaluation.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointer args must be valid C strings.
 */
int32_t divi_quest_register_criteria_flag(const struct QuestEngineFFI *engine,
                                          const char *id,
                                          const char *flag_key,
                                          bool expected);

/**
 * Register an achievement definition from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `engine` must be valid. `achievement_json` must be a valid C string.
 */
int32_t divi_quest_register_achievement(const struct QuestEngineFFI *engine,
                                        const char *achievement_json);

/**
 * Evaluate all achievements for an actor given a context. Returns JSON array of
 * newly-achieved achievement IDs.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * All pointer args must be valid.
 */
char *divi_quest_evaluate_achievements(const struct QuestEngineFFI *engine,
                                       const char *actor,
                                       const char *context_json);

/**
 * Add a mission definition from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `engine` must be valid. `mission_json` must be a valid C string.
 */
int32_t divi_quest_add_mission(const struct QuestEngineFFI *engine, const char *mission_json);

/**
 * Start a mission for an actor.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointer args must be valid C strings.
 */
int32_t divi_quest_start_mission(const struct QuestEngineFFI *engine,
                                 const char *actor,
                                 const char *mission_id);

/**
 * Update progress on a specific objective within a mission.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointer args must be valid C strings.
 */
int32_t divi_quest_update_objective(const struct QuestEngineFFI *engine,
                                    const char *actor,
                                    const char *mission_id,
                                    const char *objective_id,
                                    uint64_t increment);

/**
 * Get active missions for an actor as JSON array of `MissionProgress`.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `engine` and `actor` must be valid.
 */
char *divi_quest_active_missions(const struct QuestEngineFFI *engine, const char *actor);

/**
 * Get available missions for an actor as JSON array of `Mission`.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `engine` and `actor` must be valid.
 */
char *divi_quest_available_missions(const struct QuestEngineFFI *engine, const char *actor);

/**
 * Add a challenge definition from JSON.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `engine` must be valid. `challenge_json` must be a valid C string.
 */
int32_t divi_quest_add_challenge(const struct QuestEngineFFI *engine, const char *challenge_json);

/**
 * Join a challenge. `participant_json` is a JSON `ChallengeParticipant`.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointer args must be valid C strings.
 */
int32_t divi_quest_join_challenge(const struct QuestEngineFFI *engine,
                                  const char *challenge_id,
                                  const char *participant_json);

/**
 * Get challenge progress as JSON (entries for a challenge).
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * All pointer args must be valid.
 */
char *divi_quest_challenge_progress(const struct QuestEngineFFI *engine, const char *challenge_id);

/**
 * Grant a reward to an actor.
 *
 * `reward_json` is a JSON `RewardType`. `source_type_json` is a JSON `RewardSource`.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointer args must be valid C strings.
 */
int32_t divi_quest_grant_reward(const struct QuestEngineFFI *engine,
                                const char *actor,
                                const char *reward_json,
                                const char *source_id,
                                const char *source_type_json);

/**
 * Get all rewards for an actor as JSON array.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `engine` and `actor` must be valid.
 */
char *divi_quest_rewards_for(const struct QuestEngineFFI *engine, const char *actor);

/**
 * Get total Cool currency earned by an actor.
 *
 * Returns 0 if the actor has no rewards or on error.
 *
 * # Safety
 * `engine` and `actor` must be valid.
 */
uint64_t divi_quest_total_cool(const struct QuestEngineFFI *engine, const char *actor);

/**
 * Get the engine's aggregate summary as JSON.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `engine` must be valid.
 */
char *divi_quest_summary(const struct QuestEngineFFI *engine);

/**
 * Get the engine's current configuration as JSON.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `engine` must be valid.
 */
char *divi_quest_engine_config(const struct QuestEngineFFI *engine);

/**
 * Get the default Reign theme as JSON.
 *
 * Returns a JSON string (caller must free via `divi_free_string`),
 * or null on error (check `divi_last_error`).
 */
char *divi_regalia_default_reign(void);

/**
 * Resolve the Crest (color palette) for a given aspect ("light" or "dark").
 *
 * `reign_json` — JSON-encoded Reign. If null, uses default.
 * `aspect` — "light", "dark", or a custom string.
 *
 * Returns a JSON-encoded Crest, or null on error.
 */
char *divi_regalia_resolve_crest(const char *reign_json, const char *aspect);

/**
 * Run the Arbiter layout solver.
 *
 * `x`, `y`, `w`, `h` — viewport bounds.
 * `sanctums_json` — JSON array of Sanctum objects.
 *
 * Returns a JSON-encoded Domain (resolved layout), or null on error.
 */
char *divi_regalia_resolve_layout(double x,
                                  double y,
                                  double w,
                                  double h,
                                  const char *sanctums_json);

/**
 * Get the Pulse dashboard sanctum definitions as JSON.
 *
 * Returns a JSON array of Sanctum objects (toolbar + sidebar + content).
 * Caller must free via `divi_free_string`.
 */
char *divi_regalia_pulse_sanctums(void);

/**
 * Create a new theme collection with an initial Reign.
 *
 * `reign_json` — JSON-encoded Reign. If null, uses `Reign::default()`.
 * Free with `divi_regalia_theme_collection_free`.
 *
 * # Safety
 * `reign_json` may be null (uses default).
 */
struct RegaliaThemeCollection *divi_regalia_theme_collection_new(const char *reign_json);

/**
 * Free a theme collection.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_regalia_theme_collection_free(struct RegaliaThemeCollection *ptr);

/**
 * Get the active Reign as JSON.
 *
 * Returns JSON (Reign). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tc` must be a valid pointer.
 */
char *divi_regalia_theme_collection_active(const struct RegaliaThemeCollection *tc);

/**
 * Switch to a different theme by name.
 *
 * Returns 0 on success, -1 on error (theme not found).
 *
 * # Safety
 * `tc` must be a valid pointer. `name` must be a valid C string.
 */
int32_t divi_regalia_theme_collection_switch(const struct RegaliaThemeCollection *tc,
                                             const char *name);

/**
 * Add a new theme to the collection.
 *
 * `reign_json` — JSON-encoded Reign.
 * Returns 0 on success, -1 on error (duplicate name).
 *
 * # Safety
 * `tc` must be a valid pointer. `reign_json` must be a valid C string.
 */
int32_t divi_regalia_theme_collection_add(const struct RegaliaThemeCollection *tc,
                                          const char *reign_json);

/**
 * Remove a theme by name.
 *
 * Returns JSON (the removed Reign). Caller must free via `divi_free_string`.
 * Returns null on error (cannot remove active, last, or nonexistent).
 *
 * # Safety
 * `tc` must be a valid pointer. `name` must be a valid C string.
 */
char *divi_regalia_theme_collection_remove(const struct RegaliaThemeCollection *tc,
                                           const char *name);

/**
 * List all theme names in the collection.
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tc` must be a valid pointer.
 */
char *divi_regalia_theme_collection_list(const struct RegaliaThemeCollection *tc);

/**
 * Look up a ComponentStyle preset by name.
 *
 * Supported names: "primary_button", "card", "input_field", "text_body".
 * Returns JSON (ComponentStyle). Caller must free via `divi_free_string`.
 * Returns null if the name is unknown.
 *
 * # Safety
 * `name` must be a valid C string.
 */
char *divi_regalia_component_style_preset(const char *name);

/**
 * Look up a Gradient preset by name.
 *
 * Supported names: "sunset", "ocean", "forest".
 * Returns JSON (Gradient). Caller must free via `divi_free_string`.
 * Returns null if the name is unknown.
 *
 * # Safety
 * `name` must be a valid C string.
 */
char *divi_regalia_gradient_preset(const char *name);

/**
 * Interpolate the color at a given position along a gradient.
 *
 * `gradient_json` — JSON-encoded Gradient.
 * `position` — 0.0 to 1.0 along the gradient.
 *
 * Returns JSON (Ember). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * `gradient_json` must be a valid C string.
 */
char *divi_regalia_gradient_color_at(const char *gradient_json, double position);

/**
 * Evaluate a Surge animation curve at time `t`.
 *
 * Supported preset names: "snap", "smooth", "bouncy".
 * Returns the curve value at `t` (typically 0.0 at start, 1.0 at end).
 * Returns NaN on error (unknown preset or invalid name).
 *
 * # Safety
 * `preset_name` must be a valid C string.
 */
double divi_regalia_surge_evaluate(const char *preset_name, double t);

/**
 * Look up a FacetStyle preset by name.
 *
 * Supported names: "regular", "clear", "subtle", "frosted".
 * Returns JSON (FacetStyle). Caller must free via `divi_free_string`.
 * Returns null if the name is unknown.
 *
 * # Safety
 * `name` must be a valid C string.
 */
char *divi_regalia_facet_style_preset(const char *name);

/**
 * Apply a delta to a FacetStyle, returning the modified style.
 *
 * `style_json` — JSON-encoded FacetStyle.
 * `delta_json` — JSON-encoded FacetStyleDelta.
 *
 * Returns JSON (FacetStyle). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * Both parameters must be valid C strings.
 */
char *divi_regalia_facet_style_apply_delta(const char *style_json, const char *delta_json);

/**
 * Compute the WCAG contrast ratio between two sRGB colors.
 *
 * All channel values are in the 0.0–1.0 range.
 * Returns a ratio >= 1.0 (e.g. 21.0 for black vs white).
 */
double divi_regalia_contrast_ratio(double r1,
                                   double g1,
                                   double b1,
                                   double r2,
                                   double g2,
                                   double b2);

/**
 * Get the default Aura (design token container) as JSON.
 *
 * Returns JSON (Aura). Caller must free via `divi_free_string`.
 */
char *divi_regalia_default_aura(void);

/**
 * Create a new multi-threaded async runtime.
 * Free with `divi_runtime_free`.
 */
struct DiviRuntime *divi_runtime_new(void);

/**
 * Free the async runtime.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_runtime_new`, called exactly once.
 * All pools/handles using this runtime should be freed first.
 */
void divi_runtime_free(struct DiviRuntime *ptr);

/**
 * Encrypt plaintext with a 32-byte key.
 *
 * Returns combined format bytes (nonce || ciphertext || tag).
 * Output written to `out_data`/`out_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointers must be valid. `key` must be 32 bytes.
 */
int32_t divi_sentinal_encrypt(const uint8_t *plaintext,
                              uintptr_t plaintext_len,
                              const uint8_t *key,
                              uintptr_t key_len,
                              uint8_t **out_data,
                              uintptr_t *out_len);

/**
 * Decrypt combined-format ciphertext with a 32-byte key.
 *
 * Output written to `out_data`/`out_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * All pointers must be valid. `key` must be 32 bytes.
 */
int32_t divi_sentinal_decrypt(const uint8_t *ciphertext,
                              uintptr_t ciphertext_len,
                              const uint8_t *key,
                              uintptr_t key_len,
                              uint8_t **out_data,
                              uintptr_t *out_len);

/**
 * Derive a master key from a password using PBKDF2 (600K iterations).
 *
 * If `salt` is null, generates a random 32-byte salt.
 * Outputs: key bytes (`out_key`/`out_key_len`) and salt bytes (`out_salt`/`out_salt_len`).
 * Caller must free both via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `password` must be a valid C string. Output pointers must be valid.
 */
int32_t divi_sentinal_derive_master_key(const char *password,
                                        const uint8_t *salt,
                                        uintptr_t salt_len,
                                        uint8_t **out_key,
                                        uintptr_t *out_key_len,
                                        uint8_t **out_salt,
                                        uintptr_t *out_salt_len);

/**
 * Derive a content key from a master key and a UUID string.
 *
 * Output written to `out_key`/`out_key_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `master_key` must be 32 bytes. `id` must be a valid UUID C string.
 */
int32_t divi_sentinal_derive_content_key(const uint8_t *master_key,
                                         uintptr_t master_key_len,
                                         const char *id,
                                         uint8_t **out_key,
                                         uintptr_t *out_key_len);

/**
 * Derive a shared key from an ECDH shared secret.
 *
 * Output written to `out_key`/`out_key_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `shared_secret` must be valid for `secret_len` bytes.
 */
int32_t divi_sentinal_derive_shared_key(const uint8_t *shared_secret,
                                        uintptr_t secret_len,
                                        uint8_t **out_key,
                                        uintptr_t *out_key_len);

/**
 * Generate a random salt of the given length.
 *
 * Output written to `out_data`/`out_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * Output pointers must be valid.
 */
int32_t divi_sentinal_generate_salt(uintptr_t length, uint8_t **out_data, uintptr_t *out_len);

/**
 * Derive a 32-byte soul encryption key from a master key using HKDF-SHA256.
 *
 * Uses domain salt `"omnidea-soul-v1"` with info `"soul-data"`.
 * The resulting key is used with `divi_crown_soul_create_encrypted` and
 * `divi_crown_soul_load_encrypted` to encrypt soul.json at rest.
 *
 * Output written to `out_key`/`out_key_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `master_key` must be valid for `master_key_len` bytes. Output pointers must be valid.
 */
int32_t divi_sentinal_derive_soul_key(const uint8_t *master_key,
                                      uintptr_t master_key_len,
                                      uint8_t **out_key,
                                      uintptr_t *out_key_len);

/**
 * Create a password-protected key slot wrapping a content key.
 *
 * Returns JSON (KeySlot). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `content_key` must be 32 bytes. `password` must be a valid C string.
 */
char *divi_sentinal_key_slot_create_password(const uint8_t *content_key,
                                             uintptr_t content_key_len,
                                             const char *password);

/**
 * Create a public-key key slot (X25519 ECDH) wrapping a content key.
 *
 * Returns JSON (KeySlot). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `content_key` must be 32 bytes. `recipient_pubkey` must be 32 bytes.
 * `recipient_crown_id` must be a valid C string.
 */
char *divi_sentinal_key_slot_create_public(const uint8_t *content_key,
                                           uintptr_t content_key_len,
                                           const uint8_t *recipient_pubkey,
                                           const char *recipient_crown_id);

/**
 * Unwrap a key slot with a password credential.
 *
 * `slot_json` is a serialized KeySlot. Output is the unwrapped content key bytes.
 * Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `slot_json` and `password` must be valid C strings.
 */
int32_t divi_sentinal_key_slot_unwrap_password(const char *slot_json,
                                               const char *password,
                                               uint8_t **out_key,
                                               uintptr_t *out_key_len);

/**
 * Unwrap a key slot with a private key (X25519, 32 bytes).
 *
 * Output is the unwrapped content key bytes. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `slot_json` must be a valid C string. `private_key` must be 32 bytes.
 */
int32_t divi_sentinal_key_slot_unwrap_private(const char *slot_json,
                                              const uint8_t *private_key,
                                              uint8_t **out_key,
                                              uintptr_t *out_key_len);

/**
 * Wrap one layer of onion encryption addressed to a relay's public key.
 *
 * `relay_pubkey` must be a 32-byte X25519 public key.
 * Returns encrypted blob (ephemeral_pubkey || encrypted_combined).
 * Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * `plaintext` must be valid for `pt_len` bytes. `relay_pubkey` must be 32 bytes.
 * `out_len` must be a valid pointer.
 */
uint8_t *divi_sentinal_onion_wrap(const uint8_t *plaintext,
                                  uintptr_t pt_len,
                                  const uint8_t *relay_pubkey,
                                  uintptr_t key_len,
                                  uintptr_t *out_len);

/**
 * Unwrap one layer of onion encryption using the relay's private key.
 *
 * `relay_privkey` must be a 32-byte X25519 private key.
 * Returns the inner plaintext (which may be another onion layer).
 * Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * `ciphertext` must be valid for `ct_len` bytes. `relay_privkey` must be 32 bytes.
 * `out_len` must be a valid pointer.
 */
uint8_t *divi_sentinal_onion_unwrap(const uint8_t *ciphertext,
                                    uintptr_t ct_len,
                                    const uint8_t *relay_privkey,
                                    uintptr_t key_len,
                                    uintptr_t *out_len);

/**
 * Encrypt plaintext with AES-256-GCM and additional authenticated data (AAD).
 *
 * AAD is authenticated but not encrypted. Returns combined format bytes.
 * Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * All pointers must be valid for their respective lengths. `key` must be 32 bytes.
 * `out_len` must be a valid pointer.
 */
uint8_t *divi_sentinal_encrypt_with_aad(const uint8_t *plaintext,
                                        uintptr_t pt_len,
                                        const uint8_t *aad,
                                        uintptr_t aad_len,
                                        const uint8_t *key,
                                        uintptr_t key_len,
                                        uintptr_t *out_len);

/**
 * Decrypt combined-format ciphertext with AAD.
 *
 * The `aad` must exactly match what was provided during encryption.
 * Returns decrypted plaintext bytes.
 * Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * All pointers must be valid for their respective lengths. `key` must be 32 bytes.
 * `out_len` must be a valid pointer.
 */
uint8_t *divi_sentinal_decrypt_with_aad(const uint8_t *combined,
                                        uintptr_t combined_len,
                                        const uint8_t *aad,
                                        uintptr_t aad_len,
                                        const uint8_t *key,
                                        uintptr_t key_len,
                                        uintptr_t *out_len);

/**
 * Pad data to a multiple of `block_size` using PKCS#7 padding.
 *
 * Returns padded bytes. Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * `data` must be valid for `data_len` bytes. `out_len` must be a valid pointer.
 */
uint8_t *divi_sentinal_pad(const uint8_t *data,
                           uintptr_t data_len,
                           uintptr_t block_size,
                           uintptr_t *out_len);

/**
 * Remove PKCS#7 padding and recover original data.
 *
 * Returns unpadded bytes. Caller must free via `divi_free_bytes(ptr, *out_len)`.
 * Returns null on error.
 *
 * # Safety
 * `data` must be valid for `data_len` bytes. `out_len` must be a valid pointer.
 */
uint8_t *divi_sentinal_unpad(const uint8_t *data, uintptr_t data_len, uintptr_t *out_len);

/**
 * Generate a random 24-word recovery phrase.
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 * Returns null on error.
 */
char *divi_sentinal_recovery_generate(void);

/**
 * Validate a recovery phrase (JSON array of strings).
 *
 * Returns true if valid, false otherwise.
 *
 * # Safety
 * `phrase_json` must be a valid C string.
 */
bool divi_sentinal_recovery_validate(const char *phrase_json);

/**
 * Convert a recovery phrase to a 64-byte seed.
 *
 * Output written to `out_seed`/`out_seed_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `phrase_json` and `passphrase` must be valid C strings.
 */
int32_t divi_sentinal_recovery_to_seed(const char *phrase_json,
                                       const char *passphrase,
                                       uint8_t **out_seed,
                                       uintptr_t *out_seed_len);

/**
 * Derive a 32-byte identity key from a BIP-39 seed using HKDF-SHA256.
 *
 * Takes a 64-byte seed (from `divi_sentinal_recovery_to_seed`) and produces
 * a 32-byte key suitable for use as a secp256k1 private key. Uses domain
 * salt `"omnidea-identity-v1"` with info `"identity-primary"`.
 *
 * Output written to `out_key`/`out_key_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `seed` must be valid for `seed_len` bytes (must be >= 32 bytes, typically 64).
 */
int32_t divi_sentinal_derive_identity_key(const uint8_t *seed,
                                          uintptr_t seed_len,
                                          uint8_t **out_key,
                                          uintptr_t *out_key_len);

/**
 * Estimate password strength. Returns JSON `PasswordStrength` or null on error.
 *
 * Caller must free the returned string via `divi_free_string`.
 *
 * # Safety
 * `password` must be a valid null-terminated C string.
 */
char *divi_sentinal_password_strength(const char *password);

/**
 * Start a relay server.
 *
 * `runtime` must be a valid DiviRuntime pointer.
 * `port` is the TCP port to bind. Use 0 for OS-assigned.
 * `bind_all` — if true, binds to 0.0.0.0 (reachable from LAN). If false, 127.0.0.1 only.
 * `config_json` is optional server configuration (null for defaults).
 *
 * Returns a server pointer. Free with `divi_globe_server_free`.
 *
 * # Safety
 * `runtime` must be a valid pointer from `divi_runtime_new`.
 */
struct GlobeServer *divi_globe_server_start(const struct DiviRuntime *runtime,
                                            uint16_t port,
                                            bool bind_all,
                                            const char *config_json);

/**
 * Get the port the server is listening on.
 *
 * # Safety
 * `server` must be a valid pointer from `divi_globe_server_start`.
 */
uint16_t divi_globe_server_port(const struct GlobeServer *server);

/**
 * Get the number of active connections.
 *
 * # Safety
 * `server` must be a valid pointer from `divi_globe_server_start`.
 */
uint32_t divi_globe_server_connections(const struct GlobeServer *server);

/**
 * Inject an event directly into the server's event store.
 *
 * Bypasses WebSocket — useful for seeding test data or bootstrapping.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `server` must be a valid pointer. `event_json` must be a valid C string.
 */
int32_t divi_globe_server_seed_event(const struct GlobeServer *server, const char *event_json);

/**
 * Get the server's WebSocket URL (e.g., "ws://0.0.0.0:8080").
 *
 * Returns a C string. Free via `divi_free_string`.
 *
 * # Safety
 * `server` must be a valid pointer.
 */
char *divi_globe_server_url(const struct GlobeServer *server);

/**
 * Free a relay server.
 *
 * # Safety
 * `server` must be a valid pointer from `divi_globe_server_start`, called exactly once.
 */
void divi_globe_server_free(struct GlobeServer *ptr);

/**
 * Create a new locked vault.
 * Free with `divi_vault_free`.
 */
struct DiviVault *divi_vault_new(void);

/**
 * Free a vault. If the vault is still unlocked, locks it first.
 *
 * # Safety
 * `ptr` must be a valid pointer from `divi_vault_new`, called exactly once.
 */
void divi_vault_free(struct DiviVault *ptr);

/**
 * Whether the vault is currently unlocked.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
bool divi_vault_is_unlocked(const struct DiviVault *vault);

/**
 * Unlock the vault with a password and root directory path.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `password` and `root_path` must be valid C strings.
 */
int32_t divi_vault_unlock(const struct DiviVault *vault,
                          const char *password,
                          const char *root_path);

/**
 * Lock the vault — zeros all keys and closes the manifest.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
int32_t divi_vault_lock(const struct DiviVault *vault);

/**
 * Derive the soul encryption key from the vault's internal master key.
 *
 * The vault must be unlocked. Output is a 32-byte AES-256-GCM key
 * suitable for use with `divi_crown_soul_create_encrypted` and
 * `divi_crown_soul_load_encrypted`.
 *
 * Output written to `out_key`/`out_key_len`. Caller must free via `divi_free_bytes`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `out_key` and `out_key_len` must be valid pointers.
 */
int32_t divi_vault_soul_key(const struct DiviVault *vault,
                            uint8_t **out_key,
                            uintptr_t *out_key_len);

/**
 * Register a .idea entry in the manifest.
 *
 * `entry_json` is a JSON ManifestEntry.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `entry_json` must be a valid C string.
 */
int32_t divi_vault_register_idea(const struct DiviVault *vault, const char *entry_json);

/**
 * Remove a .idea entry from the manifest by ID.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `id` must be a valid C string (UUID).
 */
int32_t divi_vault_unregister_idea(const struct DiviVault *vault, const char *id);

/**
 * Get a manifest entry by ID.
 *
 * Returns JSON ManifestEntry, or null if not found or on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `id` must be a valid C string (UUID).
 */
char *divi_vault_get_idea(const struct DiviVault *vault, const char *id);

/**
 * Get a manifest entry by relative path.
 *
 * Returns JSON ManifestEntry, or null if not found or on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `path` must be a valid C string.
 */
char *divi_vault_get_idea_by_path(const struct DiviVault *vault, const char *path);

/**
 * List ideas matching a filter.
 *
 * `filter_json` is a JSON IdeaFilter. Returns a JSON array of ManifestEntry.
 * Returns null on error. The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `filter_json` must be a valid C string.
 */
char *divi_vault_list_ideas(const struct DiviVault *vault, const char *filter_json);

/**
 * List ideas in a folder (path prefix match).
 *
 * Returns a JSON array of ManifestEntry. Returns null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `folder` must be a valid C string.
 */
char *divi_vault_list_ideas_in_folder(const struct DiviVault *vault, const char *folder);

/**
 * Get the number of registered ideas.
 *
 * Returns the count, or -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
int64_t divi_vault_idea_count(const struct DiviVault *vault);

/**
 * Encrypt data using the content key for a specific idea.
 *
 * Returns 0 on success, -1 on error. Output written to `out_data`/`out_len`.
 * The caller must free `out_data` via `divi_free_bytes`.
 *
 * # Safety
 * `vault` must be a valid pointer. `data` must be valid for `data_len` bytes.
 * `idea_id` must be a valid C string (UUID). `out_data` and `out_len` must be valid pointers.
 */
int32_t divi_vault_encrypt_for_idea(const struct DiviVault *vault,
                                    const uint8_t *data,
                                    uintptr_t data_len,
                                    const char *idea_id,
                                    uint8_t **out_data,
                                    uintptr_t *out_len);

/**
 * Decrypt data using the content key for a specific idea.
 *
 * Returns 0 on success, -1 on error. Output written to `out_data`/`out_len`.
 * The caller must free `out_data` via `divi_free_bytes`.
 *
 * # Safety
 * `vault` must be a valid pointer. `data` must be valid for `data_len` bytes.
 * `idea_id` must be a valid C string (UUID). `out_data` and `out_len` must be valid pointers.
 */
int32_t divi_vault_decrypt_for_idea(const struct DiviVault *vault,
                                    const uint8_t *data,
                                    uintptr_t data_len,
                                    const char *idea_id,
                                    uint8_t **out_data,
                                    uintptr_t *out_len);

/**
 * Get the content key for a specific idea.
 *
 * Returns 0 on success, -1 on error. Output written to `out_key`/`out_key_len`.
 * The caller must free `out_key` via `divi_free_bytes`.
 *
 * # Safety
 * `vault` must be a valid pointer. `idea_id` must be a valid C string (UUID).
 * `out_key` and `out_key_len` must be valid pointers.
 */
int32_t divi_vault_content_key(const struct DiviVault *vault,
                               const char *idea_id,
                               uint8_t **out_key,
                               uintptr_t *out_key_len);

/**
 * Get the vocabulary seed for Babel obfuscation.
 *
 * Returns 0 on success, -1 on error. Output written to `out_seed`/`out_seed_len`.
 * The caller must free `out_seed` via `divi_free_bytes`.
 *
 * # Safety
 * `vault` must be a valid pointer. `out_seed` and `out_seed_len` must be valid pointers.
 */
int32_t divi_vault_vocabulary_seed(const struct DiviVault *vault,
                                   uint8_t **out_seed,
                                   uintptr_t *out_seed_len);

/**
 * Create a new collective. Generates a random 256-bit key.
 *
 * Returns JSON Collective on success. Returns null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `name` and `owner_pubkey` must be valid C strings.
 */
char *divi_vault_create_collective(const struct DiviVault *vault,
                                   const char *name,
                                   const char *owner_pubkey);

/**
 * Join an existing collective with a key received externally.
 *
 * `key_ptr`/`key_len` is the raw 256-bit collective key.
 * `role_json` is a JSON CollectiveRole (e.g., `"member"`).
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `id` and `name` must be valid C strings.
 * `key_ptr` must be valid for `key_len` bytes. `role_json` must be a valid C string.
 */
int32_t divi_vault_join_collective(const struct DiviVault *vault,
                                   const char *id,
                                   const char *name,
                                   const uint8_t *key_ptr,
                                   uintptr_t key_len,
                                   const char *role_json);

/**
 * Leave a collective. Removes the key from memory.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `id` must be a valid C string (UUID).
 */
int32_t divi_vault_leave_collective(const struct DiviVault *vault, const char *id);

/**
 * List all collectives.
 *
 * Returns a JSON array of Collective. Returns null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
char *divi_vault_list_collectives(const struct DiviVault *vault);

/**
 * Get a collective's encryption key as raw bytes.
 *
 * Returns 0 on success, -1 on error. Output written to `out_key`/`out_key_len`.
 * The caller must free `out_key` via `divi_free_bytes`.
 *
 * # Safety
 * `vault` must be a valid pointer. `id` must be a valid C string (UUID).
 * `out_key` and `out_key_len` must be valid pointers.
 */
int32_t divi_vault_collective_key(const struct DiviVault *vault,
                                  const char *id,
                                  uint8_t **out_key,
                                  uintptr_t *out_key_len);

/**
 * Save a module state entry.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. All C strings must be valid.
 */
int32_t divi_vault_save_module_state(const struct DiviVault *vault,
                                     const char *module_id,
                                     const char *state_key,
                                     const char *data);

/**
 * Load a module state entry.
 *
 * Returns the stored data as a C string, or null if not found or on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `module_id` and `state_key` must be valid C strings.
 */
char *divi_vault_load_module_state(const struct DiviVault *vault,
                                   const char *module_id,
                                   const char *state_key);

/**
 * Delete a module state entry.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `module_id` and `state_key` must be valid C strings.
 */
int32_t divi_vault_delete_module_state(const struct DiviVault *vault,
                                       const char *module_id,
                                       const char *state_key);

/**
 * List all state keys for a module.
 *
 * Returns a JSON array of strings. Returns null on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `module_id` must be a valid C string.
 */
char *divi_vault_list_module_state_keys(const struct DiviVault *vault, const char *module_id);

/**
 * Search ideas by text query via FTS5.
 *
 * Returns a JSON array of `SearchHit` objects ordered by relevance.
 * `limit` is the maximum number of results; values <= 0 default to 20.
 * Returns null on error. The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `query` must be a valid C string.
 */
char *divi_vault_search(const struct DiviVault *vault, const char *query, int32_t limit);

/**
 * Index an idea's content for full-text search.
 *
 * `idea_id` is a UUID string. `tags_json` is a JSON array of strings (e.g., `["tag1","tag2"]`),
 * or null for no tags.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `idea_id`, `title`, and `content_text` must be valid C strings.
 * `tags_json` must be a valid C string or null.
 */
int32_t divi_vault_index_idea(const struct DiviVault *vault,
                              const char *idea_id,
                              const char *title,
                              const char *content_text,
                              const char *tags_json);

/**
 * Remove an idea from the search index.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `idea_id` must be a valid C string (UUID).
 */
int32_t divi_vault_remove_search_index(const struct DiviVault *vault, const char *idea_id);

/**
 * Rebuild the entire search index from the manifest table.
 *
 * Returns the number of indexed entries, or -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
int64_t divi_vault_rebuild_search_index(const struct DiviVault *vault);

/**
 * Get the vault root path.
 *
 * Returns a C string, or null if locked. The returned pointer must be freed
 * via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
char *divi_vault_root_path(const struct DiviVault *vault);

/**
 * Get the personal ideas directory path.
 *
 * Returns a C string, or null if locked. The returned pointer must be freed
 * via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
char *divi_vault_personal_path(const struct DiviVault *vault);

/**
 * Get the collectives directory path.
 *
 * Returns a C string, or null if locked. The returned pointer must be freed
 * via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer.
 */
char *divi_vault_collectives_path(const struct DiviVault *vault);

/**
 * Resolve a relative path within the vault root.
 *
 * Returns a C string, or null if locked. The returned pointer must be freed
 * via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `relative` must be a valid C string.
 */
char *divi_vault_resolve_path(const struct DiviVault *vault, const char *relative);

/**
 * Add a member to a collective. Requires Admin or higher.
 *
 * `role_json` is a JSON CollectiveRole (e.g., `"member"`).
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `collective_id`, `pubkey`, and `role_json`
 * must be valid C strings.
 */
int32_t divi_vault_collective_add_member(const struct DiviVault *vault,
                                         const char *collective_id,
                                         const char *pubkey,
                                         const char *role_json);

/**
 * Remove a member from a collective by public key. Requires Owner.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `vault` must be a valid pointer. `collective_id` and `pubkey` must be valid C strings.
 */
int32_t divi_vault_collective_remove_member(const struct DiviVault *vault,
                                            const char *collective_id,
                                            const char *pubkey);

/**
 * Check if a public key is a member of a collective.
 *
 * Returns `true` if the pubkey is a member, `false` otherwise (including on error).
 *
 * # Safety
 * `vault` must be a valid pointer. `collective_id` and `pubkey` must be valid C strings.
 */
bool divi_vault_collective_is_member(const struct DiviVault *vault,
                                     const char *collective_id,
                                     const char *pubkey);

/**
 * Get a member's role in a collective.
 *
 * Returns JSON CollectiveRole string, or null if not a member or on error.
 * The returned pointer must be freed via `divi_free_string`.
 *
 * # Safety
 * `vault` must be a valid pointer. `collective_id` and `pubkey` must be valid C strings.
 */
char *divi_vault_collective_member_role(const struct DiviVault *vault,
                                        const char *collective_id,
                                        const char *pubkey);

/**
 * Create a new YokeLink.
 *
 * `relation_type_json` is a JSON RelationType (e.g. `"DerivedFrom"` or `{"Custom":"blocks"}`).
 * Returns JSON (YokeLink). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_link_new(const char *source,
                         const char *target,
                         const char *relation_type_json,
                         const char *author);

/**
 * Add metadata to a YokeLink.
 *
 * `value_json` is a JSON x::Value (e.g. `{"String":"2.0"}` or `{"Bool":true}`).
 * Returns modified YokeLink JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_link_with_metadata(const char *link_json, const char *key, const char *value_json);

/**
 * Create a new VersionTag on the "main" branch.
 *
 * `idea_id` is a UUID string. `snapshot_clock_json` is a JSON VectorClock.
 * Returns JSON (VersionTag). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_tag_new(const char *idea_id,
                                const char *name,
                                const char *snapshot_clock_json,
                                const char *author);

/**
 * Add a message to a VersionTag.
 *
 * Returns modified VersionTag JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_tag_with_message(const char *tag_json, const char *message);

/**
 * Set the branch of a VersionTag.
 *
 * Returns modified VersionTag JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_tag_on_branch(const char *tag_json, const char *branch);

/**
 * Create a new empty VersionChain for an idea.
 *
 * `idea_id` is a UUID string.
 * Returns JSON (VersionChain). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `idea_id` must be a valid C string.
 */
char *divi_yoke_version_chain_new(const char *idea_id);

/**
 * Tag a version on a VersionChain.
 *
 * `chain_json` is the existing chain, `tag_json` is the VersionTag to add.
 * Returns modified VersionChain JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_chain_tag_version(const char *chain_json, const char *tag_json);

/**
 * Create a branch on a VersionChain.
 *
 * `from_version_id` is the UUID of the version to branch from.
 * Returns modified VersionChain JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_chain_create_branch(const char *chain_json,
                                            const char *branch_name,
                                            const char *from_version_id,
                                            const char *author);

/**
 * Merge a branch back into another branch.
 *
 * `merge_version_id` is a UUID for the merge version record.
 * Returns modified VersionChain JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_chain_merge_branch(const char *chain_json,
                                           const char *source_branch,
                                           const char *target_branch,
                                           const char *merge_version_id,
                                           const char *author);

/**
 * Get all versions on a specific branch, chronologically.
 *
 * Returns JSON array of VersionTag. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_chain_versions_on_branch(const char *chain_json, const char *branch);

/**
 * Get the latest version on a branch, or null if none.
 *
 * Returns JSON (VersionTag) or null. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_chain_latest_version(const char *chain_json, const char *branch);

/**
 * Get a version by name (first match across all branches), or null if not found.
 *
 * Returns JSON (VersionTag) or null. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_version_chain_version_by_name(const char *chain_json, const char *name);

/**
 * Get all branch names (including "main").
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `chain_json` must be a valid C string.
 */
char *divi_yoke_version_chain_branch_names(const char *chain_json);

/**
 * Check if a branch has been merged.
 *
 * Returns 1 if merged, 0 if not merged, -1 on error.
 *
 * # Safety
 * All C strings must be valid.
 */
int32_t divi_yoke_version_chain_is_branch_merged(const char *chain_json, const char *branch_name);

/**
 * Get the total number of versions in a chain.
 *
 * Returns count on success, -1 on error.
 *
 * # Safety
 * `chain_json` must be a valid C string.
 */
int32_t divi_yoke_version_chain_version_count(const char *chain_json);

/**
 * Get the total number of branches in a chain (including "main").
 *
 * Returns count on success, -1 on error.
 *
 * # Safety
 * `chain_json` must be a valid C string.
 */
int32_t divi_yoke_version_chain_branch_count(const char *chain_json);

/**
 * Create a new ActivityRecord.
 *
 * `action_json` is a JSON ActivityAction (e.g. `"Created"` or `{"Custom":"archived"}`).
 * `target_type_json` is a JSON TargetType (e.g. `"Idea"` or `{"Custom":"project"}`).
 * Returns JSON (ActivityRecord). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_activity_record_new(const char *actor,
                                    const char *action_json,
                                    const char *target_id,
                                    const char *target_type_json);

/**
 * Add context to an ActivityRecord.
 *
 * Returns modified ActivityRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_activity_record_with_context(const char *record_json, const char *context);

/**
 * Set the community on an ActivityRecord.
 *
 * Returns modified ActivityRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_activity_record_in_community(const char *record_json, const char *community_id);

/**
 * Create a new Milestone.
 *
 * `significance_json` is a JSON MilestoneSignificance (e.g. `"Major"`).
 * Returns JSON (Milestone). Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_milestone_new(const char *name, const char *significance_json, const char *author);

/**
 * Add a description to a Milestone.
 *
 * Returns modified Milestone JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_milestone_with_description(const char *milestone_json, const char *description);

/**
 * Set the community on a Milestone.
 *
 * Returns modified Milestone JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_milestone_in_community(const char *milestone_json, const char *community_id);

/**
 * Add a related event to a Milestone.
 *
 * Returns modified Milestone JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_milestone_with_related_event(const char *milestone_json, const char *event_id);

/**
 * Create a new empty Timeline.
 *
 * Returns JSON (Timeline). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `owner_id` must be a valid C string.
 */
char *divi_yoke_timeline_new(const char *owner_id);

/**
 * Set the config on a Timeline.
 *
 * `config_json` is a JSON TimelineConfig.
 * Returns modified Timeline JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_with_config(const char *timeline_json, const char *config_json);

/**
 * Record an activity on a Timeline.
 *
 * `activity_json` is a JSON ActivityRecord.
 * Returns modified Timeline JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_record(const char *timeline_json, const char *activity_json);

/**
 * Mark a milestone on a Timeline.
 *
 * `milestone_json` is a JSON Milestone.
 * Returns modified Timeline JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_mark_milestone(const char *timeline_json, const char *milestone_json);

/**
 * Prune activities older than a cutoff timestamp.
 *
 * `cutoff` is a Unix timestamp (seconds since epoch).
 * Returns modified Timeline JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `timeline_json` must be a valid C string.
 */
char *divi_yoke_timeline_prune_before(const char *timeline_json, int64_t cutoff);

/**
 * Query activities by actor.
 *
 * Returns JSON array of ActivityRecord. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_by_actor(const char *timeline_json, const char *actor);

/**
 * Query activities by action type.
 *
 * `action_json` is a JSON ActivityAction (e.g. `"Created"`).
 * Returns JSON array of ActivityRecord. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_by_action(const char *timeline_json, const char *action_json);

/**
 * Query activities targeting a specific entity.
 *
 * Returns JSON array of ActivityRecord. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_for_target(const char *timeline_json, const char *target_id);

/**
 * Query activities in a specific community.
 *
 * Returns JSON array of ActivityRecord. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_in_community(const char *timeline_json, const char *community_id);

/**
 * Query activities within a time range.
 *
 * `since` and `until` are Unix timestamps (seconds since epoch).
 * Returns JSON array of ActivityRecord. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `timeline_json` must be a valid C string.
 */
char *divi_yoke_timeline_between(const char *timeline_json, int64_t since, int64_t until);

/**
 * Query milestones at or above a given significance.
 *
 * `significance_json` is a JSON MilestoneSignificance (e.g. `"Major"`).
 * Returns JSON array of Milestone. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_timeline_milestones_at_least(const char *timeline_json,
                                             const char *significance_json);

/**
 * Get the total number of activities in a Timeline.
 *
 * Returns count on success, -1 on error.
 *
 * # Safety
 * `timeline_json` must be a valid C string.
 */
int32_t divi_yoke_timeline_activity_count(const char *timeline_json);

/**
 * Get the total number of milestones in a Timeline.
 *
 * Returns count on success, -1 on error.
 *
 * # Safety
 * `timeline_json` must be a valid C string.
 */
int32_t divi_yoke_timeline_milestone_count(const char *timeline_json);

/**
 * Create a new CeremonyRecord.
 *
 * `ceremony_type_json` is a JSON CeremonyType (e.g. `"CovenantOath"` or
 * `{"Custom":"graduation"}`).
 * Returns JSON (CeremonyRecord). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ceremony_type_json` must be a valid C string.
 */
char *divi_yoke_ceremony_new(const char *ceremony_type_json);

/**
 * Add a principal to a CeremonyRecord.
 *
 * Returns modified CeremonyRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_ceremony_with_principal(const char *ceremony_json, const char *crown_id);

/**
 * Add a witness to a CeremonyRecord.
 *
 * Returns modified CeremonyRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_ceremony_with_witness(const char *ceremony_json, const char *crown_id);

/**
 * Add an officiant to a CeremonyRecord.
 *
 * Returns modified CeremonyRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_ceremony_with_officiant(const char *ceremony_json, const char *crown_id);

/**
 * Add a participant with a custom role to a CeremonyRecord.
 *
 * `role_json` is a JSON ParticipantRole (e.g. `"Witness"` or `{"Custom":"mentor"}`).
 * Returns modified CeremonyRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_ceremony_with_participant(const char *ceremony_json,
                                          const char *crown_id,
                                          const char *role_json);

/**
 * Set the community on a CeremonyRecord.
 *
 * Returns modified CeremonyRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_ceremony_in_community(const char *ceremony_json, const char *community_id);

/**
 * Set the content on a CeremonyRecord (oath text, charter, etc.).
 *
 * Returns modified CeremonyRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_ceremony_with_content(const char *ceremony_json, const char *content);

/**
 * Add a related event to a CeremonyRecord.
 *
 * Returns modified CeremonyRecord JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_ceremony_with_related_event(const char *ceremony_json, const char *event_id);

/**
 * Validate a CeremonyRecord against its structural rules.
 *
 * Returns 0 on success, -1 on validation failure (check `divi_last_error`).
 *
 * # Safety
 * `ceremony_json` must be a valid C string.
 */
int32_t divi_yoke_ceremony_validate(const char *ceremony_json);

/**
 * Get all principals in a CeremonyRecord.
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ceremony_json` must be a valid C string.
 */
char *divi_yoke_ceremony_principals(const char *ceremony_json);

/**
 * Get all witnesses in a CeremonyRecord.
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ceremony_json` must be a valid C string.
 */
char *divi_yoke_ceremony_witnesses(const char *ceremony_json);

/**
 * Get all officiants in a CeremonyRecord.
 *
 * Returns JSON array of strings. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `ceremony_json` must be a valid C string.
 */
char *divi_yoke_ceremony_officiants(const char *ceremony_json);

/**
 * Get the total participant count of a CeremonyRecord.
 *
 * Returns count on success, -1 on error.
 *
 * # Safety
 * `ceremony_json` must be a valid C string.
 */
int32_t divi_yoke_ceremony_participant_count(const char *ceremony_json);

/**
 * Create a new empty RelationshipGraph (as a GraphSnapshot).
 *
 * Returns JSON (GraphSnapshot). Caller must free via `divi_free_string`.
 */
char *divi_yoke_graph_new(void);

/**
 * Add a link to a RelationshipGraph.
 *
 * `graph_json` is a JSON GraphSnapshot. `link_json` is a JSON YokeLink.
 * Returns modified GraphSnapshot JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_add_link(const char *graph_json, const char *link_json);

/**
 * Remove all links involving an entity from the graph.
 *
 * Returns modified GraphSnapshot JSON. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_remove_entity(const char *graph_json, const char *entity_id);

/**
 * Get all links originating from an entity.
 *
 * Returns JSON array of YokeLink. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_links_from(const char *graph_json, const char *source);

/**
 * Get all links pointing to an entity.
 *
 * Returns JSON array of YokeLink. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_links_to(const char *graph_json, const char *target);

/**
 * Find all ancestors via provenance links (DerivedFrom, VersionOf, etc.).
 *
 * Returns JSON array of TraversalNodeJson. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_ancestors(const char *graph_json, const char *entity_id);

/**
 * Find all descendants via provenance links.
 *
 * Returns JSON array of TraversalNodeJson. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_descendants(const char *graph_json, const char *entity_id);

/**
 * Get all comments on an entity.
 *
 * Returns JSON array of YokeLink. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_comments_on(const char *graph_json, const char *entity_id);

/**
 * Get all version-of links pointing to an entity.
 *
 * Returns JSON array of YokeLink. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_versions_of(const char *graph_json, const char *entity_id);

/**
 * Get all endorsements of an entity.
 *
 * Returns JSON array of YokeLink. Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_endorsements_of(const char *graph_json, const char *entity_id);

/**
 * Find the shortest path between two entities in the graph.
 *
 * Returns JSON array of strings (entity IDs) or null if no path exists.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_yoke_graph_path_between(const char *graph_json, const char *from, const char *to);

/**
 * Get the total number of links in the graph.
 *
 * Returns count on success, -1 on error.
 *
 * # Safety
 * `graph_json` must be a valid C string.
 */
int32_t divi_yoke_graph_link_count(const char *graph_json);

/**
 * Get the total number of unique entities in the graph.
 *
 * Returns count on success, -1 on error.
 *
 * # Safety
 * `graph_json` must be a valid C string.
 */
int32_t divi_yoke_graph_entity_count(const char *graph_json);

/**
 * Create an exporter registry with all built-in exporters (15 formats).
 */
ExporterRegistry *divi_exporter_registry_with_defaults(void);

/**
 * Create an empty exporter registry.
 */
ExporterRegistry *divi_exporter_registry_new(void);

/**
 * Free an exporter registry.
 *
 * # Safety
 * `ptr` must have been returned by `divi_exporter_registry_*` or be null.
 */
void divi_exporter_registry_free(ExporterRegistry *ptr);

/**
 * Number of registered exporters.
 *
 * # Safety
 * `ptr` must be a valid `ExporterRegistry`.
 */
int32_t divi_exporter_registry_count(const ExporterRegistry *ptr);

/**
 * List all registered exporter IDs as a JSON array of strings.
 *
 * # Safety
 * `ptr` must be a valid `ExporterRegistry`.
 */
char *divi_exporter_registry_list(const ExporterRegistry *ptr);

/**
 * List all supported export formats as a JSON array of strings.
 *
 * # Safety
 * `ptr` must be a valid `ExporterRegistry`.
 */
char *divi_exporter_registry_supported_formats(const ExporterRegistry *ptr);

/**
 * Export digits to the format specified in the config.
 *
 * Returns a byte buffer via `out_ptr` and `out_len`. Caller must free with
 * `divi_free_bytes`. Also returns filename and mime_type as JSON via the
 * return value (free with `divi_free_string`).
 *
 * `digits_json`: JSON array of serialized Digits.
 * `root_id`: optional UUID string (null for none).
 * `config_json`: JSON-serialized `ExportConfig`.
 *
 * Returns JSON `{"filename":"...","mime_type":"..."}` on success, null on error.
 *
 * # Safety
 * All pointers must be valid. `out_ptr` and `out_len` must be non-null.
 */
char *divi_export(const ExporterRegistry *registry,
                  const char *digits_json,
                  const char *root_id,
                  const char *config_json,
                  uint8_t **out_ptr,
                  uintptr_t *out_len);

/**
 * Create an importer registry with all built-in importers (7 formats).
 */
ImporterRegistry *divi_importer_registry_with_defaults(void);

/**
 * Create an empty importer registry.
 */
ImporterRegistry *divi_importer_registry_new(void);

/**
 * Free an importer registry.
 *
 * # Safety
 * `ptr` must have been returned by `divi_importer_registry_*` or be null.
 */
void divi_importer_registry_free(ImporterRegistry *ptr);

/**
 * Number of registered importers.
 *
 * # Safety
 * `ptr` must be a valid `ImporterRegistry`.
 */
int32_t divi_importer_registry_count(const ImporterRegistry *ptr);

/**
 * List all registered importer IDs as a JSON array of strings.
 *
 * # Safety
 * `ptr` must be a valid `ImporterRegistry`.
 */
char *divi_importer_registry_list(const ImporterRegistry *ptr);

/**
 * Import data from a file into Ideas digits.
 *
 * `data` + `data_len`: raw file bytes.
 * `mime_type`: MIME type string (e.g., "text/csv", "application/pdf").
 * `config_json`: JSON-serialized `ImportConfig`.
 *
 * Returns JSON-serialized `ImportOutput` on success (free with `divi_free_string`).
 * Returns null on error.
 *
 * # Safety
 * All pointers must be valid. `data` must point to `data_len` bytes.
 */
char *divi_import(const ImporterRegistry *registry,
                  const uint8_t *data,
                  uintptr_t data_len,
                  const char *mime_type,
                  const char *config_json);

/**
 * Create a bridge registry with all built-in bridges (SMTP).
 */
BridgeRegistry *divi_bridge_registry_with_defaults(void);

/**
 * Create an empty bridge registry.
 */
BridgeRegistry *divi_bridge_registry_new(void);

/**
 * Free a bridge registry.
 *
 * # Safety
 * `ptr` must have been returned by `divi_bridge_registry_*` or be null.
 */
void divi_bridge_registry_free(BridgeRegistry *ptr);

/**
 * Number of registered bridges.
 *
 * # Safety
 * `ptr` must be a valid `BridgeRegistry`.
 */
int32_t divi_bridge_registry_count(const BridgeRegistry *ptr);

/**
 * List all registered bridge IDs as a JSON array of strings.
 *
 * # Safety
 * `ptr` must be a valid `BridgeRegistry`.
 */
char *divi_bridge_registry_list(const BridgeRegistry *ptr);

/**
 * Bridge an Equipment MailMessage to an external protocol.
 *
 * `message_json`: JSON-serialized `MailMessage`.
 * `config_json`: JSON-serialized `BridgeConfig`.
 *
 * Returns JSON-serialized `BridgeResult` on success (free with `divi_free_string`).
 * Returns null on error.
 *
 * # Safety
 * All pointers must be valid.
 */
char *divi_bridge(const BridgeRegistry *registry,
                  const char *message_json,
                  const char *config_json);

/**
 * Get the file extension for an export format (e.g., "pdf", "docx").
 *
 * `format_json`: JSON string of the format name (e.g., `"pdf"`).
 * Returns the extension string. Caller must free with `divi_free_string`.
 */
char *divi_export_format_extension(const char *format_json);

/**
 * Get the MIME type for an export format (e.g., "application/pdf").
 *
 * `format_json`: format name string (e.g., "pdf").
 * Returns the MIME type string. Caller must free with `divi_free_string`.
 */
char *divi_export_format_mime_type(const char *format_json);

/**
 * Deserialize and re-serialize a NetworkHealth from JSON.
 *
 * `network_health_json` is a JSON `NetworkHealth` (pre-computed from
 * `NetworkHealth::from_relay_health` on the Rust side).
 *
 * Note: Globe's `RelayHealth` is not serializable (it contains private
 * fields and `Url`/`Duration` types). The aggregation from relay data
 * to `NetworkHealth` must happen in Rust before crossing the FFI
 * boundary. This function validates and round-trips the data.
 *
 * Returns JSON (NetworkHealth). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `network_health_json` must be a valid C string.
 */
char *divi_undercroft_network_health(const char *network_health_json);

/**
 * Create an empty NetworkHealth snapshot.
 *
 * Returns JSON (NetworkHealth). Caller must free via `divi_free_string`.
 */
char *divi_undercroft_network_health_empty(void);

/**
 * Aggregate community health from Kingdom and Bulwark data.
 *
 * - `community_json` is a JSON `kingdom::Community`.
 * - `proposals_json` is a JSON array of `kingdom::Proposal`.
 * - `pulse_json` is a nullable JSON `bulwark::CollectiveHealthPulse`.
 *
 * Returns JSON (CommunityHealth). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `community_json` and `proposals_json` must be valid C strings.
 * `pulse_json` may be null.
 */
char *divi_undercroft_community_health(const char *community_json,
                                       const char *proposals_json,
                                       const char *pulse_json);

/**
 * Aggregate economic health from a Fortune TreasuryStatus.
 *
 * `treasury_status_json` is a JSON `fortune::TreasuryStatus`.
 * Returns JSON (EconomicHealth). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `treasury_status_json` must be a valid C string.
 */
char *divi_undercroft_economic_health(const char *treasury_status_json);

/**
 * Create an empty EconomicHealth snapshot.
 *
 * Returns JSON (EconomicHealth). Caller must free via `divi_free_string`.
 */
char *divi_undercroft_economic_health_empty(void);

/**
 * Compute top-level health metrics from a snapshot.
 *
 * - `snapshot_json` is a JSON `HealthSnapshot`.
 * - `node_count` is the estimated number of network nodes.
 * - `store_stats_json` is a nullable JSON `globe::StoreStats`.
 *
 * Returns JSON (HealthMetrics). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `snapshot_json` must be a valid C string. `store_stats_json` may be null.
 */
char *divi_undercroft_health_metrics(const char *snapshot_json,
                                     uint64_t node_count,
                                     const char *store_stats_json);

/**
 * Create a new health history with the given maximum retention.
 *
 * Free with `divi_undercroft_history_free`.
 */
struct UndercraftHistory *divi_undercroft_history_new(uintptr_t capacity);

/**
 * Create a health history with default retention (168 = 1 week of hourly snapshots).
 *
 * Free with `divi_undercroft_history_free`.
 */
struct UndercraftHistory *divi_undercroft_history_default(void);

/**
 * Free a health history.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_undercroft_history_free(struct UndercraftHistory *ptr);

/**
 * Push a snapshot into the history. Evicts the oldest if at capacity.
 *
 * `snapshot_json` is a JSON `HealthSnapshot`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `history` must be a valid pointer. `snapshot_json` must be a valid C string.
 */
int32_t divi_undercroft_history_push(const struct UndercraftHistory *history,
                                     const char *snapshot_json);

/**
 * Get the most recent snapshot, if any.
 *
 * Returns JSON (HealthSnapshot) or null if empty. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
char *divi_undercroft_history_latest(const struct UndercraftHistory *history);

/**
 * Get the number of snapshots in the history.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
uintptr_t divi_undercroft_history_len(const struct UndercraftHistory *history);

/**
 * Whether the history is empty.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
bool divi_undercroft_history_is_empty(const struct UndercraftHistory *history);

/**
 * Get all snapshots as a JSON array (oldest to newest).
 *
 * Returns JSON array of HealthSnapshot. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `history` must be a valid pointer.
 */
char *divi_undercroft_history_all(const struct UndercraftHistory *history);

/**
 * Validate and round-trip a RelayPrivacyHealth JSON.
 *
 * `health_json` is a JSON `RelayPrivacyHealth`.
 * Returns JSON (RelayPrivacyHealth). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `health_json` must be a valid C string.
 */
char *divi_undercroft_privacy_health(const char *health_json);

/**
 * Compute the privacy health score from a RelayPrivacyHealth JSON.
 *
 * `health_json` is a JSON `RelayPrivacyHealth`.
 * Returns the health score (0.0-1.0), or -1.0 on error.
 *
 * # Safety
 * `health_json` must be a valid C string.
 */
double divi_undercroft_privacy_health_score(const char *health_json);

/**
 * Build quest health from an ObservatoryReport JSON.
 *
 * `report_json` is a JSON `quest::ObservatoryReport`.
 * Returns JSON (QuestHealth). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `report_json` must be a valid C string.
 */
char *divi_undercroft_quest_health(const char *report_json);

/**
 * Create an empty QuestHealth snapshot.
 *
 * Returns JSON (QuestHealth). Caller must free via `divi_free_string`.
 */
char *divi_undercroft_quest_health_empty(void);

/**
 * Compute the quest health score from a QuestHealth JSON.
 *
 * `quest_health_json` is a JSON `QuestHealth`.
 * Returns the health score (0.0-1.0), or -1.0 on error.
 *
 * # Safety
 * `quest_health_json` must be a valid C string.
 */
double divi_undercroft_quest_health_score(const char *quest_health_json);

/**
 * Create a new empty app catalog.
 *
 * Free with `divi_appcatalog_free`.
 */
struct DiviAppCatalog *divi_appcatalog_new(void);

/**
 * Free an app catalog.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_appcatalog_free(struct DiviAppCatalog *ptr);

/**
 * Add or update a manifest in the catalog.
 *
 * `manifest_json` is a JSON `AppManifest`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `catalog` must be a valid pointer. `manifest_json` must be a valid C string.
 */
int32_t divi_appcatalog_add_manifest(const struct DiviAppCatalog *catalog,
                                     const char *manifest_json);

/**
 * Look up a catalog entry by app ID.
 *
 * Returns JSON (CatalogEntry) or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `catalog` must be a valid pointer. `app_id` must be a valid C string.
 */
char *divi_appcatalog_get(const struct DiviAppCatalog *catalog, const char *app_id);

/**
 * Search for entries by name or description (case-insensitive substring).
 *
 * Returns JSON array of CatalogEntry. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `catalog` must be a valid pointer. `query` must be a valid C string.
 */
char *divi_appcatalog_search(const struct DiviAppCatalog *catalog, const char *query);

/**
 * Get all installed entries.
 *
 * Returns JSON array of CatalogEntry. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `catalog` must be a valid pointer.
 */
char *divi_appcatalog_installed(const struct DiviAppCatalog *catalog);

/**
 * Get all entries with updates available.
 *
 * Returns JSON array of CatalogEntry. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `catalog` must be a valid pointer.
 */
char *divi_appcatalog_updates_available(const struct DiviAppCatalog *catalog);

/**
 * Get all entries in the catalog.
 *
 * Returns JSON array of CatalogEntry. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `catalog` must be a valid pointer.
 */
char *divi_appcatalog_all(const struct DiviAppCatalog *catalog);

/**
 * Get the number of entries in the catalog.
 *
 * # Safety
 * `catalog` must be a valid pointer.
 */
uintptr_t divi_appcatalog_count(const struct DiviAppCatalog *catalog);

/**
 * Mark an app as installed at the given version.
 *
 * Returns 0 on success, -1 on error (not found or already installed).
 *
 * # Safety
 * `catalog` must be a valid pointer. C strings must be valid.
 */
int32_t divi_appcatalog_mark_installed(const struct DiviAppCatalog *catalog,
                                       const char *app_id,
                                       const char *version);

/**
 * Mark an app as uninstalled (revert to Available).
 *
 * Returns 0 on success, -1 on error (not found or not installed).
 *
 * # Safety
 * `catalog` must be a valid pointer. `app_id` must be a valid C string.
 */
int32_t divi_appcatalog_mark_uninstalled(const struct DiviAppCatalog *catalog, const char *app_id);

/**
 * Set an arbitrary install status on an entry.
 *
 * `status_json` is a JSON `InstallStatus`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `catalog` must be a valid pointer. C strings must be valid.
 */
int32_t divi_appcatalog_mark_status(const struct DiviAppCatalog *catalog,
                                    const char *app_id,
                                    const char *status_json);

/**
 * Scan installed entries and mark those with newer manifest versions.
 *
 * # Safety
 * `catalog` must be a valid pointer.
 */
void divi_appcatalog_check_updates(const struct DiviAppCatalog *catalog);

/**
 * Remove an entry from the catalog entirely.
 *
 * Returns JSON (CatalogEntry) of the removed entry, or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `catalog` must be a valid pointer. `app_id` must be a valid C string.
 */
char *divi_appcatalog_remove(const struct DiviAppCatalog *catalog, const char *app_id);

/**
 * Resolve the platform-appropriate install action for a request.
 *
 * - `request_json` is a JSON `InstallRequest`.
 * - `manifest_json` is a JSON `AppManifest`.
 *
 * Returns JSON (InstallAction). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * Both C strings must be valid.
 */
char *divi_appcatalog_resolve_install(const char *request_json, const char *manifest_json);

/**
 * Verify a manifest's BIP-340 signature.
 *
 * `manifest_json` is a JSON `AppManifest`.
 * Returns 0 if the signature is valid, -1 if invalid or absent.
 *
 * # Safety
 * `manifest_json` must be a valid C string.
 */
int32_t divi_appcatalog_manifest_verify(const char *manifest_json);

/**
 * Get the latest version entry from a manifest.
 *
 * `manifest_json` is a JSON `AppManifest`.
 * Returns JSON (AppVersion) or null if no versions exist.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `manifest_json` must be a valid C string.
 */
char *divi_appcatalog_manifest_latest_version(const char *manifest_json);

/**
 * Initiate a pairing challenge.
 *
 * - `secret_key_hex` is the initiator's 32-byte private key as a 64-char hex string.
 * - `device_name` is a human-readable device name.
 * - `relay_url` is the relay URL for the pairing response.
 *
 * Returns JSON (PairingChallenge). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_device_pairing_initiate(const char *secret_key_hex,
                                   const char *device_name,
                                   const char *relay_url);

/**
 * Respond to a pairing challenge by signing the nonce.
 *
 * - `challenge_json` is a JSON `PairingChallenge`.
 * - `secret_key_hex` is the responder's 32-byte private key as a 64-char hex string.
 * - `device_name` is the responder's human-readable device name.
 *
 * Returns JSON (PairingResponse). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * All C strings must be valid.
 */
char *divi_device_pairing_respond(const char *challenge_json,
                                  const char *secret_key_hex,
                                  const char *device_name);

/**
 * Verify a pairing response and produce a DevicePair.
 *
 * - `challenge_json` is a JSON `PairingChallenge`.
 * - `response_json` is a JSON `PairingResponse`.
 *
 * Returns JSON (DevicePair). Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * Both C strings must be valid.
 */
char *divi_device_pairing_verify(const char *challenge_json, const char *response_json);

/**
 * Create a new empty device fleet.
 *
 * Free with `divi_device_fleet_free`.
 */
struct DiviDeviceFleet *divi_device_fleet_new(void);

/**
 * Free a device fleet.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_device_fleet_free(struct DiviDeviceFleet *ptr);

/**
 * Add a device to the fleet.
 *
 * `entry_json` is a JSON `FleetEntry`.
 * Returns 0 on success, -1 on error (e.g. already paired).
 *
 * # Safety
 * `fleet` must be a valid pointer. `entry_json` must be a valid C string.
 */
int32_t divi_device_fleet_add(const struct DiviDeviceFleet *fleet, const char *entry_json);

/**
 * Remove a device from the fleet.
 *
 * Returns JSON (FleetEntry) of the removed device, or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `fleet` must be a valid pointer. `crown_id` must be a valid C string.
 */
char *divi_device_fleet_remove(const struct DiviDeviceFleet *fleet, const char *crown_id);

/**
 * Get a device by crown_id.
 *
 * Returns JSON (FleetEntry) or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `fleet` must be a valid pointer. `crown_id` must be a valid C string.
 */
char *divi_device_fleet_get(const struct DiviDeviceFleet *fleet, const char *crown_id);

/**
 * List all devices in the fleet.
 *
 * Returns JSON array of FleetEntry. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `fleet` must be a valid pointer.
 */
char *divi_device_fleet_list(const struct DiviDeviceFleet *fleet);

/**
 * Update the status of a device.
 *
 * `status_json` is a JSON `DeviceStatus`.
 * Returns 0 on success, -1 on error (device not found).
 *
 * # Safety
 * `fleet` must be a valid pointer. C strings must be valid.
 */
int32_t divi_device_fleet_update_status(const struct DiviDeviceFleet *fleet,
                                        const char *crown_id,
                                        const char *status_json);

/**
 * Get aggregate fleet health.
 *
 * Returns JSON (FleetHealth). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `fleet` must be a valid pointer.
 */
char *divi_device_fleet_health(const struct DiviDeviceFleet *fleet);

/**
 * Get the number of devices in the fleet.
 *
 * # Safety
 * `fleet` must be a valid pointer.
 */
uintptr_t divi_device_fleet_count(const struct DiviDeviceFleet *fleet);

/**
 * Whether the fleet has no devices.
 *
 * # Safety
 * `fleet` must be a valid pointer.
 */
bool divi_device_fleet_is_empty(const struct DiviDeviceFleet *fleet);

/**
 * Serialize the fleet to JSON.
 *
 * Returns JSON (DeviceFleet). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `fleet` must be a valid pointer.
 */
char *divi_device_fleet_to_json(const struct DiviDeviceFleet *fleet);

/**
 * Deserialize a fleet from JSON.
 *
 * `json` is a JSON `DeviceFleet`.
 * Returns a new fleet pointer. Free with `divi_device_fleet_free`.
 * Returns null on error.
 *
 * # Safety
 * `json` must be a valid C string.
 */
struct DiviDeviceFleet *divi_device_fleet_from_json(const char *json);

/**
 * Create a new empty sync priority map.
 *
 * Free with `divi_device_sync_priority_free`.
 */
struct DiviSyncPriority *divi_device_sync_priority_new(void);

/**
 * Free a sync priority map.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_device_sync_priority_free(struct DiviSyncPriority *ptr);

/**
 * Set which device is home for a data type.
 *
 * # Safety
 * `priority` must be a valid pointer. C strings must be valid.
 */
void divi_device_sync_priority_set_home(const struct DiviSyncPriority *priority,
                                        const char *data_type,
                                        const char *device_crown_id);

/**
 * Get the home device for a data type.
 *
 * Returns a C string or null if no home is set.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `priority` must be a valid pointer. `data_type` must be a valid C string.
 */
char *divi_device_sync_priority_home_for(const struct DiviSyncPriority *priority,
                                         const char *data_type);

/**
 * Remove the home assignment for a data type.
 *
 * # Safety
 * `priority` must be a valid pointer. `data_type` must be a valid C string.
 */
void divi_device_sync_priority_remove(const struct DiviSyncPriority *priority,
                                      const char *data_type);

/**
 * Get all home assignments.
 *
 * Returns JSON object mapping data_type -> device_crown_id.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `priority` must be a valid pointer.
 */
char *divi_device_sync_priority_all(const struct DiviSyncPriority *priority);

/**
 * Serialize the sync priority map to JSON.
 *
 * Returns JSON (SyncPriority). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `priority` must be a valid pointer.
 */
char *divi_device_sync_priority_to_json(const struct DiviSyncPriority *priority);

/**
 * Deserialize a sync priority map from JSON.
 *
 * `json` is a JSON `SyncPriority`.
 * Returns a new pointer. Free with `divi_device_sync_priority_free`.
 * Returns null on error.
 *
 * # Safety
 * `json` must be a valid C string.
 */
struct DiviSyncPriority *divi_device_sync_priority_from_json(const char *json);

/**
 * Create a new empty sync tracker.
 *
 * Free with `divi_device_sync_tracker_free`.
 */
struct DiviSyncTracker *divi_device_sync_tracker_new(void);

/**
 * Free a sync tracker.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_device_sync_tracker_free(struct DiviSyncTracker *ptr);

/**
 * Set the sync state for a device + data type pair.
 *
 * `state_json` is a JSON `SyncState`.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `tracker` must be a valid pointer. C strings must be valid.
 */
int32_t divi_device_sync_tracker_set_state(const struct DiviSyncTracker *tracker,
                                           const char *crown_id,
                                           const char *data_type,
                                           const char *state_json);

/**
 * Get the sync state for a device + data type pair.
 *
 * Returns JSON (SyncState). Returns "Unknown" if not tracked.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tracker` must be a valid pointer. C strings must be valid.
 */
char *divi_device_sync_tracker_get_state(const struct DiviSyncTracker *tracker,
                                         const char *crown_id,
                                         const char *data_type);

/**
 * Get all sync states for a specific device.
 *
 * Returns JSON object mapping data_type -> SyncState, or null if device not tracked.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tracker` must be a valid pointer. `crown_id` must be a valid C string.
 */
char *divi_device_sync_tracker_states_for_device(const struct DiviSyncTracker *tracker,
                                                 const char *crown_id);

/**
 * Check if all tracked states are Synced.
 *
 * Returns true if all synced (or empty).
 *
 * # Safety
 * `tracker` must be a valid pointer.
 */
bool divi_device_sync_tracker_all_synced(const struct DiviSyncTracker *tracker);

/**
 * Get all conflict states.
 *
 * Returns JSON array of objects with `device_crown_id`, `data_type`, and `state` fields.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tracker` must be a valid pointer.
 */
char *divi_device_sync_tracker_conflicts(const struct DiviSyncTracker *tracker);

/**
 * Serialize the sync tracker to JSON.
 *
 * Returns JSON (SyncTracker). Caller must free via `divi_free_string`.
 *
 * # Safety
 * `tracker` must be a valid pointer.
 */
char *divi_device_sync_tracker_to_json(const struct DiviSyncTracker *tracker);

/**
 * Deserialize a sync tracker from JSON.
 *
 * `json` is a JSON `SyncTracker`.
 * Returns a new pointer. Free with `divi_device_sync_tracker_free`.
 * Returns null on error.
 *
 * # Safety
 * `json` must be a valid C string.
 */
struct DiviSyncTracker *divi_device_sync_tracker_from_json(const char *json);

/**
 * Create a new empty Tower directory.
 *
 * Free with `divi_zeitgeist_directory_free`.
 */
struct ZeitgeistDirectory *divi_zeitgeist_directory_new(void);

/**
 * Free a Tower directory.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_zeitgeist_directory_free(struct ZeitgeistDirectory *ptr);

/**
 * Update the directory from a JSON array of `OmniEvent`.
 *
 * Returns 0 on success, -1 on error (invalid JSON).
 *
 * # Safety
 * `dir` must be a valid pointer. `events_json` must be a valid C string.
 */
int32_t divi_zeitgeist_directory_update(const struct ZeitgeistDirectory *dir,
                                        const char *events_json);

/**
 * Look up a Tower by pubkey.
 *
 * Returns JSON `TowerEntry`, or null if not found. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `dir` must be a valid pointer. `pubkey` must be a valid C string.
 */
char *divi_zeitgeist_directory_get(const struct ZeitgeistDirectory *dir, const char *pubkey);

/**
 * Get all Towers in the directory.
 *
 * Returns JSON array of `TowerEntry`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `dir` must be a valid pointer.
 */
char *divi_zeitgeist_directory_all(const struct ZeitgeistDirectory *dir);

/**
 * Get all searchable Towers.
 *
 * Returns JSON array of `TowerEntry`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `dir` must be a valid pointer.
 */
char *divi_zeitgeist_directory_searchable(const struct ZeitgeistDirectory *dir);

/**
 * Get all Harbor Towers.
 *
 * Returns JSON array of `TowerEntry`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `dir` must be a valid pointer.
 */
char *divi_zeitgeist_directory_harbors(const struct ZeitgeistDirectory *dir);

/**
 * Get all Towers serving a specific community.
 *
 * Returns JSON array of `TowerEntry`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `dir` must be a valid pointer. `community` must be a valid C string.
 */
char *divi_zeitgeist_directory_for_community(const struct ZeitgeistDirectory *dir,
                                             const char *community);

/**
 * Get the total number of Towers in the directory.
 *
 * # Safety
 * `dir` must be a valid pointer.
 */
uint32_t divi_zeitgeist_directory_count(const struct ZeitgeistDirectory *dir);

/**
 * Get the number of searchable Towers.
 *
 * # Safety
 * `dir` must be a valid pointer.
 */
uint32_t divi_zeitgeist_directory_searchable_count(const struct ZeitgeistDirectory *dir);

/**
 * Create a new query router.
 *
 * `config_json` is a JSON `RouterConfig`, or null for defaults.
 * Free with `divi_zeitgeist_router_free`.
 *
 * # Safety
 * `config_json` may be null (uses default config).
 */
struct ZeitgeistRouter *divi_zeitgeist_router_new(const char *config_json);

/**
 * Free a query router.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_zeitgeist_router_free(struct ZeitgeistRouter *ptr);

/**
 * Route a query to the best Towers.
 *
 * Locks `dir` first, then `router`, to prevent deadlocks (consistent ordering).
 * Returns JSON array of `RoutedTower`. Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * `router` and `dir` must be valid pointers. `query` must be a valid C string.
 */
char *divi_zeitgeist_router_route(const struct ZeitgeistRouter *router,
                                  const char *query,
                                  const struct ZeitgeistDirectory *dir);

/**
 * Merge results from multiple Tower batches.
 *
 * `batches_json` is a JSON array of `TowerResultBatch`.
 * Returns JSON `MergedResponse`. Caller must free via `divi_free_string`.
 * Returns null on error.
 *
 * # Safety
 * `batches_json` must be a valid C string.
 */
char *divi_zeitgeist_merge_results(const char *batches_json, uint32_t max_results);

/**
 * Create a new local cache.
 *
 * `config_json` is a JSON `CacheConfig`, or null for defaults.
 * Free with `divi_zeitgeist_cache_free`.
 *
 * # Safety
 * `config_json` may be null (uses default config).
 */
struct ZeitgeistCache *divi_zeitgeist_cache_new(const char *config_json);

/**
 * Free a local cache.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_zeitgeist_cache_free(struct ZeitgeistCache *ptr);

/**
 * Look up cached results for a query.
 *
 * `now` is the current Unix timestamp (used for hit tracking).
 * Returns JSON `CachedQuery`, or null if not cached.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `cache` must be a valid pointer. `query` must be a valid C string.
 */
char *divi_zeitgeist_cache_get(const struct ZeitgeistCache *cache, const char *query, int64_t now);

/**
 * Store results for a query in the cache.
 *
 * `results_json` is a JSON array of `SearchResult`. `now` is the current Unix timestamp.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `cache` must be a valid pointer. C strings must be valid.
 */
int32_t divi_zeitgeist_cache_put(const struct ZeitgeistCache *cache,
                                 const char *query,
                                 const char *results_json,
                                 int64_t now);

/**
 * Remove a query from the cache.
 *
 * Returns true if the query was cached and removed.
 *
 * # Safety
 * `cache` must be a valid pointer. `query` must be a valid C string.
 */
bool divi_zeitgeist_cache_remove(const struct ZeitgeistCache *cache, const char *query);

/**
 * Clear all cached entries.
 *
 * # Safety
 * `cache` must be a valid pointer.
 */
void divi_zeitgeist_cache_clear(const struct ZeitgeistCache *cache);

/**
 * Get the number of cached queries.
 *
 * # Safety
 * `cache` must be a valid pointer.
 */
uint32_t divi_zeitgeist_cache_len(const struct ZeitgeistCache *cache);

/**
 * Check whether the cache is empty.
 *
 * # Safety
 * `cache` must be a valid pointer.
 */
bool divi_zeitgeist_cache_is_empty(const struct ZeitgeistCache *cache);

/**
 * Take a snapshot of the cache for persistence.
 *
 * Returns JSON `CacheSnapshot`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `cache` must be a valid pointer.
 */
char *divi_zeitgeist_cache_snapshot(const struct ZeitgeistCache *cache);

/**
 * Restore a cache from a snapshot.
 *
 * `snapshot_json` is a JSON `CacheSnapshot`.
 * Returns a new cache pointer. Caller must free via `divi_zeitgeist_cache_free`.
 * Returns null on error.
 *
 * # Safety
 * `snapshot_json` must be a valid C string.
 */
struct ZeitgeistCache *divi_zeitgeist_cache_restore(const char *snapshot_json);

/**
 * Create a new trend tracker.
 *
 * `config_json` is a JSON `TrendConfig`, or null for defaults.
 * Free with `divi_zeitgeist_trends_free`.
 *
 * # Safety
 * `config_json` may be null (uses default config).
 */
struct ZeitgeistTrends *divi_zeitgeist_trends_new(const char *config_json);

/**
 * Free a trend tracker.
 *
 * # Safety
 * `ptr` must be valid, called exactly once.
 */
void divi_zeitgeist_trends_free(struct ZeitgeistTrends *ptr);

/**
 * Record a search query as a trend signal.
 *
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `trends` must be a valid pointer. `query` must be a valid C string.
 */
int32_t divi_zeitgeist_trends_record_query(const struct ZeitgeistTrends *trends,
                                           const char *query,
                                           int64_t now);

/**
 * Record topics from a Tower's Semantic Profile.
 *
 * `topics_json` is a JSON array of strings.
 * Returns 0 on success, -1 on error.
 *
 * # Safety
 * `trends` must be a valid pointer. `topics_json` must be a valid C string.
 */
int32_t divi_zeitgeist_trends_record_tower_topics(const struct ZeitgeistTrends *trends,
                                                  const char *topics_json,
                                                  uint64_t content_count,
                                                  int64_t now);

/**
 * Apply time decay to all trends and prune dead ones.
 *
 * # Safety
 * `trends` must be a valid pointer.
 */
void divi_zeitgeist_trends_decay(const struct ZeitgeistTrends *trends);

/**
 * Get the top N trending topics, sorted by score.
 *
 * Returns JSON array of `TrendSignal`. Caller must free via `divi_free_string`.
 *
 * # Safety
 * `trends` must be a valid pointer.
 */
char *divi_zeitgeist_trends_top(const struct ZeitgeistTrends *trends, uint32_t n);

/**
 * Get a specific trend by topic name.
 *
 * Returns JSON `TrendSignal`, or null if not found.
 * Caller must free via `divi_free_string`.
 *
 * # Safety
 * `trends` must be a valid pointer. `topic` must be a valid C string.
 */
char *divi_zeitgeist_trends_get(const struct ZeitgeistTrends *trends, const char *topic);

/**
 * Get the number of tracked trends.
 *
 * # Safety
 * `trends` must be a valid pointer.
 */
uint32_t divi_zeitgeist_trends_count(const struct ZeitgeistTrends *trends);

#endif  /* DIVINITY_FFI_H */
